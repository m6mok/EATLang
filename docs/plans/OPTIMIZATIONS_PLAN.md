# OPTIMIZATIONS_PLAN.md — оставшиеся оптимизации (сводный план)

- **Статус:** 🚧 В ПРОЦЕССЕ (большинство пунктов закрыто; §1.5
  NONE-гигиена выполнена 2026-07-17 (bounds −29 %); §9.1 индуктивная
  проверка кандидат-интервала (клэмп-накопитель) выполнена 2026-07-18
  (обобщение %K → маска/min/насыщение, класс без committed-потребителя);
  §1.6 разведка+дизайн типа «индекс-или-NONE» 2026-07-17 — вердикт
  «не перф», дизайн 📐 в [NONE_TYPE_PLAN](NONE_TYPE_PLAN.md); остаток —
  §3.1 зеркало `read_span` (паритетный долг) и research-направления
  §1.4/§1.5 без дешёвых ходов)
- **Роль:** реестр (живой) — что можно/нельзя оптимизировать, с вердиктами замеров
- **Правки языка/грамматики:** нет
- **Новые аксиомы ОС (SPEC §7):** нет
- **Зеркало в selfhost:** по пунктам (напр. §3.1 зеркало `read_span` отложено)
- **Опирается на:** —
- **Блокеры:** нет (остаток — по профилю/замеру и решениям пользователя)
- **Блокирует:** —

Эталон всех замеров —
`build/bench/baseline_3cebb58.txt` (двадцать первая итерация
[FINDINGS](../../tests/bench/FINDINGS.md)); каждая работа ниже меряется
против него секциями `make bench` и `tests/verify_catalog.py`.
Выполнено к этому моменту (не переоткрывать): слой 0 — runtime.c с
`-O2` (Read +33 %, Io +26 %, 0be075c); слой 1а — llvm.assume из
доказанных requires (нейтрален, канал под верификатор, b88566a);
трек 3, ядро — инварианты пулов/полей + сентинел-дырки
(5270 → 5356 из 6945, 7e02d1e). Уроки, которые ограничивают выбор:
llvmlite не векторизует циклы с ветвлением и масочные редукции;
предсказанные trap-ветки на Apple Silicon почти бесплатны (~3–5 %) —
выигрыш снятых проверок ищи в размере кода и на in-order МК.

## 1. Верификатор: добить остаток (трек 3, продолжение)

### 1.1 Дизъюнктивный домен «интервал с дыркой» через циклы — СДЕЛАНО 2026-07-14

Реализовано (двадцать пятая итерация FINDINGS, verifier.py; зеркало
`selfhost/Verify.eat` — [SELFHOST_VERIFIER_PLAN](SELFHOST_VERIFIER_PLAN.md)
этап 3, 2026-07-16):
дырка `State.holes` теперь проходит через циклы. `_loop_exit` —
объединение входа и всех записей тела (сборщик `_exit_rec`);
hive-summary (`_hive_summary`, `ret_holes`) — для аккумуляторов
`return f`; 2-проходный инвариант тела (проба без следов + real) —
для масочных счётчиков `slot = (slot+1) & M` и обходов `c = pool[...]`.
Bootstrap 5356 → 5456; TrapBench 133 → 138; кейсы 42–43. Риск
«проба оставляет следы» закрыт флагом `_probe`, глушащим `_mark`,
учёт requires и return (не снапшотом — дёшево).

**Остаток**: bounds 921 (Check.eat 471). Дальше — не дизъюнктивный
домен, а §1.2 (суммы полей) и семантические инварианты дерева
(`nns[nfc[c]...]` без guard'а на промежуточном узле — интервалам
недоступно). Кодогенный §2.1 независим.

### 1.2 Реляционные инварианты полей — ПЕРЕОЦЕНЕНО, ЗАКРЫТО 2026-07-14

Гипотеза «assert 0/103 = суммы полей `self.ln + o.ln <= 256`»
**опровергнута разбором** (двадцать восьмая итерация FINDINGS,
карта в VERIFICATION_PLAN): 103 непроказанных assert доминированы
`assert <вызов>(...)` в test-блоках (is_space/hex_val/same/
parse_status), которые верификатор бракует по `_has_call`; сумм
полей ~4 штуки. Доказать основную массу = comptime-вычисление
вызова (§5), а не разностные инварианты. Направление закрыто —
не окупается интервалами.

### 1.3 Hive через границу модулей — ЗАКРЫТО замером 2026-07-17

Пробный патч (маршрут hive-summary в `_contract_iv`-ветке обоих
call-путей, §1.4): **дельта 0** на всех драйверных программах
репозитория (json 318/570, modules 129/237, elif 124/217 — байт в
байт с базой). Потребителей нет: компилятор собирается конкатенацией
(один модуль, SPARK-границы не возникает), а в `lib/` нет
межмодульных find-функций «индекс-или-NONE», чей результат
индексирует пул у вызывающего. Плюс цена выросла: у верификатора
теперь зеркало (`Verify*.eat`, дампы байт-в-байт) — любое усиление
доказательств зеркалится. Вернуться, если такой потребитель появится
в `lib/`.

### 1.4 Разведка остатка `-O` — решение пользователя 2026-07-17

Остаток на самоприменении фронтенда (`eatc verify -O`, футер):
доказано 4713/5973 (79 %); в рантайме bounds 762 (60 % остатка,
класс NONE-links/вложенные чтения пулов), overflow 272 (loop-carried
аккумуляторы — недоказуемо интервалами), requires 97, assert 91
(test-блоки, в бинарник не эмитятся), cast/div/ensures/shift 38.
`br i1` в `-O`-IR 3 899 (из них ~1 180 trap), memcpy 229 (canon ==
`-O`; §2.2 по этим числам ниже пола — LLVM ест их при инлайне).

Диагностическая итерация ПРОВЕДЕНА 2026-07-17 (патчи эталона с
откатом, тридцать пятая итерация FINDINGS). Итоги трёх проб:

1. **Классификация 762 bounds** (по исходным строкам): 358 (47 %) —
   банки `a/65536` с NONE-links без guard'а; ~335 (44 %) — «простые»
   индексы, доминированные кадровыми обращениями машин
   (`self.fp[f]`/`self.fnod[f]` при `f = nf−1` — data-flow-инвариант
   «nf ≥ 1 внутри шага», не NONE-класс); 53 — выражения `i±j`;
   14 — вложенные `p[q[..]]`. По модулям: CheckBody 232, Parser 176,
   Check 143, CheckConst 109, CheckDump 88. Потолок NONE-домена —
   ~370 из 762 (≈30 % всего остатка), и это research с риском
   незвуковости (инвариант «k==16 ⇒ ребёнок есть» — отношение между
   полями узла, вне интервалов).
2. **Прунинг веток (кандидат в проход №3 оси) — ОТВЕРГНУТ**:
   вырожденных if-условий на самоприменении 6 из 1823 (0.3 %,
   все — защитные страховки вида `if idx < 1024` при доказанном
   инварианте), на mos6502 — 0 из 130. Честный код не держит
   мёртвых веток; выход прохода — шум.
3. **§1.3 hive через границу — дельта 0** (см. §1.3, закрыто).

**Вывод разведки: остаток `-O` у потолка добросовестных средств.**
Недоказанное — семантические инварианты программы (NONE-links,
`nf ≥ 1`) и loop-carried аккумуляторы; дешёвых проходов №3 нет.
Хостовую скорость двигать в `lib/` (NumParse 27M, StrCmp 39M —
эталонные разрывы; ✅ снято кандидатом 3 §7.4: 51M и 52M оп/с),
МК-размер уже собран элизией.

### 1.5 NONE-гигиена пулов — открыто второй разведкой 2026-07-17

Вопрос пользователя «сделать NONE системной константой?» вскрыл
устройство класса 358 банк-bounds. NONE уже «системен» для
верификатора (`lib/Const.eat`, максимум u32 = сентинел дырочного
домена); эксперименты на /tmp-копиях входа показали двухчастную
причину недоказуемости:

1. **Инварианты пулов отравлены единичными записями без границ.**
   Минимальная репродукция идиомы «hoist в локаль + `assert x !=
   NONE` → банк доказан» зелёная, а в реальном коде guard не работал:
   `Check.unlink_top(e, prev)` — у `prev` нет `requires prev <
   131072`, и одна запись `nlc[0][0] = prev` растягивает плотную
   часть инварианта пула `(Parser, nlc)` на всю программу; пулы травят
   друг друга дальше копиями линков (`nns → nfc/nlc`).
2. **Чтения без guard'ов** (`nns[nlc[p]/65536][...]` напрямую) — код
   опирается на межполевые инварианты структуры данных («`nfc[p] !=
   NONE` ⇒ `nlc[p] != NONE`»), интервалам недоступные в принципе;
   «системность» константы это не изменит.

Замер (оба патча вместе, /tmp): чистка двух отравляющих записей
(+`assert m/prev < 131072`) + hoist-guard в `append_child` →
банк-bounds доказан; на сайт — −1 runtime-bounds, +1 runtime-assert
(нейтрально по счёту проверок), assert покрывает **все** последующие
чтения линка — чистый выигрыш на кластерах из нескольких чтений
(`insert_field` и т.п.).

**Направление (правки только `.eat`-кода, компилятор не трогается):**
(а) аудит отравляющих записей — `requires` на параметры-линки,
единицы мест; (б) идиома «guard — доказательство» (hoist + `assert !=
NONE`) на кластерах чтений. Потолок класса ~358 bounds. Системная
альтернатива — тип «индекс-или-NONE» с принуждением к narrowing
(правка языка, решение пользователя, большой ценник) — превращает
класс из «недоказуемо» в «непредставимо», рассмотреть отдельным
планом при желании.

**СДЕЛАНО 2026-07-17** (тридцать шестая итерация FINDINGS): на
самоприменении фронтенда runtime-bounds 762 → 539 (−223, банковый
класс 358 → 151), runtime-проверок всего 1260 → 1031 (−18 %), цена —
+14 runtime-assert идиомы; доказанность 79 → 82.6 %. Рецепты (все —
правки `.eat`, зеркал не требуют): чистка отравляющих записей
(assert на неограниченный параметр-линк перед записью, ветвление
`== NONE` вместо копии «дырочного» значения — одна запись hull'а
растягивает плотную часть инварианта пула на весь u32); хелперы
`first_child`/`second_child`/`ty_a`/`ty_b`/`frame_node`/`qp_next`
(assert внутри, наружу плотный результат через ensures); bind-return
вместо `return self.f(...)` в обёртках find-функций (прямой return
вызова теряет hive-дырку summary). Попутно Parser.eat упёрся в
потолок 2500 строк — распил через extend (ParserExpr.eat, машина
выражений), по образцу REFACTOR §6.4. Остаток 539: кадровые/стековые
индексы 233 (класс «nf ≥ 1», data-flow), банковые 151 (цепочка
`tres`/`xa` — поля без сентинела в домене верификатора, research),
прочее 155 (payload-стеки vs/vm, `i±j`). Дальше дешёвых ходов нет —
хвост либо research-домены верификатора (сентинел для полей), либо
системный тип «индекс-или-NONE».

## 2. Кодоген: факты верификатора → LLVM, копии

### 1.6 Тип «индекс-или-NONE» — разведка+дизайн 2026-07-17 (вердикт: не перф)

Разведка потолка класса банковых ссылок (§1.5, хвост 151 из 539)
системным типом. Полный дизайн — [NONE_TYPE_PLAN](NONE_TYPE_PLAN.md)
(📐 черновик); числа — FINDINGS сорок первая итерация. Компиляторы не
тронуты (дизайн на бумаге, эскизы на /tmp).

**Дизайн `idx<N>?` (звучен).** Ниша-кодированный `Option<idx<N>>`:
носитель `u32`, `NONE = 2³²−1` в свободной нише (домен индексов
≤ 65536/131072 не пересекается — прецедент `NO_CONN` SPEC §7). Тип
**проецируется на уже существующий дырочный домен верификатора**
(`State.holes` = «плотное `[0,N)` ∪ {NONE}», `_ty_iv`
тип→интервал — верификатор своей решётки типов не ведёт), а не
дублирует его; тег-`Option` (`{i32,i32}`) отвергнут — двойной размер,
не влезает в плоские пулы `[[u32;65536];2]`. Narrowing по `if != NONE`
(усиление правила 7) делает unguarded-чтение линка **ошибкой
компиляции**, а отравление инварианта пула (§1.5) — непредставимым:
переход «недоказуемо → непредставимо» показан на `unlink_top`,
`insert_field`, `first_child`.

**Потолок класса:** ~130–190 из 539 (центр ≈ 160, ≈ 30 %) — все 151
банковых `nns/nfc/nlc` + ~30–40 индекс-или-NONE «прочих» (`ya/yb`,
`qp`). НЕ берёт кадровый класс 233 («nf ≥ 1» data-flow), payload-стеки
`vs/vm`, арифметику `i±j`.

**Ценник:** ~1150–1350 строк, ~35 файлов, ~25 с байт-в-байт зеркалом;
самый дорогой/рискованный кусок — **флоу-narrowing** (машинерии
сужения типа по условию в тайпчекере нет сегодня ни в каком виде) +
bootstrap-фикспойнт. Зеркало интрикатно как §3.1 read_span.

**Вердикт: перфом не окупается.** ~160 bounds < чем §1.5 сняла без
правок компилятора (−223), цена на порядок выше. Ценность — свойство
«непредставимо» (фиксация регрессии, структурная идиома, перенос на
пулы `lib/`), довод корректности/эргономики, не скорости. Держать
дизайн 📐 готовым; ввод — отдельным решением пользователя при мотиве
помимо счёта (флоу-narrowing нужен языку сам по себе; реальный баг из
забытого NONE-assert'а; целевая картина selfhost).

## 2. Кодоген: факты верификатора → LLVM, копии

### 2.1 !range на чтениях пулов — ОТВЕРГНУТО замером 2026-07-14

Реализовано и откачено (двадцать шестая итерация FINDINGS).
Верификатор аннотировал Index-узлы `val_range=(lo,hi,sent)`
(build-only), кодоген эмитил `!range`, дырка → оборачивающийся
интервал; 562 сайта, канон чист, все ворота зелёные, семантически
корректно. Но замер: TrapBench x512 0.77 → 0.83 с (воспроизводимый
регресс ~8 %, не шум), выигрыша нигде. Политика §2.1 была «оставить
только если нейтрально» — не выполнена, откат. Канал «инварианты →
LLVM» уже закрыт `nsw`/`nuw` + `llvm.assume`; `!range` из пуловых
инвариантов на llvmlite=LLVM 22 / Apple Silicon вредит горячему
циклу. Не переоткрывать без нового стека LLVM.

### 2.2 sret прямо в слот назначения (изменение канона — оба эмиттера)

`const x: T = f(...)` сегодня: свежая alloca вызывающего → вызов →
memcpy в слот x (`emit_call` codegen.py:1104, `copy_into` :288).
Передавать слот x как sret-указатель напрямую. **RetBench показал:
при инлайне LLVM уже съедает копию (404M вызовов/с)** — выигрыш
только у неинлайнящихся функций (большие тела). Selfhost: машины
компилятора крупные — проверить профилем, сколько memcpy-возвратов
живёт в SelfIr до начала работ. **Цена высокая**: меняется
канонический IR → зеркало selfhost/Ir.eat байт-в-байт, фикспойнт,
все дампы. Делать только если профиль покажет ≥ единиц процентов.
**Профиль снят 2026-07-17 (§7.3): в горячем пути эмулятора sret-путь
и копии стоят 0 % стены — НЕ делать.**

### 2.3 Хвосты трека 4 (selfhost-код, по остаточному профилю SelfIr 0.13 с)

- `lb_tp`/копии в банкованные пулы: аффинная по-банковая форма,
  которую LLVM распознаёт как memcpy (метрики: CopyBench 220M,
  BankBench 250M, compiler-секция); потенциал ~десятки мс;
- `g_hash` инкрементально при записи в пул вместо пересчёта на
  каждый `g_commit` (Ir.eat);
- `write_span_esc`/экранированные спаны дампов — батчить между
  escape-символами (профиль дампов lex/parse: 1.59–2.45 МБ вывода).

Перед каждым — свежий `sample`-профиль нативного SelfIr: после
модульного этапа и инвариантов расклад мог сместиться.

**Оценка 2026-07-14: ниже пола измерения.** Фазы SelfIr: lex 0.01,
parse 0.02, sig 0.01, typed 0.09, ir 0.14 с — эмиссия IR (ir−typed)
≈ 0.05 с. Хвосты §2.3 — под-части этих 0.05 с, абсолютный выигрыш
единицы мс, а разброс bench ±10–20 мс. SelfIr (0.14 с, ×9 к Python)
слишком быстр для чистого `sample` (пул узлов ≤131072 не даёт
раздуть вход). Оптимизировать вслепую = риск нерегистрируемого
регресса. Трек 4 подтверждённо «низкий приоритет»: браться, только
если появится дешёвый способ надёжного профиля (TCG-плагин под МК
или инструментированный прогон).

## 3. Рантайм-шим и сборка

### 3.1 Аксиома read_span — Python-сторона ПРОВЕРЕНА, selfhost-зеркало отложено (2026-07-14)

Решение пользователя «давай всё» получено. **Python-сторона
реализована и доказана, но откачена** (паритет: «частично зелёное не
коммитится»): `read_span(a: [u8; N], off, len) -> u32` симметрична
write_span, но ПИШЕТ в массив, поэтому `a` обязан быть изменяемым
lvalue (`let`) — новая проверка мутабельности (переиспользует логику
`_check_mutable_receiver`). Сделано и проверено на ветке (не в
HEAD): runtime.c host+mcu (курсор общий с read_byte), codegen.py
(declare + gen_read_span + dispatch), typechecker (_read_span +
_check_mutable_array + сигнатура), interpreter (mutate массива —
`obj[off+i]=b`), verifier (bounds как write_span + **убийство пула
`a`**: read_span пишет произвольные u8, иначе чтение из
заполненного пула получило бы заниженный интервал — несостоятельно),
SPEC §7. **Тест `interp == binary` зелёный**, проверка мутабельности
отвергает `const`-массив.

**Блокер — selfhost-зеркало** (Check.eat + Ir.eat), интрикатное как
u64/extern: ДВЕ раздельные системы кодов (Check `xa` write_span=110,
Ir emit-код write_span=8) + typed-дамп, всё байт-в-байт, плюс
`xrspan` (bounds+gep как `xwspan` :3143, но результат i32 через
`rpre`/`e_rspan`, не void `vpre`/`e_wspan`), новый код в обеих
системах, ert/result-конвенция значащего вызова. Оценка — отдельный
фокус-этап (как каждый axiom-коммит в истории); риск — bootstrap
фикспойнт. Python-дизайн де-рискован, зеркало механическое.

**По выигрышу оговорка:** после слоя 0 read_byte уже 466M байт/с;
read_span экономит только цену вызова-аксиомы на байт, а обработку
(классификацию байта лексером) — нет. Реальный выигрыш на лексере
skromный; замерить ReadBench-вариантом до адаптации лексера.

### 3.2 LTO как release-режим — СДЕЛАНО 2026-07-14 (`--release`)

Флаг `eatc build --release`/`-r` реализован (двадцать девятая
итерация FINDINGS): `clang -O2 -flto` над каноническим `.ll` +
runtime.c, аксиомы инлайнятся; SelfIr файл −19 %, `__text` −25 %,
цена — время линковки (2.6 → 5.0 с). Канон и семантика не тронуты.
Для финальных/МК-сборок, не для цикла разработки.

### 3.3 Порог батчинга write_span — СДЕЛАНО 2026-07-15 (32 → 16)

Гибрид «< 32 байт побайтно, иначе fwrite» калибровался при -O0-шиме.
Спан-микробенч после -O2 (тридцатая итерация FINDINGS): кроссовер ~12
байт (fixed-cost fwrite ~22 нс/вызов против ~2 нс/байт), к 16 байтам
fwrite ×1.4. Порог опущен к измеренному оптимуму (32 → 16, риск-free —
C-шим, паритет не трогается). **На продакшн-workload'ах ниже пола**:
SelfIr-эмиссия/parse-дамп/typed-дамп compute-bound, спаны окна 12–31
тонут в вычислении; IoBench нечувствителен. Корректно для
write-доминированных путей, нейтрально на текущих. Закрыто.

### 3.5 Скорость полного гейта — СДЕЛАНО 2026-07-17 (243 → 140 с)

Профиль (впервые): 10 целей, 4м03 итого; **`verify_selfhost` —
132 с (54 %)**, дальше `verify_selfhost_verify_all` 36 с,
`verify_selfhost_opt` 21 с, `verify` 15 с, смок 13 с, `bootstrap`
12 с, `trapcodes` 11 с, `check` 3 с, `suite`/`sig` ≈ 0. Старт
процесса `eatc` — 0.06 с (не доминирует); время — в сотнях
последовательных Python-прогонов пофайловых циклов на одном ядре.

Сделано (решение пользователя; правки только Makefile + новые
воркеры `tests/gate/*.sh`, паритет не тронут): пофайловые циклы
пяти целей — через `xargs -P $(JOBS)` (JOBS = ядра), временные
файлы воркеров уникальны (mktemp-каталог на файл); пять сборок
`Self*` в `verify_selfhost` — параллельно (`&` + `wait`); хвост
`verify_selfhost` (поток драйвера, конкатенации, самоприменение,
четыре interp==native) — независимые блоки в
`tests/gate/selfhost_tail.sh` параллельно.

**Числа (полный гейт, worktree на чистом HEAD): 243 → 140 с
(−42 %)**; `verify_selfhost` 132 → 54 с, `verify_selfhost_verify_all`
36 → 18 с, `verify_selfhost_opt` 21 → 17 с. Остаток — одиночные
`eatc build` компиляторных бинарников (~10–14 с каждый, внутри цели
уже параллельны) и самые долгие interp-блоки хвоста. Бонус:
ограничение «один гейт на машину» снято (WORKFLOW §4) — сверочные
дампы больше не делят /tmp; гейт этой правки прогнан в worktree
при живом соседе в общем дереве. Семантика сверок не менялась:
те же команды, те же diff'ы, только порядок строк вывода
недетерминирован внутри цели.

Уроки: (1) `VAR=x cmd` из переменной POSIX-shell не является
присваиванием — воркеры зовут `env $EATC …`; (2) негативные кейсы
эталона (`eatc verify` с ненулевым кодом) в исходных циклах
маскировались `;` — в воркерах с `set -e` это явные `|| true`.

### 3.4 Стоимость верификатора в build — O(n²) устранён 2026-07-15

cProfile verify() на SelfIr: `_func_by_key` — линейный скан по
`program.decls` на каждый межпроцедурный вызов (0.206 с self /
24642 вызова). Заменён индексом (dict, первое совпадение), verify()
351 → 314 мс (−10 %), доказательства идентичны. Побочный вывод:
2-проход тел циклов (§1.1) **не** бутылочное горло — гейт не нужен.
Остаток self-time — `_iv` (интервал-эвалюатор, ядро) и `_path_of`
(у пола) — не дешёвые сканы. Тайпчекер профилирован отдельно: плоский,
супер-линейности нет. Дальше по верификатору дёшево не выжать.

## 4. МК-профиль: где снятые проверки стоят по-настоящему

- **Цена проверок во флеше — измерено 2026-07-14** (двадцать седьмая
  итерация FINDINGS, таблица в [mcu/README.md](../../mcu/README.md)):
  mos6502 на Cortex-M3, тот же `.ll` с верификатором и без —
  trap-сайтов 322 → 61 (−81 %), `.text` 6156 → 5624 Б (−8.6 %).
  Тезис замкнут: проверки нейтральны на суперскаляре, стоят флеша на
  МК. Самодостаточные константные программы (`work(7)`) непригодны —
  LLVM сворачивает их целиком; нужен вход из ROM (mos6502);
- **цикловый замер (QEMU `-icount`)** — отдельный шаг, нужен
  TCG-плагин счётчика инструкций (в homebrew-qemu по умолчанию нет);
  .text-метрика детерминирована и без него;
- прошивочные порты и периферийный шим — отдельный трек
  ([MCU_PLAN](MCU_PLAN.md)), не смешивать.

## 5. Comptime — ярус A СДЕЛАН 2026-07-15 ([COMPTIME_PLAN](COMPTIME_PLAN.md))

A1 (скаляры) реализован в обоих компиляторах одним паритет-этапом
(тридцать вторая итерация FINDINGS): constexpr-из-вызовов, строгая
по-операционная семантика (= бинарник), бюджет 10⁶ шагов, глубина 64;
вычислитель selfhost — секция struct Check (Eval.eat отдельным файлом
невозможен из-за алиасинг-правила §5.4). Дампы байт-в-байт, фикспойнт
держится, кейсы 44–48. Синергия с треком 3 подтверждена: comptime-
constexpr → точка [v, v] → доказанные границы. A2 (constexpr-массивы в
`__const` — таблицы во флеш) — кейсы 49–50. **Ярус B реализован**
(COMPTIME_PLAN §11, `eatc build --fold`, по умолчанию выключен):
свёртка вызовов с константными аргументами в телах в литералы,
Python-эталон только (как снятие проверок; `eatc ir` не сворачивает →
паритет цел). Замер на пробе: 6 вызовов, call-инструкций 98 → 92.

## 6. Порядок по умолчанию и готовность

1. **1.1 дизъюнктивный домен** (наибольший остаток; сделано,
   зеркало в selfhost — SELFHOST_VERIFIER_PLAN этап 3) → попутно 1.3;
2. **2.1 !range из инвариантов** (дёшево, тот же контур замеров);
3. **4 (МК-метрика цены проверок + размер)** — закрывает вопрос
   «сколько стоят проверки там, где это важно»;
4. **2.3 хвосты по профилю** (lb_tp → g_hash → esc-спаны), каждый —
   отдельный коммит с числами до/после;
5. **3.1 read_span / 3.2 LTO-флаг / 2.2 sret** — по решениям
   пользователя и профилю;
6. **5 comptime** — после решений §7.

Каждый этап: `make check verify verify_suite verify_selfhost
verify_bootstrap verify_trapcodes` целиком зелёный + секции bench
до/после + запись в FINDINGS (стиль итераций). Правки только
verifier.py не требуют зеркала; правки канона IR — оба эмиттера и
фикспойнт; новые аксиомы — решение по SPEC §7 до кода.

## 7. Кросс-языковой замер 2026-07-17: внешние рычаги

Источник — контур `make bench_crosslang` (CROSSLANG_BENCH_PLAN ✅,
отчёт tests/bench/CROSSLANG.md): EATLang против C/Rust/Go/Python на
шести микробенчах и эмуляторе mos6502, сверки stdout байт-в-байт,
счётчики instructions/cycles. Итог фона: геосреднее EAT/C = 0,79,
паритет 1,02 без выбросов в пользу EAT. Рычаги, извлечённые из цифр:

### 7.1 llvm.assume в оси -O — СДЕЛАНО 2026-07-17: снят как инертный, гипотеза вреда ОПРОВЕРГНУТА

Вариант A доставлен: `llvm.assume` из доказанного `requires` (слой 1а,
23-я итерация FINDINGS) не эмитится вовсе — элизия и `nsw`/`nuw`
остаются; правка зеркальная (codegen.py + IrStmt/IrEmit, минус
`assume_safe`/`assume_leaf`/`e_assume`, −130 строк зеркал), паритет
`eatc ir -O` == SelfIrOpt байт-в-байт держится, полный гейт зелёный.

**Гипотеза «assume съедает выигрыш элизии» опровергнута числами**:

- branch: в оси `-O` assume **0 вызовов** (нечему вредить — IR оси
  совпадает с build с точностью до trap-строк); разрыв 1,46–1,52
  против 1,09–1,28 у build — это **конвейер сборки** (llvmlite у
  build против clang -O2 по .ll), передан в §7.2;
- mos6502: снятие всех 15 assume не сдвинуло инстр/шаг
  (56,32 → 56,29–56,30), стена 4,00 → 3,89–3,90 нс/шаг при дрейфе
  канона −2…−2,7 % (шум фона); разрыв оси `-O` против канона
  (56,3 против 54,6 инстр/шаг на одном clang) — цена самой элизии
  (кроссовер аутлайн/инлайн, уже разложено в §7.3, п. 1);
- sort/u64: без изменений (0,404/4,73–4,83) в коридоре шума.

Ожидания −5…−10 % не было и быть не могло — согласуется с §7.3
(«assume ≈ 0, проверено вырезанием» на llvmlite-пути; теперь
подтверждено и на clang-пути). Assume оставлен снятым: инертный
механизм в parity-критичных зеркалах — чистый пассив; канал «факты,
недоступные LLVM» при нужде вернётся отдельным решением.

Попутные находки (детали — FAULTS, FINDINGS 38-я итерация):
посылка «build = элизия без assume» из отчёта CROSSLANG была
**неверна** (build эмитил те же assume — общий кодоген); гейт
`verify_selfhost_opt` не покрывает бенч-конкатенацию mos6502 — под
ней вскрыт предсуществующий дрейф одного решения верификаторов
(`hex_digit` requires: Python proven, selfhost runtime; 330 против
331 из 491) — кандидат-этап правки верификатора.

### 7.2 Конвейер сборки — СДЕЛАНО 2026-07-17: llvmlite ≥ clang, маршрут build остаётся

Чистый эксперимент проведён (`tests/bench/crosslang/backends.py`):
один и тот же байт-идентичный .ll — неоптимизированный, с элизией,
без assume, который `eatc build` пишет рядом с бинарником — прогнан
через оба бэкенда; методика run.py (медиана 5 прогонов), два полных
прохода подряд, дрейф между проходами ≤ 4 %. Сверки stdout всех
вариантов байт-в-байт зелёные.

**Вердикт: clang-путь НЕ стабильно ≥ — наоборот, штатный
llvmlite-путь быстрее или равен на всех 5 нагрузках.** Смена
маршрута build не предлагается, вопрос снят.

| нагрузка | build (llvmlite), нс/оп | .ll → clang -O2 | clang к build |
| --- | --- | --- | --- |
| arith | 1,154–1,156 | 1,150–1,183 | −0,3…+2,3 % (паритет) |
| sort | 0,381–0,384 | 0,396–0,398 | +3,1…+4,5 % |
| branch | 1,096–1,132 | 1,440–1,447 | **+28…+31 %** |
| u64 | 4,426–4,509 | 4,428–4,601 | −1,8…+4,0 % (паритет) |
| mos6502 (нс/шаг) | 3,632–3,665 | 3,846–3,878 | +4,9…+6,8 % |

Разложение (гибриды мид-энд × кодоген на том же .ll): на **branch
виноват кодоген clang** (clang-мид → llvmlite-кодоген быстр как
build: 1,08–1,13; llvmlite-мид → clang медлен как clang: 1,43–1,46;
инстр/оп 14,47 против 15,48, IPC 3,84 против 3,09 — llvmlite-бэкенд
строит csel-цепь плотнее); на **mos6502 виноват мид-энд clang**
(llvmlite-мид → clang быстр: 3,69; clang-мид → llvmlite медлен:
3,80–3,92). Причина обоих направлений одна: llvmlite 0.48 несёт
LLVM 22.1.0 — **новее** Apple clang 21.0.0; «родной clang» на деле
старее штатного конвейера. `-flto` (путь `--release`) по скорости
== clang -O2 везде (на mos6502 +5,8…+7,2 % к build), выигрыш только
в размере: 33К против 50К на микробенчах — release остаётся осью
размера, не скорости.

Иллюзия исходного замера «clang не хуже на 5/7» разобрана: там
сравнивались РАЗНЫЕ IR (clang на каноне против llvmlite на элизии),
а канон эмулятору сам по себе быстрее элизии (§7.3, п. 1) — на
байт-идентичном .ll преимущество clang исчезает. Урок и версионный
перекос текстового .ll (LLVM 22 ↔ clang 21) — FAULTS 7.2;
числа и методика — FINDINGS, 39-я итерация.

Дозамер 2026-07-17, шестая нагрузка **http** (`--only http
--decompose`, два прохода, дрейф ≤ 3 %): паритет всех пяти вариантов
(build 301–306 нс/запрос, clang -O2 302–305, -flto 300, гибриды
303–314) — вердикт «маршрут build не меняется» распространён и на
разрыв EAT/C = 1,35: он не в конвейере, а в самом коде. Гипотеза
«дело в отсутствии TBAA» опровергнута разведкой §8.3: alias-инфа не
рычаг ни с EAT-стороны (TBAA-прототип: −1,2 % инстр, 0 стены), ни с
C-стороны (-fno-strict-aliasing: 0 инстр); носитель разрыва — идиома
сканов с нуля в lib/Http.eat (75 % инстр, ≈97 % стены), рычаг —
§8.1 (span-API + единый проход от позиции).

### 7.3 Эмулятор: инструкций меньше C, стена хуже — ВЕРДИКТ 2026-07-17

Расследование проведено (тридцать седьмая итерация FINDINGS; xctrace
недоступен — на машине только CommandLineTools, без root; метод —
попарные изоляции: один вход 936,9 M шагов, счётчики
`/usr/bin/time -l`, варианты IR в /tmp, сверки stdout байт-в-байт).

**Подозреваемые §2.2 оправданы полностью — доля стены 0 %.** В
горячем пути эмулятора sret/memcpy и «копий let self» нет вообще:
`let self` в бинарнике — указатель; memcpy в модуле 8 штук, все —
холодные строковые хелперы Rt и разовый `cpu_new` на старте.
**Рекомендация: §2.2 НЕ делать** — канон IR не менять, выигрыша нет
ни на эмуляторе, ни (по RetBench) на инлайнящихся вызовах.

Реальное разложение разрыва build → C (12,35 → 10,97 такта/шаг,
+12,6 % стены), цепочкой изоляций на llvmlite-конвейере build:

1. **Элизия оси -O вредит эмулятору**: канон (все-trap) через тот же
   llvmlite — 11,59 такта/шаг, т.е. элизия стоит **0,76 такта/шаг**
   (на clang-пути 1,17). Механизм — кроссовер 2×2 (аутлайн/инлайн ×
   канон/элизия): при аутлайновых хелперах канон быстрее (trap-icmp
   в вызывающем восстанавливает диапазон результата через границу
   вызова — `ensures result <= 65535` LLVM не видит), при полном
   инлайне быстрее уже элизия (маски видны насквозь). assume ≈ 0
   (16 вызовов на модуль), nsw/nuw = 0 — проверено вырезанием.
2. **Аутлайн 4 горячих хелперов** (`ea_mem`/`ea_alu`/`shift_val`/
   `flags_byte` — llvmlite и clang их не инлайнят): **0,36 такта/шаг**
   (11,59 → 11,23 канон+alwaysinline через llvmlite).
3. **Остаток формы кода** против C: 0,26 такта/шаг (плотность цепочки
   зависимостей шага: load pc → load mem[pc] → диспетчер → операнд →
   флаги; C несёт больше независимых инструкций — IPC 5,31).

**Находка-рычаг (лучший вариант EAT, быстрее C)**: штатный build-IR
(элизия) + `alwaysinline` четырём хелперам post-parse через
llvmlite-API (`ValueRef.add_function_attribute`) — **10,61 такта/шаг,
49,2 инстр/шаг, 2,92 с — быстрее C -O2 на 3,3 % тактов (−7 % стены)**;
канонический текст .ll не меняется (пишется до оптимизации), зеркало
не требуется. Слепое поднятие `inlining_threshold` вредит (13,87 на
build-IR, 17,38 на каноне — раздувает всё). Кандидат-этап: эвристика
инлайн-хинтов малых листовых функций в build-конвейере (Python-only,
решение пользователя).

**Второй свидетель (2026-07-17, бенчи U128)**: на `U128Bench` —
профиль «глубокие цепочки мелких методов lib/U128.eat» — канон-IR
(все-trap) стабильно быстрее build-элизии (после прямолимбовой
оптимизации 9,7 против 10,5 нс/оп, ~8 %; до неё 11,0 против 13,7,
~25 % — чем больше мелких вызовов, тем шире разрыв). Бекенды по
дозамеру §7.2 в паритете ⇒ это тот же кроссовер «аутлайн × элизия»
(п. 1 выше: trap-icmp восстанавливает диапазоны через границы
неинлайнящихся вызовов, ensures LLVM не видит). Выигрыш
инлайн-хинтов ожидается именно на методо-ёмких lib-профилях;
U128Bench — готовая метрика для этапа.

### 7.4 Строки: лидерство ×1,8–2,6, но 492 инстр/оп — подтверждён §лib

strcmp: EAT 25,7 нс/оп против C snprintf 67, Rust format! 47,
Go strconv 43 — специализированный Rt.append_u32 бьёт идиомы
соперников. Внутри всё ещё O(cap)-идиомы str<256> (Rt.eq скан,
append) — рычаги lib/NumParse·StrCmp из шапки реестра получили
внешнюю планку.

**СДЕЛАНО 2026-07-17 (кандидат 3, сороковая итерация FINDINGS)** —
O(cap)-идиомы сняты правками только `.eat` (зеркал не требуется):
lib/Parse.parse_i32 — один прямой проход по `str<256>` вместо
копии в RtStr побайтными append_char и тройного скана цифр
(parse_value звал parse_status повторно); Rt.eq — группы по 8 байт,
одна ветка на группу, безветвевое xor/or-дерево, ранний выход
сохранён (урок 23-й итерации не нарушен). NumParseBench 37,8 →
19,7 нс/оп (×1,92; часть разбора ×2,5), StrCmpBench 25,0 → 19,4
(×1,29; часть eq ×1,8), StrBench нейтрален. Кросс-языковой strcmp:
отрыв от C ×2,6 → ×3,4 (19,7 против 66,9 нс/оп). Остаток обоих
бенчей — сборочная сторона: append_u32 аутлайнен — это профиль
кандидата 2 (инлайн-хинты), lib-рычагами не берётся.

### 7.5 Отрицательные результаты (фикс)

Маска-как-доказательство работает: bank/u64/arith — паритет с C
(Rust unchecked ≈ safe: LLVM сам снимает маскированные границы).
Элизия окупается на ветвистом коде: branch build быстрее канона на
41 % — вес research-веток NONE-домена (§1.4/1.5) подрос: на
dependency-chain коде недоказанный trap стоит дороже всего.
Скорость компиляции: чистая эмиссия SelfIrOpt — 10 мс микробенч /
20–30 мс эмулятор (~25× Python-эталона); цепочка ограничена clang.

### 7.6 → Следующий шаг

Замерные этапы закрыты: **7.1 assume ✅** → **7.2 конвейер ✅
(llvmlite ≥ clang, маршрут не меняется)** → **7.3 профиль IPC
эмулятора ✅** (все 2026-07-17, вердикты выше). Открытые кандидаты,
по приоритету:

1. **Дрейф `hex_digit` + гейт конкатенаций — ✅ СДЕЛАНО 2026-07-17**
   (из FAULTS 7.1): паритет решений восстановлен — три правила в
   selfhost-верификаторе (утечка отложенных requires-сайтов `pend_n`
   через границу стейтмента/декларации: flush в `refine_cmp`/
   `refine_neq`/`ensures`/`store_array`; записи полей вложенных
   struct-литералов: глубокий `note_structlit` + путь присваивания;
   тай-брейк вердикта в сортировке дампа — зеркально в эталоне);
   гейт расширен: `VERIFY_PROGS` (12 полносоставных конкатенаций
   Rt + lib + модули, `tests/gate/verify_prog.sh`) — `verify`/-O в
   `verify_selfhost_verify_all`, IR `-O` в `verify_selfhost_opt`.
2. **Инлайн-хинты малых листовых функций** (находка-рычаг 7.3):
   `alwaysinline` post-parse в build-конвейере (Python-only, канон
   не меняется); решение пользователя ✅ принято 2026-07-17 —
   двухэтапно. **Этап 1 ✅ СДЕЛАНО 2026-07-17** (42-я итерация
   FINDINGS): эвристика `codegen.inline_hints` в `compile_binary` —
   тело ≤ T=384 инстр, рост размер×(сайтов−1) ≤ B=768, замыкание
   снизу вверх по DAG вызовов; u128 −39 % (10,5 → 6,4 нс/оп),
   numparse −8,8 %, strcmp −3,3 %, mos6502 −2,7 %; SelfIr: дамп
   байт-в-байт, размер/скорость без изменений. Уроки: мелочь ≤ 128
   LLVM ест сам (свип 0), носители — тела 144–384 с 1–4 сайтами;
   без бюджета B — взрыв ×6,4 на SelfIr (многосайтовые хелперы).
3. **Профильные рычаги 7.4–7.5 — ✅ СДЕЛАНО 2026-07-17** (кандидат 3,
   сороковая итерация FINDINGS): parse_i32 одним проходом + eq
   группами по 8 байт; NumParse ×1,92, StrCmp ×1,29, отрыв strcmp
   от C ×3,4. Открытым остаётся вес research-веток NONE-домена
   (§1.4/1.5) на dependency-chain коде.

Каждый этап: worktree при живом соседе, полный гейт зелёный целиком
(для правок компиляторов), bench-числа до/после, запись в FINDINGS,
итог по WORKFLOW §3.

→ следующий шаг: кандидат 2, **этап 1 ✅ СДЕЛАНО 2026-07-17**
(вердикт положительный, ручка в `eatc build`: T=384, B=768 —
см. п. 2 выше и 42-ю итерацию FINDINGS). Открыт **этап 2** (по
двухэтапному решению пользователя): зеркало эвристики в ось `-O` —
атрибут `alwaysinline` в **тексте** .ll эмиссии `eatc ir -O` и
SelfIrOpt, паритет байт-в-байт, полный гейт. Дизайн-вопросы этапа:
эвристика этапа 1 считает инструкции распарсенного llvmlite-модуля —
для текстовой эмиссии обе стороны должны считать по эмитируемому IR
(счёт инструкций + граф вызовов в Ir/IrEmit и зеркально в
codegen-эмиссии оси), константы T/B — те же; выигрыш ждать на
selfhost-цепочке `SelfIrOpt → clang -O2` (u128-профиль: clang по
.ll+атрибут 9,97 → 6,60 нс/оп в разведке). Промт этапа 2:

> OPTIMIZATIONS §7.6, кандидат 2, этап 2 (ступень Sonnet/Opus,
> высокий effort): зеркало инлайн-хинтов в ось -O. Эвристика этапа 1
> (codegen.inline_hints: T=384, B=768, замыкание снизу вверх) — в
> текстовую эмиссию `eatc ir -O` (codegen) и SelfIrOpt
> (Ir/IrEmit.eat), атрибут `alwaysinline` в define-строках; паритет
> `eatc ir -O` == SelfIrOpt байт-в-байт, счёт инструкций/графа — по
> эмитируемому IR одинаково с обеих сторон. Канон `eatc ir` (без -O)
> не меняется. Полный гейт целиком (10 целей, worktree при живом
> соседе); метрики: u128/strcmp по цепочке SelfIrOpt → clang -O2
> (build_eat_self в crosslang), FINDINGS-запись, MODIFYING §3
> чеклист «правка эмиссии IR».

**Этап 2 ✅ СДЕЛАНО 2026-07-17** (43-я итерация FINDINGS): зеркало
инлайн-хинтов в ось `-O`. Реализация — `codegen.axis_inline_hints`
(Python, текстовый пост-проход по эмитируемому `.ll`) + selfhost
`Ir/IrStmt.eat` (двухпроходная эмиссия: измерительный `gen_body` без
печати → замыкание → сброс модульных пулов → печать с `alwaysinline`).
Ключ паритета: обе стороны считают по **эмитируемому тексту** (каждая
инструкция LLVM = одна физическая строка, включая switch), поэтому
байт-идентичность гарантирует одинаковый набор помеченных; размер тела
= строки-инструкции (отступ 2 пробела), граф вызовов — сканом строк
`call ... @"ИМЯ"` с разрешением карты мангл-имя→fi (покрывает и
пользовательские вызовы, и встроенные RtStr-`print`/`write`/`append`/
`eq` — хук только e_ucall недосчитывал сайты и **перепомечал**). Канон
`eatc ir`/SelfIr и trap-коды не тронуты (флаг `him`=0). Паритет `eatc
ir -O` == SelfIrOpt байт-в-байт на всех входах `verify_selfhost_opt`
(самоприменение фронтенда: 97 помечено). **Метрики** (SelfIrOpt →
clang -O2, A/B «атрибут / без»): u128 **9,6 → 6,24 нс/оп (−35 %)**
(62 `alwaysinline`, совпало с разведкой 9,97→6,60); strcmp — в шуме
машины (±10 %, 19 меток, нейтрально: клан по-разному инлайнит
`append_u32`). Полный гейт 10 целей зелёный в worktree на HEAD.
**Упор в потолок §6:** зеркало добавило функции, компилятор был
1006/1024 — вписались слиянием хелперов (1019/1024); поднятие предела
— отдельный [LIMITS_PLAN](LIMITS_PLAN.md) (решение пользователя:
флаг-конфигурируемые лимиты).

**Этап 2b (чистка графа вызовов) ✅ СДЕЛАНО 2026-07-18**: замена
текстового скана `.ll` на прямую запись рёбер во время эмиссии. Раньше
граф вызовов строился сканом строк буфера `lb` на `call ... @"ИМЯ"` с
разрешением карты мангл-имя→fi (хелперы `mn_gen`/`mn_hash`/`mn_insert`/
`mn_find`/`line_callee`/`scan_calls`/`mq_lit`/`mq_sb` + поля `imq`/`imnp`/
`imh`…). Теперь ребро пишется прямо на сайте эмиссии call к
**определённой** функции под флагом `msr==1` измерительного прохода
(`rec_edge`): пользовательские — через `e_ucall` (fi под рукой,
`not fn_ext`), встроенные методы RtStr (`init`/`print`/`write`/
`append_*`/`eq`) — через `rt_mfi` (fi по владельцу `rsi` = struct RtStr +
имя метода через `Check.span_is`, а не по тексту IR). Аксиомы/интринзики
(`eat_write_byte`, `eat_str_append_bytes`, `llvm.*`) — не рёбра: их нет в
таблице define-функций. **Поведение не изменено** — набор `alwaysinline`
в `-O` байт-в-байт прежний (оракул `verify_selfhost_opt`: самоприменение
+ 75 файлов корпуса). Питон (`codegen.axis_inline_hints`) — эталон, не
тронут (паритет по **выводу** `.ll`, не по методу). Убрано 8 хелперов,
добавлено 2 (`rec_edge`, `rt_mfi`) → **−6 функций** в компиляторе
(`IrStmt.eat` 71→65), ослабляет упор в потолок §6. Урок: хук только на
`e_ucall` недосчитывал встроенные RtStr-сайты (тот же баг, что закрывал
text-scan, коммит 4185711) — крит-точка была в резолве fi встроенных
методов по владельцу+имени.

**Этап 3 (сквозной замер + закрытие) ✅ СДЕЛАНО 2026-07-18** (44-я
итерация FINDINGS): A/B «атрибут / без» по **всему** crosslang-корпусу
(цепочка `build_eat_self` → SelfIrOpt → clang -O2), чистый замер над
эмитированным `.ll` (эмиссия не тронута, `him`=0). **Геосреднее** (11
нагрузок): instr **−10,3 %** (детерминизм), стена −4,6…−6,4 % (тянет
u128). **Носители** — методо-ёмкие lib-профили: u128 **−47 % instr /
−35 % стена** (флагман, совпал с разведкой 7.3 и этапом 2), todo −13 %,
u128div −10 %, mos6502 −7 %, http −5,5 % instr. **Пустышки** —
самодостаточные арифметические циклы (sort/branch/bank/u64 ≈0):
20 базовых RtStr-меток несут, но горячий цикл не пересекает границ
вызова, LLVM и так инлайнит. Регресса нет (instr вне шума не растёт;
«медленнее» только на суб-0,2-с микробенчах = дрожь запуска, там же
instr −). Парадокс arith: −14 % instr при нейтральной стене (метки вне
горячего цикла). **Компилятор-на-себе — 0** (как у fold): SelfIr,
собранный через SelfIrOpt С хинтами (133 метки) против БЕЗ, на эмиссии
IR фронтенда — Δ стена +0,0 %, Δ instr +0,1 %, IR байт-идентичен;
профиль компилятора не «мелкие методы в тесном цикле». **Вердикт:
кандидат 2 ЗАКРЫТ** — свободный выигрыш там, где ложится, безвреден
там, где нет; поведение байт-в-байт, канон не тронут. **Ниши для этапа
4 нет**: пустышки пусты не из-за промаха порога T/B, а потому что
восстанавливать нечего; тюнинг порога/эвристики за пределами
листовости — правка **поведения** (паритет обеих сторон + решение
пользователя), данные его не просят.

Кандидат 3 (рычаги lib/) ✅ 2026-07-17.

## 8. HTTP-парсер: разложение ×3,7 и три рычага (2026-07-17)

Вход — бенч `http` и декомпозиция разрыва замером по частям
(85f2a22, FINDINGS «HTTP-парсер в crosslang»): EAT/C = 1,35 на полном
бенче; строки EAT **быстрее** C ×5,8 (интерполяция против `snprintf`),
`req_new` — паритет (99/95 нс, копии возврата элидированы), весь
разрыв — **побайтовый цикл парсера**: ×3,7 за вычетом общего memset,
8 291 против 3 074 инстр/запрос, горячий путь полностью заинлайнен
(нуль вызовов), рантайм-проверки лишь +23 %. Гипотеза причины была:
состояние автомата (`n`/`state`/`ls`) живёт в полях 8,5-КБ `Req` и не
промотируется в регистры через цикл (llvmlite-IR без alias-метаданных);
IPC 4,77 — «много дешёвой лишней работы». Разведка §8.3 гипотезу
опровергла и нашла настоящего носителя: идиома «скан с нуля под
стражем» (`find_sp`, двоеточие `parse_header`) — 75 % разрыва в
инструкциях, ≈97 % стены. Три рычага, порядок —
8.1 → 8.2 → 8.3; каждый этап: worktree при живом соседе, полный гейт
целиком, bench-числа до/после в FINDINGS, итог по WORKFLOW §3.

### 8.1 Span-парс lib/Http.eat — состояние в локалах (промт)

> OPTIMIZATIONS §8.1, span-парс lib/Http.eat: убрать круговорот полей
> через память в побайтовом цикле. Контекст: декомпозиция 85f2a22 —
> ядро парсинга ×3,7 к C за вычетом memset; причина — состояние
> автомата в полях 8,5-КБ Req. Правка ТОЛЬКО lib/Http.eat (+ примеры/
> бенч при смене API): внутренности `feed_line` (и батч-метод, напр.
> `feed_str`) держат `n`/`state`/`ls` в ЛОКАЛАХ на весь диапазон и
> сбрасывают в поля один раз на выходе; построчные `parse_reqline`/
> `parse_header` остаются на полях (раз на строку); `push_byte`
> сохранить для побайтовых клиентов (сервер со стражем socket_avail).
> Наблюдаемое поведение 1:1: те же 400/413/431, те же test-блоки,
> порты crosslang НЕ меняются (чек-сумма та же). Компилятор и
> Verify* не трогать; грабли языка: break ≤ 1/цикл, `let` только у
> self (FAULTS 2026-07-17). Новые экспортируемые имена → реген
> lib.sig в worktree, стейдж блобом (`update-index`) мимо WIP
> соседей. Порог: `bench_crosslang --only http` — EAT build с 305 к
> цели ≤ 250 нс/запрос, сверка 9 вариантов байт-в-байт, полный гейт
> 10 целей зелёный целиком, числа до/после в FINDINGS + дополнение
> CROSSLANG, итог по WORKFLOW. Исполнитель: Sonnet, средний effort.

*(После §8.2 базовая точка сместилась: EAT build 205 нс/запрос,
C 123 — порог этапа пересчитать от свежего замера «до»; весь
остаток разрыва теперь в побайтовом цикле, рычаг стал весомее.)*

*(Дополнение по §8.3: главный носитель разрыва — не круговорот
полей, а сканы с нуля под стражем (75 % инстр, ≈97 % стены);
в объём этапа обязательно входят `find_sp` и скан двоеточия
`parse_header` — единый прямой проход по строке от позиции с
состоянием в локалах, идиома `parse_i32` кандидата 3. Ориентир
после §8.3: C с EAT-идиомой сканов стоит 199 нс/запрос при EAT 202 —
потолок рычага ≈ цена C 123 + остаток проверок ~490 инстр.)*

**СДЕЛАНО 2026-07-17 — 203 → 124 нс/запрос (−39 %), EAT/C 1,75 →
1,06; потолок рычага достигнут.** Два изменения, только lib/Http.eat:
(1) прямые сканы — `find_sp` и двоеточие `parse_header` локальным
индексом от `b0`/`ls2` вместо холостого пропуска `[0..b0)`; границы
без trap-ветки через requires-цепочку `line_end` (новый общий финал
строки, `e0 < 8192`) → `parse_reqline`/`parse_header`/`find_sp`
(`le <= 8192`); (2) спан-путь `feed_line` + батч `feed_str` — строка
без LF одной полосой `span_copy` (копия + LF-детект or-свёрткой,
n/ls раз на строку), редкие LF-в-строке/кромка 413 — на прежнем
`push_byte` (сохранён для посимвольных клиентов). Буква исходного
промта «состояние автомата в локалах на байт» замером опровергнута
(три формы автомата на локалах 370/334/331 нс против 302 базы:
store-forwarding Apple silicon делает круговорот полей почти
бесплатным, недоказуемая граница `s[i]` держит trap-ветку и
запрещает векторизацию) — выиграли прямые диапазоны и меньше
проходов, как и предсказала разведка §8.3. Инстр/запрос 4 044 →
2 505 (остаток к C 2 091 ≈ те самые ~490 проверок), все-trap 277 →
119 — налог канона 0,96 (канон не медленнее элизии); EAT обходит
Rust safe (141) и Go (220) вдвое. Суммарно по реестру §8: 305 →
124 нс/запрос (×2,5), порты и чек-сумма не менялись, сверка
9 вариантов, полный гейт 10 целей зелёный. Числа — FINDINGS «HTTP:
span-парс lib/Http.eat», CROSSLANG «шестой прогон»; уроки методики
(A/B только после пересборки обеих сторон; профиль+дизасм раньше
догадок) — FAULTS §8.1. Реестр §8 закрыт целиком.

### 8.2 Переиспользование кадра Req — reset вместо req_new (промт)

> OPTIMIZATIONS §8.2, переиспользование кадра Req: снять ~95
> нс/запрос обнуления `raw[8192]` (декомпозиция 85f2a22: треть цены
> запроса у EAT, 42 % у C). Неинициализированной памяти в языке нет
> (Power of 10 не трогаем!) — рычаг: НЕ пересоздавать кадр. Метод
> `Req.reset(let self)`: скалярные поля в 0, `raw`/`hdr` НЕ
> трогаются — парсер читает только `raw[0..n)` записанного после
> сброса, срезы Header — в пределах нового n; инвариант зафиксировать
> в докстринге и test-блоке (разбор после reset чистый, старые байты
> недостижимы). Внедрить в examples/http (Pool.eat: `req_new()` →
> `reset()`; Hello/Router — кадр наружу цикла) и в HttpBench + ВСЕ
> четыре порта зеркально (порты 1:1: reset вместо memset — иначе
> замер нечестен); вывод примеров в verify байт-в-байт не меняется.
> Честная оговорка в FINDINGS: рычаг двигает абсолют обоих языков
> (EAT ≈ 210, C ≈ 131 нс/запрос), отношение EAT/C вырастет — это
> ожидаемо (сервер быстрее, бенч честнее). Порог: числа до/после,
> сверка 9 вариантов, полный гейт (lib.sig — блобом мимо WIP), итог
> по WORKFLOW. Исполнитель: Haiku или Sonnet, низкий-средний effort.

**СДЕЛАНО 2026-07-17** — `Req.reset(let self)` в lib/Http.eat
(11 скалярных полей в 0, `raw`/`hdr` не трогаются; инвариант чистоты
разбора — докстринг + test-блок `http_reset_reuse`); `let` только у
self ⇒ в HttpBench кадр несёт `Bench { r: Req }`, профили — её
методы; порты C/Rust/Go/Py зеркально (указатель/`&mut`/receiver;
у Python очистка `hdr`-списка — аналог `nh = 0`); examples/http:
Pool (`open`/keep-alive → reset слота), Hello/Router (кадр и так вне
цикла — reset вместо пересоздания), сверка EAT_NET байт-в-байт не
изменилась. Числа (медиана 5): EAT build 294 → **205** нс/запрос
(−30 %, прогноз ≈ 210), C 221 → **123** (−44 %, прогноз ≈ 131),
Rust 245 → 140, Go 429 → 220; 9 вариантов сходятся, чек-сумма та же.
Оговорка подтверждена: EAT/C 1,33 → 1,67 — ожидаемо (у C memset был
42 % цены запроса). Остаток разрыва — побайтовый цикл: §8.1/§8.3.
Детали — FINDINGS «HTTP: переиспользование кадра Req», CROSSLANG
«пятый прогон».

### 8.3 Разведка alias-инфы эмиссии (TBAA/noalias) — сначала замер (промт)

> OPTIMIZATIONS §8.3, разведка alias-информации: почему llvmlite-
> конвейер оставляет ~74 инстр/байт против ~27 у clang на той же
> семантике (декомпозиция 85f2a22). Методика §7.2 — одна ручка за
> раз, байт-идентичный .ll; инструмент
> tests/bench/crosslang/backends.py уже умеет: шаг 1 — прогнать
> `backends.py --only http` (build-.ll с элизией: llvmlite против
> clang -O2; помнить версионный перекос LLVM 22 ↔ clang 21, FAULTS
> 7.2; на 5 старых нагрузках llvmlite ≥ clang — http в тот замер не
> входил). Развилка по вердикту: (а) clang на том же .ll даёт
> ~C-числа → дело в мид-энд-пайплайне llvmlite на профиле «поля
> большой структуры» — искать проходы (SROA/LICM/GVN, NewPM
> speed_level), Python-only ручка build; (б) clang НЕ ускоряет →
> alias-инфы не хватает в самом IR — эскиз эмиссии TBAA-метаданных
> или noalias-атрибутов на GEP-доступах к полям struct строго
> build-only слоем ВНЕ канона (прецеденты: `--fold` ярус B без
> зеркала; урок 7.1 — сначала замер, потом внедрение; канон `eatc
> ir` не меняется, verify_selfhost/фикспойнт не затронуты; если
> метаданные нужны в каноне — это правка эмиссии с полным зеркалом
> Ir*.eat байт-в-байт и отдельным решением пользователя). Паритет
> поведения обязателен: бинарник == интерпретатор на всех
> verify-целях. Порог разведки: вердикт с числами (http + 2–3
> старых бенча на регресс) в FINDINGS, карточка в этот реестр;
> внедрение — отдельным этапом по решению пользователя.
> Исполнитель: Fable или Opus, высокий effort.

**СДЕЛАНО 2026-07-17 — вердикт: alias-инфа НЕ рычаг, развилка (а)/(б)
не реализовалась; внедрять нечего.** Шаг 1 закрыл дозамер 7.2
(паритет бекендов на http). Эскиз (б) построен и замерен:
`tests/bench/crosslang/tbaa.py` — скалярный TBAA пост-проходом по
llvmlite-модулю (узел на «тип корня GEP + путь полей», покрытие
1 021 тег / 0 пропусков на http), канон не тронут ни байтом
(monkeypatch в памяти процесса эксперимента). Числа (HEAD 9c8bee7,
медиана 5): http build 202,1 нс / 4 044 инстр → +TBAA 202,3 / 3 996
(−1,2 % инстр, 0 стены), теговый .ll через clang -O2 — 201,9 / 3 894;
branch/sort/mos6502 — нуль эффекта (регресса тоже нет). TBAA работает
(статически −41 load из 232, перезагрузка `n` после store в `raw[i]`
убрана) — перезагрузки полей просто не были носителем; `noalias` на
агрегатных аргументах штатная эмиссия ставила и до разведки.
Контроль с C-стороны: `-fno-strict-aliasing` не меняет НИ ОДНОЙ
инструкции порта (2 090,80 → 2 090,78), а пересадка в C идиомы
сканов lib/Http.eat даёт 116 → 199 нс и 2 091 → 3 556 инстр — C
догоняет EATLang. Носитель разрыва: **идиома «скан с нуля под
стражем» — 1 465 из 1 953 инстр (75 %), ~83 из 86 нс (≈97 % стены)**;
остаток ~490 инстр — рантайм-проверки (+23 % декомпозиции), стены
почти не стоят. Рычаг уходит в §8.1 (дополнение выше). Детали и
методика — FINDINGS «HTTP: разведка alias-инфы эмиссии», правки
атрибуции — CROSSLANG «третий прогон», §7.2 и шапка §8 здесь;
уроки — FAULTS 8.3.

## 9. Верификатор: интервал loop-carried модульного накопителя (2026-07-17)

Вход — бенч `todo` (седьмой прогон CROSSLANG): EAT build 195,8 нс /
1 301 инстр против C 167 / 1 025 (EAT/C = 1,17). Разложение горячей
`Store.serialize` по `eatc ir -O` (`cat Rt + TodoBench`): из trap-блоков
элизия снимала всё, кроме **ровно 6 проверок переполнения на накопителе**
`sum = (sum*31 + x) % 65536` — `llvm.umul.with.overflow(sum,31) ×3` и
`llvm.uadd.with.overflow ×3` по трём циклам свёртки. Причина: `_mod`
даёт точный `x % K → [0,K-1]`, но накопитель пишется во **вложенном**
цикле, поэтому исключён из observed внешнего (`_nested_loop_paths`), а
существующий loop-carried анализ (`_loop_exit`) заточен под линейные
индукции и на входе витка расширял `sum` до полного диапазона типа —
`sum*31` недоказуем.

**СДЕЛАНО 2026-07-17** (минимальная звуковая форма). Новый проход
`_mod_carry` (verifier.py + зеркало `mod_carry`/`mod_k`/`mod_apply` в
`selfhost/VerifyFlow.eat`, байт-в-байт): путь-простое-имя, у которого
**ВСЕ** определения в теле цикла (рекурсивно, включая вложенные) — вида
`p = _ % K` (K — целый **литерал** ≥ 1, интервал `[0,K-1]` не зависит от
входного `p`), а предцикловое значение (`env`/`ein`) ⊆ `[0,K-1]`, несёт
`[0,K-1]` на входе витка. Звучно по индукции без фикспойнта: клэмп `%`
безусловен, значит `p ∈ [0,K-1]` в любой точке тела. **Обе ноги** (вход
⊆ [0,K-1] И сохранение телом: каждое определение ⊆ [0,K-1]) обязательны —
пропуск любой снял бы настоящую trap-проверку. Именно литерал, не
константа-путь: изменяемый по витку делитель дал бы незвуковой интервал.
Парные кейсы — `tests/verify/56_mod_carry_accumulator.eat` (позитив,
накопитель во вложенном цикле, N/N) и `57_mod_carry_no_reclamp.eat`
(негатив `acc = acc + j` без `%`, проверка обязана выжить, 0/N).

**Результат.** `serialize`: 6 overflow-проверок сняты (`umul` 6 → 2,
`uadd` 17 → 13 в `eatc ir -O`; попутно накопитель `acc` в `main` тем же
классом — `acc*31` доказан, `acc + id` корректно **остаётся** runtime:
`id = next_id` монотонно-неограничен). `verify` футер TodoBench:
overflow 56/82 → 64/82 (+8, все — накопительные статменты). Замер
(EATLang build, медиана раннера, чек-суммы 46615/17664 неизменны,
9 вариантов сходятся):

| бенч | было нс / инстр | стало нс / инстр | EAT/C |
| --- | --- | --- | --- |
| todo | 195,8 / 1 301 | **166,5 / 1 031** | 1,17 → **1,00** |
| http | 111,4 / 2 410 | 111,1 / 2 410 | **0,93** (держит) |

Класс снят целиком (любой `acc = f(acc) % K`, не один бенч), пример не
тронут — рычаг в интервальном анализе. Канон `eatc ir` не изменён ни
байтом (сворачивает только ось `-O`); полный гейт 10 целей зелёный в
worktree чистого HEAD (самоприменение верификатора канон + `-O`,
bootstrap-фикспойнт, sig — все зелёные). Общее направление (индуктивная
проверка кандидата на входе И обратной дуге без монотонного фикспойнта)
— возможное продолжение; здесь реализована её минимальная безфикспойнтная
форма. Числа — FINDINGS «Верификатор: модульный накопитель», CROSSLANG
«восьмой прогон»; уроки — FAULTS §9.

### 9.1 Индуктивная проверка кандидат-интервала (шаг 2) — СДЕЛАНО 2026-07-18

Обобщение шага 1 с минимальной `%K`-формы на произвольный клэмп через
**индуктивную проверку кандидат-интервала без монотонного фикспойнта**:
путь p несёт кандидат C на входе витка, если держатся ОБЕ ноги —
(1) вход `предцикл ⊆ C` И (2) обратная дуга `тело при p=C даёт ⊆ C`.
Кандидат C фиксируется вычислением RHS при `p=⊤` (полный диапазон типа)
общим интервал-эвалюатором; C НЕ итерируется (иначе ложные
доказательства). Для клэмпов (`_ % K`, маска `_ & M`, min/насыщение через
`ensures`) выход ограничен независимо от входа ⇒ `C ⊊ диапазона типа`;
не-клэмп даёт ⊤ и отбраковывается «бесполезным» гейтом.

`_mod_carry`/`mod_carry` заменены на `_clamp_carry`/`clamp_carry`
(verifier.py + зеркало `selfhost/VerifyClamp.eat`, байт-в-байт). Проба
`_clamp_probe`/`clamp_probe` считает интервал RHS в предцикловом env
(`env.copy`/`ein`) с обнулёнными loop-carried путями (звуковое
над-приближение) и закреплённым p; отметки заглушены (annotate=False /
`_probe`; в selfhost — `rec_off=1` от отложенных requires, `ann=0` от
проверок). Изменяемый по витку клэмп-предел ∈ tracked ⇒ в пробе ⊤ ⇒ не
несётся (парный негатив) — вот почему **нелитеральный** предел допустим
лишь loop-инвариантный. Несём только пути, записи которых скрыты во
вложенных циклах (`_nested_loop_paths`): наблюдаемые линейные индукции
точнее ведёт accel/`_loop_exit` (регресс 09/10/11 при перекрытии их
`count−1`-надмножеством — исправлено гейтом «только вложенные»).

Строгий супермножество шага 1: `% литерал` даёт тот же `[0,K-1]` через
эвалюатор (кейсы 56/57 без изменений). Парные кейсы — `58_clamp_carry_mask`
(маска `& m`, m loop-инвариантна, N/N), `59_clamp_carry_var_bound`
(та же маска, m изменяется по витку ⇒ 0/N), `60_clamp_carry_saturate`
(насыщение через `ensures result <= 255`, N/N), `61_clamp_carry_unbounded`
(`ensures true` ⇒ ⊤ ⇒ 0/N).

**Числа.** На демо-накопителе `sum = (sum + r*j) & 65535` во вложенном
цикле (Rt + main): overflow-проверок доказано 48/63 → **49/63** (+1,
именно накопительный `sum + r*j`), в `eatc ir -O` overflow-интринсик
`with.overflow` 18 → 17 (одна элидирована). На committed-коде репозитория
(lib/ + examples/) движения нет — 11/23 overflow идентично HEAD: **класс
без committed-потребителя** (единственный нативный — TodoBench из §9 —
живёт отдельной untracked-веткой; он же покрыт как супермножество).
Полный гейт 10 целей зелёный в worktree чистого HEAD (самоприменение
верификатора канон + `-O`, bootstrap-фикспойнт, sig). Канон `eatc ir` не
тронут. Уроки методики — FAULTS §9.1.

Границы формы: насыщение через ветвление (`x = x+1; if x>H {x=H}`) НЕ
берётся — промежуточный `x = x+1` при `p=⊤` даёт ⊤ и отравляет кандидат
(звуково: перед клэмпом значение реально не ограничено). Клэмп должен быть
ОДНИМ выражением-присваиванием (`& m`, `% q`, `min(...)`/`sat(...)` через
контракт). Знаковые накопители работают тем же кодом (сравнения в
знак-величине), потолок — тот же «выход ⊊ диапазона типа».
