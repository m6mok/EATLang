# План: LSP на EATLang (language server для VSCode)

- **Статус:** 🚧 В ПРОЦЕССЕ (этапы 0–5 ✅; этап 6 — Test Explorer,
  подтверждён пользователем 2026-07-19)
- **Роль:** активный план
- **Рабочие поверхности:** `editor/lsp/` (сервер на EATLang:
  `Transport.eat`, `Rpc.eat`, `Handlers.eat`, `LspMain.eat`),
  `editor/vscode/` (клиент-расширение: `package.json`, `src/extension.ts`),
  `Makefile` (цели сборки/проверки сервера), `tests/lsp/` (транскрипт
  сессии). Фазы `selfhost/*` — читаем как модули, **не пишем**.
- **Правки языка/грамматики:** нет
- **Новые аксиомы ОС (SPEC §7):** нет (сервер целиком на наличных
  `read_byte`/`write_span`/`write_err_byte`/`exit`)
- **Зеркало в selfhost:** не требуется. Это **новая программа-приложение
  на EATLang**, а не правка компилятора: паритет `src/eatc/ ↔ selfhost/`
  она не затрагивает. Сервер переиспользует фазы `selfhost/*` как
  read-only модули (склейка файлов на входе `eatc`), но их логику не
  меняет — значит зеркалить нечего.
- **Опирается на:** [SELFHOST](SELFHOST.md) (фазы `lex/parse/check/verify`
  как переиспользуемые модули — структуры-по-значению, пулы-результаты),
  [JSON_PLAN](JSON_PLAN.md) (`lib/json/Json.eat` — разбор/сборка JSON-RPC
  1:1), [HTTP_PLAN](HTTP_PLAN.md) (`lib/http/Http.eat` — машинерия
  `Content-Length`/`Body`, переносится с доработкой),
  [REFACTOR_LAYOUT_PLAN](REFACTOR_LAYOUT_PLAN.md) (пути фаз по папкам),
  [LIMITS_PLAN](LIMITS_PLAN.md), этап 1 (потолок функций 2048 — уже поднят)
- **Блокеры:** решение пользователя по открытым вопросам §10 (место
  исходников сервера, порядок этапов, объём протокола). Этап 0 можно
  начинать сразу — код языка не трогает.
- **Блокирует:** —

---

## 1. Итог одной строкой

Language server для EATLang, **написанный на самом EATLang** и линкующий
фазы компилятора `lex+parse+check+verify` как модули: один бинарник
говорит по JSON-RPC поверх stdio и делает видимыми в VSCode ровно те
доказательства, которыми уникален язык, — диагностики, доказанные/оставшиеся
trap-проверки, границы памяти §8, типы и контракты.

## 2. Почему язык к этому приспособлен

LSP-сервер — это событийный цикл: читать сообщение, ответить, повторить.
Это **ровно реактивная модель EATLang** (SPEC §5.1): один корневой `loop`
в `main`, каждая итерация тотальна, состояние — bounded-пулы в кадре
`main`. Сервер строится как `Server.tick` из `lib/http/Server.eat`, только
источник байтов — stdin (`read_byte`), а не сокет.

Больше того — язык приспособлен к этому **лучше**, чем к обычному
приложению, потому что три вещи уже готовы и переиспользуются как есть:

- **Анализатор.** Фазы `selfhost/` — не монолит, а структуры-по-значению
  с публичными конструкторами (`lexer_new → parser_new → check_new →
  verify_new`) и результатами в SoA-пулах (токены в `Lexer`, узлы AST в
  `Parser`, обязательства `ck_k/ck_n/ck_o` в `Verify`). Дампы — отдельные
  методы; ось `-O` уже читает `v.nck` напрямую, минуя текст. LSP-хэндлер
  делает то же: гоняет фазы, читает пулы, **не зовёт** `dump()`/`print`
  (stdout занят каналом протокола).
- **Транспорт.** `Content-Length` + JSON-тело — та же рамка, что у HTTP:
  `Header`/`find_header`/`span_u32`/`content_length` и `Body.begin_cl`/`push`
  из `lib/http/Http.eat`; разбор и сборка JSON — `JDoc.parse` + `JOut`
  из `lib/json/Json.eat` — переносятся почти 1:1.
- **Реестр документов** — прямой шаблон `Store` из
  `examples/http/todo/`: массив фикс. размера + счётчик + флаг слота,
  живёт в кадре `main`, переживает сообщения.

Диагностики сервера — это диагностики **настоящего** компилятора (те же
фазы), поэтому они не могут разойтись с поведением сборки.

## 3. Рамка: свойство → правило → потолок

| Свойство сервера | Правило Power of 10 | Потолок |
| --- | --- | --- |
| Цикл обслуживания | правило 1 (один `loop` в `main`) | 1 корневой `loop`, `break` по `exit`-уведомлению |
| Реестр открытых документов | правило 3 (пул — часть типа, не куча) | `LSP_MAX_DOCS` слотов, каждый — буфер `LSP_DOC_CAP` байт |
| Кадр входящего сообщения | правило 3 | `LSP_MSG_CAP` байт (по образцу `HTTP_MAX_BODY`) |
| Разбор JSON-RPC и обход AST | правило 1 (нет рекурсии) | явные стеки кадров (как в `Json.eat`), пулы фикс. размера |
| Линковка всех фаз в один бинарник | правило 4 (≤ 2048 функций) | ~1017 (фронтенд) + транспорт/диспетч/хэндлеры ≈ **1250/2048** |
| Прогон анализа на сообщение | правило 2 (`for` с границей) | один общий анализ-кадр, переиспользуется (`reset`) |

## 4. Числа (пределы)

Все — bounded, кандидатные значения (уточняются замером на этапе 0–1):

- `LSP_MAX_DOCS = 32` — открытых документов одновременно. Полный реестр →
  сервер отвечает ошибкой, **не растёт** (аналог `507` в `Store`).
- `LSP_DOC_CAP = 65536` байт на документ (как `HTTP_MAX_BODY`; крупнее —
  диагностика «файл превышает предел LSP», редактирование не блокируется).
- `LSP_MSG_CAP = 262144` байт на входящий JSON-RPC кадр (тела `didChange`
  крупнее одного документа с запасом; превышение → ошибка протокола).
- **Функции:** фронтенд `lex+parse+check+verify` = 744 (уже существует как
  `VerifyMain`); + все пять фаз с `ir` = 1017 (не нужен — ir серверу не
  требуется). Бюджет LSP-слоя (транспорт + диспетчер + ~12 хэндлеров) —
  оценка 200–350 функций. Итог ~950–1100 из 2048, запас ~2×.
- **Память:** большие пулы фаз (токены `[u32;65536]×4`, узлы AST
  `[u32;65536]×2`, обязательства `[u32;16384]×3`) существуют в кадре
  `main` в **одном** экземпляре — общий анализ-кадр. На документ хранится
  только его текст (`LSP_DOC_CAP`) и **компактный** кэш диагностик
  (позиция + вид + вердикт), не полное состояние анализа. Пересчёт — по
  запросу (`didChange`), в переиспользуемый кадр.

## 5. Аксиомы ОС

Новых нет. Всё покрыто наличными аксиомами (SPEC §7):
- вход — `read_byte() -> Result<u8, IoError>` (stdin, `Err(Eof)` в конце);
- выход — `write_span(a, off, len)` (тело ответа одним вызовом ОС),
  при нужде `write_byte`;
- журнал сервера (не мешая каналу протокола) — `write_err_byte` в stderr;
- завершение — `exit(code)` в `main` по уведомлению `exit`.

## 6. Архитектура / механизм

Два артефакта:

**(A) Сервер `editor/lsp/` — программа на EATLang.** Склейка модулей на
входе `eatc build`: `Rt.eat` → `lib/json` → новый `editor/lsp/Transport.eat`
(header-парсер и stdio-драйвер) → `editor/lsp/Rpc.eat` (диспетчер по
`method`) → фазы `selfhost/lex,parse,check,verify` → `editor/lsp/Handlers.eat`
→ `editor/lsp/LspMain.eat` (`main` с реестром документов и корневым `loop`).

Виток цикла (по образцу `Server.tick`, источник — stdin):
1. дренаж `read_byte()` в header-парсер до `PARSE_DONE` (пустая строка);
2. `content_length()` → `Body.begin_cl(n)` → добор `n` байт тела;
3. тело → `JDoc.parse` → `method`/`id`/`params`;
4. диспетчер → хэндлер; хэндлер строит ответ `JOut`;
5. `Content-Length: {JOut.n}\r\n\r\n` + `write_span(JOut.buf, 0, n)`;
6. `break` только по уведомлению `exit`.

**Реестр документов** — `struct DocStore { docs: [Doc; 32]; count }` в
кадре `main` до `loop`; `Doc { uri, version, buf[65536], used }`. `didOpen`
→ занять слот; `didChange` (полная синхронизация текста на старте) →
заменить текст + пересчёт; `didClose` → освободить слот. Поиск по URI —
линейный, как `Store.find`.

**Анализ** — общий кадр `let an = analyzer_new()` (или пересоздание на
запрос, если так чище по памяти): `parser_new(lexer над текстом)` →
`check_new` → `run_typed()` → `verify_new` → `analyze()`. Читаем позиции
ошибок (`EatError` → line:col) и обязательства (`v.ck_*`); **не** зовём
`dump()`.

**Конверсия позиций.** Компилятор даёт 1-based `line:col` в байтах; LSP
хочет 0-based `line`/`character` в кодовых единицах UTF-16. Нужен слой
пересчёта байтовой колонки → UTF-16 (bounded проход по строке документа).
Это единственная нетривиальная новая машинерия транспорта; её граница —
длина строки документа.

**(B) Клиент `editor/vscode/` — тонкое расширение.** `LanguageClient`
(vscode-languageclient), который запускает бинарник сервера как процесс с
транспортом stdio. `package.json`: `activationEvents` на язык `eat`, `main`
= точка входа, зависимость `vscode-languageclient`. TextMate-грамматика
остаётся — LSP её дополняет (семантика поверх лексической подсветки), не
заменяет.

## 7. Что реализуем НЕ

- **Не порождаем процессы.** В EATLang нет process-аксиом — сервер не
  «зовёт `eatc`», а линкует фазы в себя. Это осознанный выбор, а не
  ограничение: диагностики тем самым тождественны компиляторным.
- **Не инкрементальная синхронизация текста на старте.** Полный текст на
  `didChange` (`TextDocumentSyncKind.Full`). Инкрементальные правки —
  возможный поздний этап, только по замеру задержки.
- **Не инкрементальный анализ.** Пересчёт всего документа на изменение
  (документ bounded ≤ 64 КБ — быстро). Кэш фаз между правками — не делаем.
- **Не форматирование, не переименование, не rename-refactor** на первых
  этапах — это отдельные capability, вводятся по спросу.
- **Не multi-root / не кросс-файловый анализ** сверх модели модулей
  компилятора на первых этапах.
- **Не сервер на Python (pygls)** и не shell-out — отвергнуто выбором
  пользователя в пользу self-host.
- **Не трогаем `selfhost/*` на запись** — только читаем как модули.

## 8. Этапы

Каждый — со своим определением готовности.

**Этап 0 — Транспорт и рукопожатие (proof of concept). ✅ 2026-07-18.**
Сделано: `editor/lsp/Transport.eat` — самодостаточный мини-парсер
заголовков (`scan_clen`, старт сразу с заголовков, без request-line
Http; регистронезависимо через `to_lower` из `lib/fmt/Ascii.eat`),
stdio-драйвер на аксиоме `read_byte` (`next_line`/`read_headers`/`read_body`
→ тело в `JDoc.put`), диспетч (`handle`/`dispatch`) и ответы билдером
`JOut` (`reply_initialize`/`reply_shutdown`/`reply_error`, `send` с
CRLF-обрамлением). `editor/lsp/LspMain.eat` — корневой `loop` в `main`,
зовёт `server.step()`, выход по EOF/`exit`, единственный `exit(0)`.
Клиент `editor/vscode/extension.js` (чистый JS, `vscode-languageclient`)
запускает сервер по stdio; подсветка декларативна и переживает падение
сервера. Хэндлеры: `initialize` (capabilities: `textDocumentSync: 1`,
`serverInfo`), `initialized`, `shutdown` (`result: null`), `exit`;
неизвестный запрос → `-32601`. *Готово:* `make verify_lsp` зелёный
(транскрипт рукопожатия == `tests/lsp/handshake.golden`, вход строит
`tests/lsp/verify.sh` с CRLF-обрамлением); `make check` включает LSP
(27 тестов, `scan_clen` чистый — без аксиом); `verify_sig` пересобран
(`to_lower` в `lib.sig`). *Отклонение от §6:* диспетч этапа 0 живёт в
`Transport.eat` (не отдельные `Rpc.eat`/`Handlers.eat`) — он ~40 строк;
распил на `Rpc`/`Handlers` — при росте на этапе 1. Клиент автоматически
не тестируется (нет npm/VSCode в гейте) — проверяется вручную; сервер
и рукопожатие в гейте.

**Этап 1 — Диагностики (`publishDiagnostics`). ✅ 2026-07-18.** Сделано:
фазы `lex+parse+check` слинкованы в сервер ПЛОСКОЙ склейкой (модуль 0) —
`build/JsonFlat.eat` (Json.eat со снятыми import-блоками, `tools/json_flat.py`)
+ фазы `selfhost/` + `editor/lsp/*` без import/export; единый запуск
`editor/lsp/serve.sh` (клиент VSCode и `verify.sh`). Реестр документов
`DocStore` (32 слота × 64 КБ, поиск по URI) в кадре main;
`textDocument/didOpen`/`didChange` (full sync)/`didClose`. На изменение —
прогон текста слота через `lexer_new→parser_new→check_new→run_typed` в
транзитный анализ-кадр (как `TypedMain`), первая ошибка (fail-fast,
решение пользователя) читается из НОВЫХ полей `el/ec/em` фаз (заполняются
рядом с `eprint`, вывод/паритет байт-в-байт не тронуты), позиция
конвертируется 1-based кодпойнт `line:col` → 0-based UTF-16
`line/character`, отправляется `publishDiagnostics` билдером `JOut`
(`dump()`/`print` не зовём — stdout занят). Чистый разбор и `didClose` —
пустой список (снять подчёркивания). *Готово:* `make verify_lsp` зелёный
(сессия `tests/lsp/session.golden`: лексическая/синтаксическая/типовая
ошибка + UTF-16-многобайт + чистый разбор + `didChange`/`didClose`);
`make check` собирает склейку (`build --no-bin`). *Правки фаз:* только
аддитивные поля `el/ec/em` в `Lexer/Parser/Check` (позиция+текст первой
ошибки) — дампы/IR их не читают, полный гейт §3 зелёный. *Отклонение от
§6:* линковка не «список файлов с lib/json», а плоская склейка +
`JsonFlat` (модульный `lib/json` и конкатенационные фазы несовместимы —
FAULTS 2026-07-18, урок 1). *Пределы этапа 1:* один диагноз за проход
(fail-fast компилятора); **файлы с `import`-блоками анализируются в
одиночку** — сервер на EATLang не имеет файловой аксиомы и не читает
импортируемые модули, поэтому ложная ошибка границы модуля (`Check.eat`
«модуль не встретился») **подавляется** (`lsp_is_import_err`): такой файл
получает лишь лексические/синтаксические диагностики, семантические —
нет (кросс-файловый анализ вне этапа, §7; полноценно возможен лишь с
резолвом модулей на стороне клиента VSCode или новой FS-аксиомой). URI >
256 байт не поддержан.

**Этапы 1a/1b/1c — Go-to-definition + резолв импортов (переприоритезирован
пользователем 2026-07-18, кросс-файловый). ✅ 2026-07-18 (make check +
verify_lsp зелёные).** **1c — кросс-файловая ДИАГНОСТИКА модульных файлов:**
сервер (`analyze_module`) строит синтетический поток `[Rt + deps за
#module-маркерами + открытый файл]` из корпуса и лексит целиком → `Check`
резолвит границы модулей → реальная типовая диагностика (не ложная «модуль
не встретился»). `#module` сбрасывает нумерацию строк на модуль, поэтому
позиция уже локальна файлу (ремап не нужен). Клиент шлёт `eat/order`
{uri, paths} (топ-порядок: Rt первым, deps раньше зависимых);
`on_order` пересчитывает диагностику модульным анализом. Подавление
обобщено (`lsp_suppress_err`): «import:» И «нет функции main» (библиотечный
модуль законно без main). Сделано: same-file (1a) + кросс-файл через корпус (1b) —
`definitionProvider`, `find_def`/`resolve_import`/`find_def_cross`,
`reply_def` (UTF-16 char = кодпойнт col−1, буфер не нужен: декларация за
ASCII-ключевым словом), корпус `DocStore.mods[24]` (клиент шлёт `eat/module`
{path,uri,text} — замыкание импортов из `extension.js`), транскрипт-кейсы
same/cross в `session.golden`. `textDocument/definition`: клик по имени
`func`/`struct`/`constexpr`/`enum` → переход к объявлению, включая
импортированные модули. *Механизм — лексический, без AST:* объявление =
токен-ключевое-слово (`func`=4, `constexpr`=7, `struct`=8, `enum`=9) +
следующий IDENT (=3) с искомым именем; поиск по токенам лексера, позиции
кодпойнт `line:col` уже есть. Кросс-файл: имя не найдено локально →
ищется в `import`-блоках документа (`import { N } from "path"`), путь
резолвится в **корпусе** (см. доставку), целевой файл лексируется, там
ищется объявление. *Доставка модулей (сервер без FS):* клиент VSCode
(Node, есть FS) на `didOpen`/`didChange` считает транзитивное замыкание
импортов открытого файла (парсит строки `from "..."`, рекурсивно от корня
репозитория, dedupe) и шлёт файлы серверу уведомлением `eat/module`
`{path, text}`; сервер копит их в bounded-корпусе (≤ N слотов, путь →
текст). Память ограничена замыканием открытых файлов, не всем воркспейсом.
*Позиции:* курсор приходит 0-based UTF-16 `line/character` → обратная
конверсия в кодпойнт-колонку (по байтам строки документа) → поиск токена,
накрывающего позицию; результат — кодпойнт `line:col` объявления →
вперёд в UTF-16 `Location{uri,range}`. Capability `definitionProvider:
true`. *Слои реализации:* (1а) same-file (пломбирует запрос
`textDocument/definition`, поиск токена под курсором, локальные декларации,
обратный UTF-16, ответ `Location`) — работает сразу, без корпуса; (1б)
корпус + кросс-файл (клиент шлёт `eat/module`, резолв импорт → путь →
корпус → лекс цели). *Готово:* клик по локальной `func`/`struct` прыгает к
объявлению; клик по импортированному имени открывает нужный модуль на
строке объявления; транскрипт-тест `textDocument/definition` (локальный +
кросс-файловый кейс). *Пределы:* разрешение по ИМЕНИ (лексическое, без
областей видимости) — одноимённые локальная и импортированная сущность
разрешаются локальной; перегрузок в языке нет, коллизии редки. Правок
языка/фаз нет.

**Этап 2 — Verifier-inlays (флагман). ✅ 2026-07-19.** Сделано: фаза
`verify` (Verify + Expr/Rel/Flow/Clamp/Dump; Dump — потому что в нём
живёт машина `analyze`, сам дамп не зовётся — stdout занят протоколом)
и `CheckFold` (ось `-O`) слинкованы в склейку (`LSP_PHASES`); бюджет —
868/2048 функций (запас 2,4×), худшая цепочка стека 181 МБ < 256 МБ
линкера. Запрос `textDocument/inlayHint` (`reply_inlay` в Handlers):
прогон потока слота `lex+parse+check[+fold]+verify` (лекс-построители
`lex_single`/`lex_module`/`lex_any` выделены из анализа этапа 1),
чтение таблицы обязательств `v.nck`/`ck_k`/`ck_n`/`ck_o` напрямую, на
каждую проверку в диапазоне строк запроса — хинт `{position, label
✓/⚠, tooltip «вид: доказано на компиляции / останется trap в
рантайме», paddingRight}`. Обязательства чужих модулей потока
отсекаются порогом: узлы открытого файла созданы после последней
`#module`-директивы (id узлов растут в порядке разбора), а позиции уже
локальны файлу (`#module` сбрасывает нумерацию строк — ремап не нужен).
Потолок 256 хинтов на ответ (JOut 64 КБ); переполнение → честный
`null`. Ось `-O`: уведомление `eat/opt {on}` (клиент шлёт настройку
`eatlang.optimize`) → `ct_fold_pass` перед verify (конвейер как `eatc
ir -O`), после переключения сервер сам просит
`workspace/inlayHint/refresh` (первый серверный запрос; ответ клиента —
id без method — диспетч теперь молча глотает). Capability
`inlayHintProvider: true`. Клиент: `vscode-languageclient` 8.x умеет
inlay hints сам; добавлены только настройка и отправка `eat/opt`.
*Готово (по критерию):* транскрипт-кейсы в `session.golden` —
недоказанная граница ⚠ / доказанная ✓ (канон), модульный поток
(вердикты только открытого файла), переключение `-O` меняет ⚠→✓ на
том же тексте; живой запуск при незакрытом stdin отвечает мгновенно.

→ *Следующий шаг — этап 6 (Test Explorer и CodeLens действий;
подтверждён пользователем 2026-07-19)*.

**Этап 3 — Hover. ✅ 2026-07-19.** Сделано: `textDocument/hover`
(`reply_hover` в Handlers, поиск под курсором — `Verify.lsp_hover` в
новом `editor/lsp/Hover.eat`). Прогон потока слота
`lex+parse+check[+fold]+verify` (как inlay), затем по токену под курсором:
(а) **тип выражения** — узел-выражение, начинающийся ровно на позиции
идентификатора (имя/вызов/член/метод), тип из `Check.nty` рендерится в
строку (`lsp_type_str` — итеративный принтер пула типов, как `write_type`,
без stdout); (б) **сигнатура функции с эффективным контрактом** — имя под
курсором в таблице `Check` (`find_func`): локальная функция → заголовок
ВЕРБАТИМ из буфера (строки от `func` до блока — сигнатура + заданные
клаузы) плюс досинтез опущенных как `requires true`/`ensures true`
(омиссия ≡ `true`); импортированная → сигнатура из таблицы (контракт в
модуле); (в) **учительский hover «почему trap»** — обязательство
верификатора на строке курсора (`v.ck_*`, ближайшее по колонке, порог
`#module` как у inlay): ✓ доказано / ⚠ не доказано + разбор `lsp_teach`
(какого `requires`/инварианта/маски не хватило — в рамках языка, не
обход). Тип и разбор совмещаются сегментами markdown (потолок строки
256 — значение собирается из сегментов `body/body2/body3` в JOut).
Capability `hoverProvider: true`. Правок фаз НЕТ: `lsp_*`-методы живут в
`extend Check`/`extend Verify` ТОЛЬКО в сборке LSP (в списках `SELFHOST_*`
файла `Hover.eat` нет) — паритет `src/eatc ↔ selfhost` не затронут.
*Готово (по критерию):* транскрипт-кейсы в `session.golden` — сигнатура
+ синтез `requires/ensures true`, ⚠ bounds + разбор + тип, ✓ overflow +
тип, промах → `null`, импортированная функция; живой запуск при
незакрытом stdin отвечает мгновенно.

**Этап 4 — Автодополнение + snippets. ✅ 2026-07-19.** Сделано:
`textDocument/completion` (capability `completionProvider`, trigger «.»).
Курсор **не за точкой** → общий список без анализа: снипеты каркасов
(`func` с `requires/ensures` напоказ, `for … in`, `match` с `Ok/Err`,
`test`; `insertTextFormat: 2` — табстопы `${N}`), ключевые слова
(kind 14; `func/for/match/test` не дублируются — их несут снипеты),
встроенные (касты/ядро/аксиомы ОС, kind 3) и конструкторы `Ok/Err/
Some/None` (kind 4); фильтрует по префиксу клиент. Курсор **за точкой**
→ поля (kind 5, detail — тип) и методы (kind 2, detail — сигнатура без
self) структуры приёмника: разбор сырой строки буфера назад от курсора
(`cmpl_ctx`: префикс → точка → идентификатор приёмника или `self`;
цепочка `a.b.` не поддержана — честный `null`), прогон `lex+parse+
check` (верификатор completion не нужен) и **тип приёмника из
`Check.nty`**: name-узел (37) с именем приёмника / self-узел (38) /
инициализатор let-узла (13 — случай «объявлена, первое использование
под курсором»); выбор — ближайший кандидат выше строки курсора, резерв
— первый любой. Тип вида struct → `find_struct` → поля `sgs/sgc→gn_*`,
методы `fown == si`; рендер деталей — наличные `lsp_type_str`/
`lsp_sig_str` (Hover.eat). **Сломанный текст** (обычный случай: строка
курсора кончается точкой, `p.` не парсится) анализируется повторным
лексом с «прогалом» — строка курсора заменяется пробелами
(`lex_any_b`), нумерация остальных строк сохраняется; `Check.failed`
при этом допустим (таблицы деклараций полны после разбора, `nty`
заполнен до точки провала — «не используется» на прогале не мешает).
Резерв для тел без типизации (нет `main` → Check падает до тел):
владелец `self` лексически — последняя пара `struct|extend IDENT` выше
курсора. Вся машинерия — в НОВОМ `editor/lsp/Complete.eat` (`extend
Check` + свободные `lsp_*`), только в сборке LSP — паритет не тронут;
правок фаз нет. *Готово (по критерию):* транскрипт-кейсы в
`session.golden` — общий список со снипетами; поля+методы по `p.` со
сломанной строкой; `null`, когда прогал не чинит разбор (точка на
другой строке того же файла); `self.` через nty; поля на валидном
тексте; `extend`-блок без `main` через лексический резерв; живой
запуск при незакрытом stdin отвечает мгновенно. Бюджет: стек худшей
цепочки 188,6 МБ < 256 МБ.

**Этап 5 — CodeLens бюджета §8. ✅ 2026-07-19.** Сделано:
`textDocument/codeLens` (capability `codeLensProvider`) — сводная ленза
§8 на строке 0 («функций N/2048 · стмтов T · стек ≤ S Б · глобалы G Б ·
доказано P/Q») плюс ленза каждой функции открытого файла («кадр F Б ·
цепочка ≤ C Б · стмтов s/60 · доказано p/q»). Решение пользователя:
фаза `ir` СЛИНКОВАНА в склейку (`LSP_PHASES` + `lib/fmt/Fmt.eat`) ради
точного стека в байтах; бюджет 1212/2048 функций, худшая цепочка
197,4 МБ < 256 МБ. Машинерия — НОВЫЙ `editor/lsp/Lens.eat` (`extend
Check`/`Verify`/`Ir`, LSP-only — паритет не тронут, правок фаз нет):
кадры — measure-прогон эмиссии (`gen_body` при `msr==1`, печати нет —
stdout занят протоколом) + скан alloca-строк entry-блока (блок 0 держит
только alloca и br) + обратный поиск интернированного типа по ТЕКСТУ в
мемо принтера (`tys`/`tyl`; равный текст ⇒ равный ABI-размер) + свой
ABI-сайзер по пулу типов Check (фикспойнт, лейаут LLVM: str →
{i32,[256 x i8]} = 260, struct — смещения по выравниванию); худшая
цепочка — DFS с мемо по рёбрам DAG Check (`eg_c`/`eg_d`, зеркало
`codegen._memory_report` вкл. оговорку «callee not in frames»); глобалы
— пулы `gss/gsl/gvk` (trap-тексты с NUL; литералы 4+256) + массивы-
константы A2; доля доказанного — `v.ck_*` c атрибуцией по функциям
(владелец узла — max `fnd ≤ n` до следующей декларации). Сверка с
отчётом `eatc build` на идентичном входе: кадры/стек/доказанность
байт-в-байт, глобалы — с точностью до имени файла в trap-строках
(«stdin» против пути; 58 = 71 − 13 ровно). Грабли (FAULTS): Verify и
Ir в одном кадре превысили бы 256 МБ → ДВА последовательных кадра
(`lens_obl`: verify + упаковка маски `nvm` 8 бит/узел; `lens_mem`:
свежий lex+parse+check + разлив маски + measure — без элизии глобалы
разошлись бы с build, кадры от элизии не зависят — проверено на
Python); эмиссия требует рантайм-модуль первым в потоке → без корпуса
клиента (struct RtStr) — честный `null`, живой клиент всегда шлёт Rt в
`eat/order`. *Готово (по критерию):* транскрипт-кейсы id 50–52 в
`session.golden` — полный отчёт по модульному потоку (числа сверены с
build), null без корпуса, обновление лензы на правку (`didChange`:
стмтов 5→6, стек 25→29 Б); живой запуск при незакрытом stdin отвечает
мгновенно (0,03 с на Math+Rt).

**Этап 6 (опц.) — Test Explorer и CodeLens действий.** `test`-блоки → в
Test Explorer VSCode; CodeLens `run ▸ / verify ✓ / build ⚙` над `main`.
*Готово:* тест-блоки запускаются из панели тестов.

## 9. Паритет

Зеркала `src/eatc/ ↔ selfhost/` нет — план новый код компилятора не
пишет (см. поле «Зеркало в selfhost»). Сверка вместо паритета — **транскрипт
JSON-RPC-сессии** (`tests/lsp/*.jsonl` → `make verify_lsp`), по образцу
`EAT_NET` для HTTP: детерминированная последовательность запрос→ответ,
байт-в-байт. Если позже сервер захочет собственный структурный вывод
(например, эталон для дифф-теста) — оформить как стабильный построчный
формат, как у прочих дампов.

## 10. Открытые вопросы (решение пользователя)

1. **Место исходников сервера.** ✅ Решено 2026-07-18: `editor/lsp/`
   (рядом с клиентом `editor/vscode/`, всё редакторское в одной ветке).
2. **Порядок этапов после флагмана.** Пользователь выбрал все четыре
   фичи; предложенный порядок 2→3→4→5 (сначала уникальное: verifier-inlays
   и учительский hover, затем классический IDE-минимум). Переставить?
3. **Объём протокола на этапе 0.** Минимум = `initialize/initialized/
   shutdown/exit` + `didOpen/didChange/didClose`. Достаточно?
4. **Позиция в стеке задач.** ✅ Решено 2026-07-18: №4, старт с этапа 0
   сразу (код языка не трогает — можно параллельно REFACTOR_LAYOUT).
5. **Зависимость клиента (`vscode-languageclient`)** — единственная новая
   npm-зависимость расширения (не Python, компилятор остаётся на одном
   `llvmlite`). Приемлемо?

## 11. Инварианты

План **не открывает** ни одного обхода Power of 10:

- **Куча — нет.** Реестр документов, кадр сообщения, анализ-кадр — пулы
  фикс. размера в кадре `main`; переполнение → ошибка-значение (как `507`
  у `Store`), не рост.
- **Рекурсия — нет.** Разбор JSON и обход AST — явные стеки кадров
  (наличная машинерия `Json.eat`/фаз); граф вызовов остаётся DAG.
- **Цикл без границы — нет.** Единственный `loop` — корневой в `main`
  (правило 1), как у HTTP-сервера; все внутренние циклы — `for` с
  bounded-границей.
- **Глобалов — нет.** Состояние сервера живёт в кадре `main` значением,
  передаётся по `let self`.
- **Молча отброшенного результата — нет.** Ответы протокола и коды фаз —
  явные; неиспользуемое — через `discard`.
- **Warning вместо ошибки — нет.** Сервер лишь **показывает** вердикты
  компилятора; правил не ослабляет.

## 12. Затронутые поверхности

- **`editor/lsp/`** (новый) — сервер: `Transport.eat`, `Handlers.eat`,
  `Hover.eat` (этап 3 — рендер типа/сигнатуры/учительский разбор в
  `extend Check`/`extend Verify`, LSP-only), `Complete.eat` (этап 4 —
  контекст/тип приёмника/члены и таблицы общего списка, LSP-only),
  `LspMain.eat`.
- **`editor/vscode/`** — клиент: `package.json` (`activationEvents`,
  `main`, зависимость `vscode-languageclient`), `src/extension.ts`,
  `client/`; README расширения. TextMate-грамматика остаётся.
- **`Makefile`** — цели сборки сервера и `verify_lsp` (транскрипт-сверка);
  добавить в полный гейт после стабилизации.
- **`tests/lsp/`** (новый) — записанные JSON-RPC-сессии.
- **`lib/http/Http.eat`** — при выделении общего header-парсера (старт с
  заголовков) координировать с владельцем файла; при возможности —
  переиспользовать без правки, вынести header-старт в сервер.
- **Документация** — `README.md` (витрина: «LSP для VSCode»),
  `AGENTS.md`/`docs/PROCESSES.md` (новая цель `verify_lsp`), индекс
  `docs/plans/README.md`.
- **Координация с [DX_PLAN](DX_PLAN.md)** — он тоже объявляет `editor/vscode`
  рабочей поверхностью (трек L2, `tmLanguage.json`). Пересечение —
  `package.json`; грамматику L2 и клиент LSP держать в разных файлах,
  правки `package.json` согласовывать.

## 13. Определение готовности

Полный гейт AGENTS.md §3 остаётся зелёным (сервер — приложение, канон
компилятора не трогает), **плюс** новая цель:

```sh
make verify_lsp   # сервер == записанный транскрипт JSON-RPC-сессии,
                  # байт-в-байт (рукопожатие + диагностики + inlays +
                  # hover + completion)
```

Каждый этап добавляет кейсы в `tests/lsp/`. Клиент проверяется вручную в
VSCode (подключение, подчёркивания, inlay-значки, hover, дополнение,
CodeLens) — по чеклисту в README расширения.
