# FAULTS — журнал ошибок исполнения работ

Сюда записываются ошибки и сбои, встреченные при исполнении этапов
планов (правило [docs/WORKFLOW.md](../WORKFLOW.md) §3.4). Индекс
планов — [README.md](README.md).

Формат записи: дата · план/этап · симптом · причина · как
исправлено/обойдено · урок.

---

## 2026-07-17 · HTTP_PLAN · этап 4 (тело/сериализация)

1. **i64-литерал под `i64(…)` не помещается в i32.** Симптом:
   `Some(i64(5000000000))` падало «5000000000 не помещается в i32».
   Причина: внутренний числовой литерал типизируется как i32 ещё до
   каста `i64(…)`. Исправлено: широкие литералы вводить в i64-контексте
   напрямую — `let v: i64 = 5000000000` (как `const … : i64 =` в
   CheckConst), а не оборачивать i32-литерал в `i64(…)`. Урок: каст
   не расширяет литерал — целится тип объявления/контекста, не обёртки.

2. **Функция chunked-автомата упёрлась в правило 4 (≤ 60 statements).**
   Симптом: `Body.chunk` — 64 statements, `make check` красный.
   Причина: один метод вёл все шесть подсостояний chunked (размер,
   данные, два разделителя, финальный CRLF). Исправлено: расщеплён на
   `chunk_size` (size-строка + расширения) и `chunk_delim` (CRLF после
   данных и финал), `chunk` — диспетч. Урок: потолок statements — не
   помеха, а подсказка резать автомат по фазам; дробить сразу, не
   «на потом».

3. **Тело не ломало Req — отдельная структура вместо расширения
   состояния.** Симптом-риск: втянуть тело в конечный автомат `Req`
   означало переномеровать состояния (0 reqline · 1 headers · 2 done ·
   3 error) и задеть все проверки `state == 2/3` и существующие тесты.
   Решение: тело — самостоятельная `Body` (Content-Length/chunked),
   `Req` не тронут (после CRLFCRLF вызывающий переключается на
   `Body.push`). Тесты `Req` остались зелёными без правок, паритет —
   через общий фронтенд. Урок: расширяя обкатанный автомат, дешевле
   вынести новую фазу в свой тип, чем перенумеровать состояния
   несущего.

1. **Вакуумный пофайловый паритет.** Симптом: пофайловая сверка
   «эталон против self-hosted верификатора» показывала зелёным все
   модули компилятора и библиотеки. Причина: файлы без функции `main`
   не проходят проверку типов — оба сравниваемых вывода пусты,
   сравнение ничего не проверяло. Исправлено: содержательное покрытие
   этих файлов даёт тест самоприменения (все модули склеиваются в один
   корректный вход); ограничение задокументировано в плане §5 и в
   описании гейта. Урок: «зелёный» отрицательный кейс (обе стороны
   упали) — не доказательство паритета; у каждого пункта сверки
   должен быть позитивный сигнал.

2. **Глобальные регистры против вложенных циклов.** Симптом: точечные
   расхождения дампов только на коде с вложенными циклами. Причина:
   несколько «регистров» состояния анализатора (список ускоряемых
   путей, снимок значений на входе цикла) были одиночными полями и
   затирались обработкой вложенного цикла до того, как внешний цикл
   их дочитает. Исправлено: состояние переведено на кадровые копии
   (своя копия на каждый уровень вложенности). Урок: в машине с
   явным стеком кадров любое состояние, живущее «через» вложенный
   кадр, обязано храниться в кадре, а не в глобальном поле.

3. **Сверка по устаревшему входу.** Симптом: анализ расхождения
   опирался на склеенный входной файл во временной папке, собранный
   до очередных правок исходников, — часть выводов не сходилась с
   кодом. Причина: временный артефакт не пересобирался. Исправлено:
   вход пересобирается перед каждой сверкой той же командой. Урок:
   временные артефакты сверок пересобирать всегда, привычкой, а не
   по подозрению.

4. **Правка занятого файла невозможна.** Симптом: индекс планов
   (docs/plans/README.md) требовал обновления строки верификатора, но
   файл содержал чужие незакоммиченные правки параллельной работы.
   Обойдено: правка отложена и передана в промт следующего этапа.
   Урок: закреплено правилом WORKFLOW §3.6 (не трогать чужие
   незакоммиченные файлы).

## 2026-07-16 · элизия защитных копий интерпретатора (JSON_PLAN §10, коммит e3ef4bf)

1. **Красный гейт на общем дереве чужим WIP.** Симптом: полный гейт
   этапа падал на verify_bootstrap — segfault `./build/SelfIr2`
   (stage1 при этом «BOOT OK»). Причина: незакоммиченные правки
   параллельного трека верификатора в selfhost/*.eat; собственный
   дифф этапа (interpreter.py, lib/Json.eat) кодоген не трогает.
   Обойдено атрибуцией в три прогона: (а) общее дерево без своих
   файлов — падает так же; (б) чистый HEAD в worktree — зелёный;
   (в) HEAD + свой дифф в worktree — полный гейт зелёный, коммит
   легитимен. Чужой segfault ушёл с финальным коммитом параллельного
   трека (4f8683e). Урок: красный гейт на общем дереве сначала
   атрибутируется, потом чинится; зелёный полный гейт на HEAD+дифф в
   worktree — достаточное определение готовности. Закреплено в
   WORKFLOW §4 (атрибуция + worktree-гейт, с нюансом `.venv`).

## 2026-07-16 · AGENTS_ORCHESTRATION_PLAN · конвенции §§1–3 → WORKFLOW

1. **Красный гейт на общем дереве при одновременных прогонах.**
   Симптом: полный гейт этапа (правки только docs) упал на
   verify_selfhost — `LEX DIFF examples/alias/Alias.eat`; файл давно
   в HEAD, кода в диффе этапа нет. Причина (атрибуция): параллельный
   агент (аудит документации) в тот же момент вёл работы в общем
   дереве — два одновременных make-прогона в одном каталоге затирают
   друг другу артефакты сверок (дампы). Обойдено: полный гейт в
   worktree на HEAD + свой дифф — зелёный целиком, коммит легитимен
   (WORKFLOW §4). Урок: гейт на общем дереве не запускать, пока в нём
   активен другой агент; при любом подозрении на соседа — сразу
   worktree-стенд, не пересчёт на общем дереве.

## 2026-07-16 · REFACTOR_SELFHOST_PLAN · этап 1 (потолок 100)

1. **`make bench` падал на замере размеров до начала правок.**
   Симптом: `bench_size` → трейсбек
   `ValueError: invalid literal for int(): ' 16 (zerofill)'`
   (tests/bench/bench.py:676). Причина: новый вывод `size -m` на
   macOS дописывает к числу пометку вида `(zerofill)`; парсер ждал
   голое число. Ошибка воспроизводилась на чистом состоянии до
   правок этапа — не следствие рефакторинга. Исправлено:
   `int(val.split()[0])` — берём первое поле. За падением size
   скрывался второй устаревший кейс: стресс токенов ждал отказа на
   ~140К при пределе 131072, но предел поднят до 262144 ещё этапом 4
   верификатора (4f8683e) — bench с тех пор целиком не добегал.
   Кейсы сдвинуты на ~260К/~270К. Урок: эталон метрик «до» снимается
   в начале этапа — это же выявляет сломанные инструменты замера до
   того, как они смешаются с эффектом правки; при подъёме предела §6
   обходить и стрессы bench, не только tests/.

2. **Автосплит elif-таблицы терял `elif` у таблиц без `else`.**
   Симптом: после скриптового переформатирования однострочных
   elif-таблиц (Ast.eat:130, Lexer.eat:58) сверка потоков токенов
   показала пропажу одного `ELIF`. Причина: запасная ветка скрипта
   для таблиц, заканчивающихся не `else`, а последним `elif`,
   склеивала хвост без ключевого слова. Поймано сверкой лекс-дампов
   до/после (позиции и NEWLINE отброшены — остальное байт-в-байт),
   исправлено точечно. Урок: у механических трансформаций исходников
   держать машинную инварианту (поток токенов не меняется, кроме
   заявленных вставок) и гонять её после каждого файла.

## 2026-07-16 · AGENTS_ORCHESTRATION_PLAN · поле «Рабочие поверхности»

1. **Гонка гейтов через общие /tmp-дампы: сосед появился посреди
   прогона.** Симптом: полный гейт этапа (дифф — только docs/plans)
   упал на verify_selfhost сразу после «VERIFIED SelfTyped», без
   строки диагностики; повтор цели — зелёный. Причина (атрибуция):
   перед стартом гейта дерево было чистым и make-процессов не было,
   но параллельный этап (backlog-план элизии comptime-копий,
   c98de60) вёл свой полный гейт в те же минуты на worktree-стенде;
   verify_selfhost и соседние цели пишут сверочные дампы в
   фиксированные пути `/tmp/eat_*_{ref,self}.txt` — они общие для
   всей машины, worktree изолирует только `build/`. Обойдено: полный
   гейт на worktree-стенде HEAD+дифф после завершения соседа —
   зелёный целиком (9 целей, 2:53). Урок: проверка «соседа нет»
   перед стартом не страхует весь прогон — сосед может начать позже;
   для целей с фиксированными /tmp-путями действует «один прогон на
   машину», а не «один прогон на дерево». Кандидат-фикс: уникальный
   каталог дампов на прогон (`mktemp -d` в переменной Makefile) —
   правка общего файла, отдельным шагом.

## 2026-07-16 · SELFHOST_OPT_PLAN · этапы 1–2

1. **Правки selfhost на общем дереве при активном соседе.** Симптом:
   правки `selfhost/Check.eat` (soft-fail D4) начаты в общем рабочем
   дереве, пока трек REFACTOR_SELFHOST вёл этап 4 (распил тех же
   файлов); пользователь остановил вручную. Причина: решение «можно на
   общем дереве» принято по шапке соседского плана («поверхности
   свободны до старта этапа 4») и чистому `git status` на момент
   старта — но сосед стартовал этап позже, посреди моей работы;
   правило WORKFLOW §4 «≥ 2 активных трека → worktree» уже выполнялось
   на старте (оба трека 🚧). Исправлено: свой дифф перенесён в worktree
   `track/selfhost-opt`, общее дерево возвращено на HEAD, гейт — в
   стенде. Урок: условие для worktree — «активны ≥ 2 трека с риском
   пересечения поверхностей», оценивать по статусам планов на старте
   трека, а не по текущей занятости файлов: сосед может начать в любой
   момент.

## 2026-07-16 · ASYNC_PLAN · этап 0 (аксиомы in_avail/ticks)

1. **Код вида типа вместо интернированного индекса.** Симптом:
   self-hosted компилятор на вызове `ticks()` падал с «тип значения
   не совпадает», эталон файл принимал. Причина: результат встроенной
   в `tres` (Check) и `serrr` (Ir) — это **интернированный индекс**
   пула типов, а не код вида; для u32 они совпадают (пред-интерн 0..5),
   для u64 — нет. Исправлено: `self.u64_ty()` в CheckBody,
   `self.u64t()` в IrExpr; правило дописано в MODIFYING §2. Урок:
   прецедент `arg_count` (`tres = 1`) обманчив — совпадение индекса и
   вида верно только для первых шести типов.

2. **Устаревшая таблица xa-кодов в MODIFYING §2.** Симптом: план
   этапа зарезервировал «коды 116/117 — строго в конец» по таблице
   (`…115 Some`), а коды уже заняты `arg_len`/`arg_byte` (трек argv).
   Причина: таблицу не дополнили при добавлении argv. Исправлено:
   116/117 внесены в таблицу; выяснено, что 0-аргументным встроенным
   код вообще не нужен (диспетчеризация по спану имени). Урок: перед
   резервированием кодов сверяться с кодом (`grep 'xa\[f\] ='`), а не
   только со справкой; таблицу дополнять в том же коммите, что и код.

3. **Ложная просадка bench сразу после гейта.** Симптом: `make bench`
   показал ir 586K ток/с против 672K у соседнего свежего замера
   (−13%), lex/parse — тоже ниже, хотя лексер/парсер не менялись.
   Причина: одиночный прогон бенча сразу после трёхминутного гейта на
   тёплой машине; шум. Исправлено: прямой A/B — SelfIr эталонного
   коммита и свой на одном входе, по 5 прогонов: 0.15 с == 0.15 с,
   регрессии нет. Урок: цифре одиночного bench-прогона не верить,
   если просели и непотроганные фазы; атрибуция — A/B двух бинарников
   на одном входе, лучшие из ≥ 5 прогонов.

## 2026-07-17 · SELFHOST_VERIFIER_PLAN · этап 5.1 (диагностика решений -O)

1. **Верификатор не зеркалировал argv-аксиомы — вскрыло самоприменение.**
   Симптом: после добавления флага `-O` в `VerifyMain.eat` разошёлся
   **канонный** дамп самоприменения: эталон ставит на `arg_len`/
   `arg_byte` bounds-обязательства «в рантайме» (verifier.py:2182),
   SelfVerify эти встроенные не обрабатывал вовсе. Причина: ни один
   вход с `main` до сих пор argv-аксиом не использовал (lib/Args.eat —
   файл без `main`, вакуумный паритет); пробел жил с трека argv
   незамеченным. Исправлено: ветка `call_arg_axiom` в VerifyExpr
   (зеркало эталона). Урок: **новая конструкция в самом коде
   верификатора/мейна = новый вход самоприменения** — правка мейна
   может вскрыть незеркалированную ветку анализа; при первом
   использовании встроенной в selfhost-коде проверить её зеркало в
   Verify (пофайловая зелень этого не докажет).

2. **Ложный OK сверки на отсутствующем файле кейса.** Симптом:
   кейс `51_fold_call_point` из VERIFY_GATE был переименован в
   `54_...` (номера 51–53 заняты), а гейт остался зелёным: на
   несуществующем файле оба дампа пусты, `diff` пустоты с пустотой —
   OK. Причина: сверочный цикл не проверяет доступность входа.
   Исправлено: `test -f` перед сверкой (`VERIFY NO CASE`). Урок:
   сверочные циклы обязаны падать на недоступном входе; «оба пусто»
   — не паритет, а отсутствие теста (родня вакуумного пофайлового
   паритета, FAULTS 2026-07-16).

## 2026-07-17 · ASYNC_PLAN · второй пример (конвейер фильтров stdin)

1. **Гейт в общем дереве при живом соседе.** Симптом: полный гейт
   упал на `verify_selfhost_opt` (`IR-O DIFF tests/fold/Fold.eat`),
   хотя правки этапа — только пример и обвязка Makefile, ось `-O`
   не задевались. Причина: во время прогона в общем рабочем дереве
   появилась незакоммиченная правка соседа (`src/eatc/__main__.py`,
   этап 5.2 верификатора): конвейер `ir -O` стал `fold → verify`,
   эталон уже элидирует (`llvm.assume` вместо trap-блоков), а зеркало
   `SelfIrOpt` ещё нет. Исправлено/обойдено: причинность доказана
   сверкой `ir -O` на исходниках HEAD (`PYTHONPATH` на worktree)
   против текущих; готовность этапа подтверждена полным гейтом в
   чистом worktree HEAD + свои правки; чужой файл в коммит не вошёл.
   Урок: перед гейтом и коммитом сверять `git status` с состоянием
   на старте этапа; появившийся чужой ханк в поверхности гейта —
   сигнал прогонять гейт в worktree (HEAD + свои правки), а не в
   общем дереве.

## 2026-07-17 · ASYNC_PLAN · этап 1 (ярус 1, lib/Async.eat)

1. **Тест с аксиомой ticks() в lib сдвинул виртуальные часы
   программы.** Симптом: после перевода Blink на `Timer` вывод
   Async.eat сместился на 2 тика (`blink: №1 на тике 2` вместо
   `тике 0`) — перевод обязан был быть байт-в-байт. Причина:
   интерпретатор исполняет test-блоки всех модулей потока перед
   `main` (`__main__.py`, cmd_run), а тест `async_timer` дважды звал
   `fire()` → `ticks()` и продвигал счётчик `EAT_TICKS=virt`; в
   бинарник тесты не компилируются, так что ломался и паритет
   `interp == native`. Исправлено: у `Timer` теста нет намеренно
   (комментарий-обоснование в lib/Async.eat); поведение Timer
   доказывают примеры examples/async в `make verify`. Урок: в
   test-блоках модулей нельзя звать аксиомы с наблюдаемым
   состоянием (часы, stdin) — тесты исполняются только
   интерпретатором и только перед `main`, «одинаковый вход» сверки
   включает и вызовы аксиом из тестов.

## 2026-07-17 · OPTIMIZATIONS_PLAN · этап 7.3 (профиль IPC эмулятора)

1. **Промт этапа закладывался на xctrace CPU Counters, которого на
   машине нет.** Симптом: `xcrun xctrace` — «unable to find utility»;
   профиль стойл по событиям PMU (зависимости/store-to-load против
   ветвлений) снять нечем. Причина: на машине только
   CommandLineTools (без Xcode/Instruments), root-доступа для
   kperf/powermetrics нет; headless-предположение промта не
   проверялось заранее. Исправлено/обойдено: атрибуция выполнена
   попарными изоляциями — варианты одного IR (канон/элизия,
   ±assume, ±nsw/nuw, ±alwaysinline) через оба бэкенда (llvmlite и
   clang) на одном входе со сверкой байт-в-байт; счётчики
   instructions/cycles из `/usr/bin/time -l`. Разложение получилось
   строже событийного профиля (прямая причинность вместо
   корреляции стойл). Урок: в промт измерительного этапа закладывать
   проверку инструментария первым шагом и явный fallback; для
   вопросов «какая конструкция IR сколько стоит» дифф-эксперименты
   над .ll предпочтительнее счётчиков стойл.

## 2026-07-17 · OPTIMIZATIONS_PLAN · этап 7.1 (снятие llvm.assume оси -O)

1. **Диагноз реестра строился на неверной посылке об оси сравнения.**
   Симптом: §7.1 объяснял разрыв «-O медленнее build» тем, что build —
   «элизия без assume». Причина: кодоген общий, `requires_proven`
   ставит verify и в `cmd_build`, и в `ir -O` — build эмитил те же
   assume (в branch-артефактах build/ ось `-O` вообще без assume, IR
   совпадает с build с точностью до trap-строк). Исправлено: гипотеза
   перепроверена на артефактах до правки (grep llvm.assume + diff
   .ll), вердикт §7.1 переписан по фактам; реальный виновник branch —
   конвейер сборки (§7.2). Урок: перед оптимизацией «убрать X»
   сначала убедиться, что X присутствует именно на медленной оси и
   отсутствует на быстрой — один grep по готовым артефактам дешевле
   этапа правки.

2. **Гейт verify_selfhost_opt не покрывает конкатенации примеров —
   дрейф решений жил незамеченным.** Симптом: смок-паритет этапа на
   бенч-конкатенации mos6502 (Rt+Hex+Cpu6502+Tests+Main) разошёлся
   (272 строки диффа), хотя полный гейт на HEAD зелёный; на чистом
   HEAD в worktree — тот же дифф, правка ни при чём. Причина: гейт
   гоняет examples/tests пофайлово (вакуумный паритет без вызовов)
   плюс самоприменение; полносоставные программы примеров в него не
   входят, а дрейф проявляется только в контексте вызовов:
   `hex_digit` (lib/Hex, requires v<=15) Python-верификатор
   доказывает, selfhost — нет (дампы `verify -O` расходятся одной
   строкой: proven 331 против 330 из 491), и selfhost-ось `-O` несёт
   лишний trap-блок. Обойдено: расхождение зафиксировано как
   предсуществующее (вчерашние артефакты: 16 assume у build против
   15 у SelfIrOpt — тот же дрейф), этап закрыт по своим гейтам.
   Урок: сверки решений `verify -O` ↔ `SelfVerify -O` и IR-паритет
   нужны и на полносоставных программах (конкатенации примеров) —
   кандидат-этап: расширить гейт + починить доказательство
   `hex_digit` в selfhost-верификаторе.

## 2026-07-17 · OPTIMIZATIONS_PLAN · этап 7.2 (конвейер сборки: llvmlite против clang)

1. **Текстовый .ll не переносим между версиями LLVM без чистки.**
   Симптом: гибрид «llvmlite-мид → clang» падал на парсинге —
   `unterminated attribute group` (`nocreateundeforpoison`), затем
   `Intrinsic has incorrect argument type` (`llvm.lifetime.*`).
   Причина: llvmlite 0.48 несёт LLVM 22.1.0, Apple clang — 21.0.0;
   оптимизированный дамп LLVM 22 содержит атрибуты и сигнатуры
   интринсиков, которых clang 21 не знает (канонический
   НЕоптимизированный .ll обе стороны едят без проблем — конструкции
   простые). Обойдено: чистка дампа в backends.py (снятие
   атрибутов-подсказок, вырезание lifetime-маркеров) — семантика
   цела, сверки stdout зелёные; гибридные числа — с точностью до
   вычищенных подсказок. Урок: любой будущий разговор о «clang по
   .ll как маршрут» обязан фиксировать версию LLVM обеих сторон;
   переносим объектник/битод одной версии, а не текст между
   версиями.

2. **Сравнение конвейеров через разные IR смешало две переменные.**
   Симптом: посылка §7.2 «clang не хуже на 5/7 нагрузок» из отчёта
   CROSSLANG. Причина: там clang мерился на каноне (все trap'ы), а
   llvmlite — на элизии; для эмулятора канон сам по себе быстрее
   элизии (кроссовер аутлайн/инлайн, §7.3), и «преимущество clang»
   было преимуществом канон-IR. На байт-идентичном .ll clang нигде
   не выигрывает (branch +28…+31 %, mos6502 +4,9…+6,8 % к build).
   Урок: сравнение тулчейнов допустимо только при байт-идентичном
   входе; «две ручки за раз» в замере — та же ошибка, что и в
   посылке 7.1 (см. выше), теперь закреплена методикой backends.py.

## 2026-07-17 · U128_PLAN · этапы 0–3 (lib/U128.eat + Q32.32)

1. **Ханки общего дока уехали в чужой коммит.** Симптом: после
   коммита соседа (http_p0, 5e23e72) `docs/GUIDE.md` исчез из
   `git status`, хотя мои правки (карта lib/ + подраздел про
   U128/Q32.32) ещё не коммитились — сосед добавил файл целиком, и
   его коммит унёс мои строки (в истории http_p0 упоминает модуль,
   которого на том коммите ещё нет). Причина: при живом соседе оба
   трека держали незакоммиченные ханки в одном общем файле дольше,
   чем нужно. Урок: правку общего файла — держать как можно ближе к
   своему коммиту, а перед `git add` общего файла всегда смотреть
   `git diff <файл>` на предмет чужих строк (правило точечных ханков,
   прецедент a34d5fe); после коммита соседа — пересверить, что из
   твоего уже в HEAD, и не коммитить дубли.

2. **HEAD уехал во время гейта.** Симптом: полный гейт крутился в
   worktree от HEAD 81a4b0a, а к моменту коммита HEAD стал 5e23e72
   (сокет-аксиомы в обоих компиляторах). Урок: при живом соседе гейт
   в worktree — это «HEAD на момент старта»; перед коммитом сверить
   `git log -1` и, если HEAD сместился, перегнать гейт на новом HEAD
   + свой дифф (2–3 минуты дешевле сломанного master).

## 2026-07-17 · HTTP_PLAN · этапы 0–3 (сокет-аксиомы, lib/Http, пул, роутер)

1. **Имя встроенного длиннее 16 байт не матчится `span_is`.** Симптом:
   selfhost на `socket_write_span` (17 байт) падал «неизвестная
   функция», хотя ветка диспетча написана. Причина: `span_is` принимает
   `kw: str<16>` и первым делом отвечает `false` при `l > 16` — имя
   молча не сравнилось. Урок: для имени длиннее 16 байт — отдельный
   хелпер (`span_is_sws`: первые 16 байт через `span_is` + хвост
   прямым чтением пула `sb_at`); при добавлении встроенного сверять
   длину имени с потолком `span_is` до отладки диспетча.

2. **`var` в параметрах — только у `self`.** Симптом: дважды за трек
   (feed_line в lib/Http, html_page в Router) парсер падал «ожидалось
   self (var в параметрах — только у self)». Урок: хелпер, мутирующий
   структуру, — либо метод `var self` на ней, либо функция,
   возвращающая новое значение; свободных `var`-параметров в языке нет
   (правило языка, не пробел) — помнить до написания, а не после.

## 2026-07-17 · U128_PLAN · бенчи (внутренний контур + crosslang)

1. **Закоммиченные ханки затёрты чужим коммитом из устаревшей копии.**
   Симптом: после серии http-коммитов соседа мои строки SigProbe
   (import `mul_64` + проба, коммит 9a26dc0) исчезли из HEAD — сосед
   готовил свою правку SigProbe от копии до 9a26dc0 и закоммитил файл
   целиком. Исправление: рабочее дерево держало объединённую версию —
   `regen_sig` и повторный коммит своих ханков. Урок (продолжение
   урока http_p0): после каждого чужого коммита в общий файл сверять
   `git show HEAD:<файл>` не только на дубли своих строк, но и на
   **потерю уже закоммиченного**; смешанные файлы стейджить блобом
   (`git hash-object -w` + `git update-index --cacheinfo`), а не
   файлом целиком.

## 2026-07-17 · OPTIMIZATIONS_PLAN · кандидат 7.6 (дрейф hex_digit + гейт конкатенаций)

1. **«Один дрейф» оказался тремя независимыми правилами.** Симптом:
   заявленный дрейф — одна строка (`hex_digit` requires, 330 против
   331); первый же прогон нового гейта конкатенаций вскрыл ещё два:
   `Json.kind_of` ensures (Python proven, selfhost runtime) и
   нестабильный порядок строк дампа на `examples/fixed` (коллизия
   модульно-локальных позиций 114:31 из разных модулей). Причины:
   (а) отложенные requires-сайты `pend_n` утекали через границу
   стейтмента/декларации — `refine_cmp`/`refine_neq` (iv_eval другой
   стороны сравнения), оценка `ensures`, `store_array`/
   `note_structlit` аппендят вызовы после последнего `flush_requires`
   стейтмента; хвост оценивался чужим env (эталон считает сайт
   синхронно внутри `_iv`); (б) записи полей struct-литералов не
   отмечались на пути присваивания и во вложенных литералах
   (`JDoc { nodes: [JNode {...}; N] }` — вклад `kind` терялся,
   эталон `_note_field` на каждом вычислении); (в) порядок вставки
   отметок при равных (строка, колонка, вид) не зеркалируется
   (у эталона он зависит от обхода `set` в `_topo_order`).
   Исправлено: flush в самих точках оценки (той же env), глубокий
   `note_structlit` явным стеком (рекурсия замкнула бы граф вызовов,
   правило 1) + вызов из `flow_assign`, тай-брейк вердикта в
   компараторе дампа зеркально в обоих (`verify_dump` + `ck_less`).
   Урок: чинить дрейф решений — только под гейтом полносоставных
   программ; одна видимая строка диффа не значит один дефект.

2. **Отладка паритета: минимальное репро + eprint в зеркале дешевле
   чтения кода.** Симптом: механизм утечки не находился чтением
   Verify*.eat (8K строк). Найден за минуты: бисекция композиции до
   «цикл + assert с вызовом + следующий test-блок», затем одна
   отладочная строка `eprint` в `analyze_test` показала `npend=1` на
   входе второго теста. Сборка отладочного SelfVerify — 9 с. Урок:
   для дрейфа решений сначала минимизировать вход (штатные механизмы
   бисекции: состав конкатенации → файл → конструкция), потом одна
   диагностическая точка в зеркале; в конце — снять отладку и
   пересобрать (дамп сверяется байт-в-байт, stderr свободен).

3. **Ручная правка генерируемого инициализатора сломала стиль.**
   Симптом: первый прогон полного гейта убит: `check_style` — строка
   117 > 100 кодпойнтов в инициализаторе `Verify` (поля добавлены
   руками в существующую строку; `gen_verifymain.py --check` длину
   не проверяет). Урок: после правки зеркала — `check_style.py` и
   `gen_verifymain.py --check` до старта долгих прогонов; новые поля
   инициализатора — отдельной строкой.

## 2026-07-17 · OPTIMIZATIONS_PLAN · кандидат 3 (рычаги lib/ NumParse·StrCmp)

1. **Параллельный замер при живом гейте в одном каталоге.** Симптом:
   кросс-языковой `run.py --only strcmp` запущен фоном одновременно с
   полным гейтом в общем дереве — нарушено собственное правило «один
   прогон на дерево» (WORKFLOW §4). Обошлось: пути в `build/` не
   пересеклись, но тайминги замера шли под нагрузкой всех ядер и
   были выброшены. Исправлено: чистовой замер повторён на тихой
   машине после гейта. Урок: правило «один прогон на каталог»
   касается и bench-контуров, не только make-целей; фоновые запуски —
   строго последовательно с гейтом.

2. **regen_sig на общем дереве захватил чужой WIP.** Симптом:
   SIG DRIFT в гейте с дельтой funcs +5 / structs +1, которую
   собственный дифф (одна функция без смены сигнатуры) дать не мог.
   Причина: посреди этапа в общем дереве появился незакоммиченный
   WIP соседа в lib/U128.eat (Qr64/clz64/divlu) — `git status` был
   чист только на старте; `make regen_sig` вписал его интерфейс в
   снапшот. Исправлено: снапшот откачен к HEAD, regen и полный гейт —
   в worktree-стенде HEAD+свой дифф; в коммит взят снапшот из стенда.
   Урок: проверка соседа по `git status` действительна на момент
   снятия — повторять её перед regen_sig и перед коммитом; снапшоты,
   накрывающие каталог целиком (lib.sig), регенерировать только в
   стенде, если в каталоге возможен чужой WIP.

## 2026-07-17 · OPTIMIZATIONS_PLAN · этап 8.3 (разведка alias-инфы)

1. **Гипотеза-атрибуция дважды записана в отчёты как факт без
   одноручечной проверки.** Симптом: декомпозиция http (85f2a22) и
   дозамер 7.2 (95d5ede) объясняли разрыв EAT/C «поля 8,5-КБ Req не
   промотируются без TBAA» — CROSSLANG, FINDINGS и шапка §8 плана
   несли это как установленную причину. Проверка §8.3 (TBAA-прототип
   с EAT-стороны + `-fno-strict-aliasing` с C-стороны) дала ноль:
   alias-инфа не рычаг, носитель — идиома сканов с нуля в
   lib/Http.eat (найден пересадкой идиомы в C-порт: 116 → 199
   нс/запрос). Исправлено: атрибуция переписана по фактам во всех
   трёх документах. Урок — тот же, что в 7.1, но с обратным знаком:
   не только «убрать X» требует сначала показать X на медленной оси —
   «X отсутствует и потому медленно» тоже проверяется одноручечным
   экспериментом (добавить X дешёвым прототипом или отнять X у
   быстрой стороны флагом) до записи в отчёт; формулировку-гипотезу
   помечать гипотезой.

2. **Стена под живыми соседями лгала, инстр/оп — нет.** Симптом:
   первый полный прогон дал http build 514 нс/запрос против 202 на
   тихой машине (×2,5) — параллельно крутились чужой гейт (eatc
   99 % CPU) и osqueryd (97 %); IPC просел 5,9 → 3,9. При этом
   инстр/оп в грязном и чистом прогонах совпали с точностью ~1 %, и
   внутренние сравнения вариантов остались верны. Исправлено: чистовые
   числа сняты после освобождения машины; вердикт формулировался по
   инстр/оп с подтверждением стеной. Урок: при живых соседях
   первичная метрика сравнения — инстр/оп (счётчик процесса,
   устойчив к конкуренции), стена — только с тихой машины; перед
   чистовым замером — `ps`/load average, как перед коммитом —
   `git status`.

## 2026-07-17 · OPTIMIZATIONS_PLAN · этап 8.1 (span-парс lib/Http.eat)

1. **A/B-замер против стухшего бинарника — ложный вердикт формы.**
   Симптом: после правки формы цикла (0..258 → 0..256 + CRLF-хвост)
   сравнительный замер показал «фикс ничего не дал» (334 нс как у
   прежней формы) — но бинарник «после» не был пересобран, замерялась
   предыдущая версия; дизасм этого же бинарника чуть не ушёл в отчёт
   как дизасм новой формы. Исправлено: пересборка обеих сторон перед
   каждым замером включена в цикл (сборка и замер — одна команда).
   Урок: в A/B-цикле артефакт устаревает при каждой правке —
   собирать и мерить одной связкой, свежесть бинарника проверять по
   времени сборки в выводе; вердикт «правка не помогла» требует той
   же строгости, что и «помогла».

2. **Три формы «состояние в локалах» медленнее базы — гипотезу
   промта опроверг только профиль.** Симптом: буква промта §8.1
   (автомат на локалах, сброс раз на выходе) в трёх вариантах давала
   370/334/331 нс против 302 базы — счётчики (инстр −2 %, циклы
   +16 %, IPC 4,8 → 4,1) показывали «меньше работы, больше стойл»,
   но не место. Причину показала пара sample + objdump: круговорот
   полей на Apple silicon почти бесплатен (store-to-load forwarding),
   а недоказуемая граница s[i] (строковый индекс не связывается с
   len(s) у верификатора) держит trap-ветку в теле каждого цикла по
   строке и запрещает векторизацию. Исправлено: рычагом стали прямые
   диапазоны сканов + одна спан-полоса (124 нс). Урок: у гипотез о
   форме горячего цикла первый инструмент — профиль по функциям и
   дизасм горячего блока, счётчики целого процесса — только рамка;
   «оптимизация» без профиля может возить те же байты дороже.

## 2026-07-17 · OPTIMIZATIONS_PLAN · §7.6 кандидат 2, этап 1 (инлайн-хинты)

1. **Эвристика без ограды роста повесила пасс на большом модуле.**
   Симптом: свип порога «малости» на четырёх бенчах прошёл чисто
   (микромодули, 20–70 функций), но первая же проверка на SelfIr
   (722 функции) дала бинарник ×6,4 уже при T=160, а llvmlite-пасс
   при T=384 не завершился за 10+ минут — процесс пришлось убить.
   Причина: у селфхост-модулей есть хвост малых функций с сотнями
   call-site'ов (`RtStr.write` — 385, `Ir.lb_lit` — 217); безусловный
   `alwaysinline` размножает их каскадно — статический рост ×3
   превращается в комбинаторный. Исправлено: в эвристику добавлен
   бюджет роста `размер × (call-site'ов − 1) ≤ B` (B=768 —
   зафиксирован замером: выигрыши бенчей целы, SelfIr нейтрален).
   Урок: константы инлайн-эвристики нельзя фиксировать по
   микробенчам — до вердикта обязателен замер на самом большом
   модуле репозитория (selfhost-компилятор), и ограничитель
   массовости (сайты × размер) в эвристике обязателен, а не
   опционален.

## 2026-07-17 · OPTIMIZATIONS_PLAN · §9 модульный накопитель верификатора

1. **Правки Edit ушли в главное дерево вместо worktree.** Симптом:
   `selfhost/VerifyFlow.eat` собирался и curated-гейт был зелёным, но
   паритет `SelfVerify == eatc verify` на TodoBench краснел — selfhost
   не несёт инвариант, Python несёт. Причина: Edit-правки зеркала
   применились по абсолютному пути главного дерева (Read зеркала был
   сделан до входа в worktree), а гейт гнался в worktree — там зеркала
   не было, `SelfVerify` собран без `mod_carry`. Исправлено: файл
   скопирован в worktree, дальше правился он. Урок: после входа в
   worktree сверять `git diff --stat` в worktree после каждой правки
   зеркала — что Edit пишет в worktree, а не в главное дерево с WIP.

2. **Многострочное условие `if` без скобок — parse error.** Симптом:
   `ожидалось '{', найдено '\n'` на переносе внутри `if a and b\n and
   c {`. Причина: перенос строки в условии `if` допустим только внутри
   скобок (образец — `elif (…\n …)` в `er_feed`). Исправлено: условие
   обёрнуто в `(...)`. Урок: многострочные булевы условия в selfhost —
   всегда в скобках.

3. **Звуковость требует ОБЕИХ ног, и негатив это доказал первым.**
   Ловушка, отвергнутая до кода: брать K из `_iv(делитель)` в
   предцикловом env — тогда изменяемый по витку делитель
   (`K2 = i; sum = x % K2`) дал бы ложный интервал. Зафиксировано
   ограничением «K — целый **литерал**» (не путь) на обеих сторонах.
   Урок: у интервального инварианта проверять «интервал определения не
   зависит от входного значения» буквально — литерал даёт это даром,
   любой путь-операнд требует доказательства неизменности; парный
   негативный кейс (`acc = acc + x` без `%`, ожидание 0/N) писать
   вместе с позитивным, до реализации.
