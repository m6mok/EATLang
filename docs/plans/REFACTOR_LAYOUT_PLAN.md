# План: раскладка selfhost по папкам + аксессоры пула + дедуп lib

- **Статус:** 🚧 В ПРОЦЕССЕ (этап 1 ✅ раскладка папок; этап 2 ✅
  РАЗБЛОКИРОВАН 2026-07-18 — [LIMITS_PLAN](LIMITS_PLAN.md) этап 1 поднял
  потолок функций до 2048: `SelfIrOpt` 1017/2048, +19 аксессоров → 1036,
  запас ~1012; готов к работе; **этапы 3–4 ✅ 2026-07-18** — дедуп lib
  выполнен, гейт+bench зелёные)
- **Роль:** активный план — читаемость и структура репозитория
  (продолжение [REFACTOR_SELFHOST_PLAN](REFACTOR_SELFHOST_PLAN.md))
- **Рабочие поверхности:** этап 1 — **✅ свободны** (перенос закоммичен);
  этап 2 — `selfhost/check/Check*.eat`, `selfhost/ir/Ir*.eat`;
  этапы 3–4 — `lib/http/Http.eat`, `lib/json/Json.eat` (**✅ свободны**:
  дедуп выполнен и закоммичен 2026-07-18)
- **Правки языка/грамматики:** нет — только перенос файлов, добавление
  методов-аксессоров (по образцу существующих) и импорты lib
- **Новые аксиомы ОС (SPEC §7):** нет
- **Зеркало в selfhost:** не требуется — этапы 1–2 правят только
  исходники `selfhost/` (сами вход фикспойнта: эталон Python и
  self-hosted видят одни исходники → паритет держится автоматически);
  этапы 3–4 правят только `lib/` (в Python не зеркалится)
- **Опирается на:** [REFACTOR_SELFHOST_PLAN](REFACTOR_SELFHOST_PLAN.md) ✅
  (распил на `extend`-файлы — предпосылка группировки),
  [MODULES_PLAN](MODULES_PLAN.md) ✅ (склейка списком файлов, разрешение
  `import`), существующие `lib/Hex.eat`, `lib/Ascii.eat`
- **Блокеры:** ✅ СНЯТ 2026-07-18. Был — **потолок функций 1024**:
  `SelfIrOpt` на **1017/1024**, +19 аксессоров → 1036 > 1024.
  [LIMITS_PLAN](LIMITS_PLAN.md) этап 1 поднял потолок до **2048**
  (пулы function-индексируемых сущностей расшиты, кодировка графа
  сдвинута, паритет байт-в-байт, гейт зелёный). Теперь `SelfIrOpt`
  1017/**2048**, +19 → 1036, запас ~1012 — этап 2 свободен. Плюс перф-зависимость от инлайнинга (кандидат 2
  «инлайн-хинты» уже доставлен, [OPTIMIZATIONS_PLAN](OPTIMIZATIONS_PLAN.md)).
  Этапы 3–4 — заняты `lib/http/Http.eat`/`lib/json/Json.eat`
  (параллельные правки; вдобавок `lib/` переехал в подпапки — пути в
  §6.3/§8 обновлены)
- **Блокирует:** —

## 1. Итог одной строкой

Разложить 28 файлов `selfhost/` по 5 папкам-фазам (`lex/ parse/ check/
ir/ verify/`, `Rt.eat` в корне), свернуть ~800 сырых обращений к пулу
AST `self.p.nX[i / 65536][i % 65536]` в методы-аксессоры (образец уже
живёт в `Verify.eat`) и убрать три локальных дубля в `lib/` (`Http.hexv`,
разбор цифр в `Json`/`Http`) переиспользованием `lib/Hex`, `lib/Ascii`.

## 2. Почему язык к этому приспособлен

Ничего в семантике не меняется — это гигиена исходника, продолжение
линии REFACTOR_SELFHOST (потолок строк, конструкторы фаз, `extend`).
Папки материализуют группировку по фазам, которую уже задают префиксы
имён (`Check*`, `Ir*`, `Verify*`). Аксессоры пула — тот же приём, что
`Verify.eat` применил к своим обращениям: доменный метод вместо ручной
двухуровневой индексации страничного массива `[[u32; 65536]; 2]`.
Дедуп `lib/` — прямое следствие модульной библиотеки: то, что уже есть
в `Hex`/`Ascii`, не должно жить копией в `Json`/`Http`.

## 3. Диагноз (2026-07-17)

| Симптом | Причина | Лекарство |
| --- | --- | --- |
| 28 файлов плоским списком в `selfhost/` | распил Check/Ir/Verify на `extend`-файлы (REFACTOR_SELFHOST этап 4) без каталогов | 5 папок-фаз, `Rt.eat` в корне (этап 1) |
| ~800 обращений `self.p.nX[i / 65536][i % 65536]` | ручная индексация страничного пула; аксессоры внедрены только в фазе Verify | методы-аксессоры на `Check` и `Ir` по образцу `Verify.eat:561-622` (этап 2) |
| `lib/Http.eat:634` `hexv` | локальная копия `lib/Hex.eat hex_val` (Hex не импортирован) | импорт + удаление копии (этап 3) |
| сырой разбор цифр `b >= 48 and b <= 57` / `b - 48` в `Json`/`Http` (~7 мест) | `lib/Ascii` не импортирован | `is_digit`/`digit_value` из `lib/Ascii` (этап 4) |

Масштаб идиомы пула (`grep '/ 65536]['`): всего **878** вхождений —
Check-группа ~541, Ir-группа ~265, Parser/Lexer ~145, Verify ~13 (уже
свёрнуто). Своднo к вызовам подлежат ~806 обращений Check+Ir.

## 4. Числа (пределы)

- Раскладка: **5 папок-фаз** + `Rt.eat` в корне. Новых языковых
  пределов нет — склейка идёт списком файлов (Makefile), не по путям.
- Аксессоры: `requires n < 131072` на каждый (граница пула
  `2 × 65536` — как в `Verify.eat`). ~11 методов × 2 struct (`Check`,
  `Ir`); проверить запас по потолку методов struct §6 SPEC (в `Verify`
  прецедент уже есть).
- Дедуп lib: ноль новых пределов — переиспользование существующих
  тотальных функций.

## 5. Аксиомы ОС

Не нужны.

## 6. Архитектура / механизм

### 6.1. Раскладка (этап 1)

`Rt.eat` остаётся в `selfhost/` (нулевой модуль рантайма; на него
жёстко смотрит `src/eatc/driver.py:27` — не двигаем, продакшн-драйвер
не трогаем). Остальное — по фазам, каждый `*Main.eat` в папке своей
фазы:

```text
selfhost/
  Rt.eat
  lex/     Tok  Lexer  LexMain
  parse/   Ast  Parser  ParserExpr  ParseMain
  check/   Check  CheckConst  CheckBody  CheckDump  CheckFold
           SigMain  TypedMain
  ir/      Ir  IrEmit  IrExpr  IrStmt  IrMain  IrCodesMain  IrOptMain
  verify/  Verify  VerifyExpr  VerifyRel  VerifyFlow  VerifyClamp
           VerifyDump  VerifyMain
```

Факт (2026-07-18): в `verify/` семь файлов — `VerifyClamp.eat`
появился в фазе Verify после написания плана; итого **28** файлов в
папках + `Rt.eat` в корне (не 27, как в раннем черновике §1).

`SigMain`/`TypedMain` — точки входа фазы Check (эмитят sig/typed из
Check), поэтому в `check/`. Перенос — `git mv` (сохраняет историю).

**Обязательные правки (иначе сборка не находит файлы):**
1. `Makefile` — переменные `RT`, `SELFHOST_LEXER|PARSER|SIG|TYPED|IR|
   IR_CODES|IR_OPT|VERIFY` (строки 13, 207–247). Правила гоняют через
   переменные — их не трогать.
2. `tests/gate/verify_all_file.sh:8` — жёстко зашитый фильтр
   `selfhost/Verify.eat|selfhost/VerifyMain.eat` → новые пути
   `selfhost/verify/Verify.eat|selfhost/verify/VerifyMain.eat`
   (**критично**: иначе верификатор попробует верифицировать сам себя).
3. `tests/bench/bench.py` — списки файлов (строки 36, 460–464,
   547–561).
4. `tools/gen_verifymain.py` — пути `Verify.eat`/`Check.eat`/
   `VerifyMain.eat` (строки 24–27).

Устойчиво к переносу (рекурсивные `find`/`rglob` — править НЕ надо):
`Makefile` `find … selfhost …` (313, 361, 390), `tests/check_style.py`
`rglob` + `parts[0] == "selfhost"` (потолок 2500 строк продолжит
действовать на подпапки), `tests/gate/selfhost_file.sh`/`opt_file.sh`
(фильтров по путям нет). Импорт-блоков `from "selfhost/..."` нигде нет.

**Доккарты (сборку не ломают, обновить синхронно):** карта файлов
`docs/plans/SELFHOST.md` (таблица + текст), `AGENTS.md:79`, `README.md`,
`docs/MODIFYING.md`, `docs/PROCESSES.md`, `docs/POWER_OF_10.md`,
`docs/GUIDE.md`, `tests/bench/README.md`; строчные ссылки вида
`selfhost/Ir.eat:1468` в планах (STR_MODEL, NONE_TYPE, COMPTIME,
SELFHOST_VERIFIER, VERIFICATION, OPTIMIZATIONS и др.) — устаревают, но
не load-bearing; комментарии-ссылки в `src/eatc/*.py` — пояснительные.

### 6.2. Аксессоры пула (этап 2)

Образец — `selfhost/Verify.eat:561-622`:

```eat
func nk(let self, n: u32) -> u32
    requires n < 131072
    ensures true
{ return self.c.p.nk[n / 65536][n % 65536] }
```

- На `Check` (держит парсер полем `p`): методы читают `self.p.nX[…]`;
  имя метода `nX` не конфликтует (пул — на поле `p`, не на `Check`).
  Вызовы `self.p.nX[i / 65536][i % 65536]` → `self.nX(i)`.
- На `Ir` (держит Check полем `c`, парсер — `self.c.p`): методы читают
  `self.c.p.nX[…]` — идентично `Verify`. Вызовы `self.c.p.nX[…]` →
  `self.nX(i)`.
- Набор полей пула: `nk nl nc nss nsl nx ny nxh nfc nlc nns` (объявление
  `Parser.eat:33-44`) — вводить аксессоры по факту обращений каждой
  группы (в Ir встречаются не все).
- Методы кладутся в `extend Check { … }` / `extend Ir { … }` рядом с
  инфраструктурой фазы (или в базовый файл фазы).

**Перф-ворота (обязательны).** Check и Ir — горячий путь компилятора.
Выигрыш безопасен только при инлайнинге аксессоров; иначе — регресс.
`make bench` до/после, критерий — без регрессии сверх шума. Если
аксессоры не промотируются — либо инлайн-хинты (кандидат 2,
[OPTIMIZATIONS_PLAN](OPTIMIZATIONS_PLAN.md)), либо этап откатывается.
Вывод компилятора (IR/дампы тестовых программ) не меняется — рефактор
внутренний; но весь гейт готовности перегоняется (исходники selfhost —
вход фикспойнта и самоприменения).

### 6.3. Дедуп lib (этапы 3–4)

Пути lib/ обновлены (переезд в подпапки, 2026-07-18): `lib/http/Http.eat`,
`lib/json/Json.eat`, `lib/fmt/Hex.eat`, `lib/fmt/Ascii.eat`. Строки
(`:634` и т.п.) до старта этапов 3–4 пересверить — файлы дрейфовали.

- **Этап 3.** `lib/http/Http.eat`: `import { hex_val } from
  "lib/fmt/Hex.eat"` (Http уже импортирует из `Fmt` — добавить связку),
  удалить метод `hexv` (не экспортируется — `lib.sig` не меняется),
  заменить `self.hexv(b)` на `hex_val(b)`.
- **Этап 4.** `lib/json/Json.eat`, `lib/http/Http.eat`: `import {
  is_digit, digit_value } from "lib/fmt/Ascii.eat"`, заменить сырые
  `b >= 48 and b <= 57` → `is_digit(b)` и `b - 48` → `digit_value(b)`.
  Сверить семантику границ `digit_value` с локальной. Горячий
  парс-путь — `make bench` (http, json) без регрессии; при регрессе
  оставить инлайн в горячих точках.

## 7. Что реализуем НЕ

- **Папки в `lib/`** — файлы `lib/` подключаются `import from "lib/…"`;
  их канонические пути вшиваются в `#module` и в снапшот
  `tests/sig/lib.sig`, перенос сломал бы паритет и все примеры. Вне
  рамок.
- **Папки в `src/eatc/`** (Python) — решение пользователя: не зеркалим
  раскладку, `src/eatc/` остаётся плоским (правка Python-импортов — не
  оправданный churn ради косметики).
- **Свёртка visitor-машин выражений** (Check/Ir/Verify) — дублирование
  структурно необходимо: тела расходятся (проверка типов / сериализация
  IR / накопление фактов), общий обход не вынести без first-class
  функций и изменил бы вывод IR. Разведка подтвердила — не трогаем.
- **Централизация таблиц «код→имя»** (Tok/Ast) — уже централизованы,
  вывод дампов байт-в-байт; массив вместо `elif` требует кучи.
- **Вынос строк ошибок в константы** — повторы локальны (в основном
  `CheckBody`), выигрыш мал, а тексты зеркалятся в Python байт-в-байт.
- **`Rt.eat` в подпапку** — нулевой модуль, жёсткая ссылка драйвера.

## 8. Этапы

Порядок — по возрастанию риска и по свободе поверхностей.

- **Этап 1 — папки. ✅ (2026-07-18).** `git mv` 28 файлов по §6.1
  (`Rt.eat` в корне), правка путей: `Makefile` (переменные
  `SELFHOST_*`), `tests/gate/verify_all_file.sh` (жёсткий фильтр
  `verify/`), `tests/bench/bench.py`, `tools/gen_verifymain.py`,
  доккарты (`SELFHOST.md`, `AGENTS.md`, `README.md`, `MODIFYING.md`,
  `POWER_OF_10.md`, `GUIDE.md`). Полный гейт готовности (10 целей) и
  `make bench` — зелёные (проверено в чистом worktree мимо живого
  соседа: `src/eatc/` был занят параллельной работой). Попутно
  устранён **предсуществующий дрейф** `bench.py`: списки-зеркала
  Makefile не содержали `ParserExpr.eat` (появился в REFACTOR_SELFHOST,
  Makefile обновили — зеркало bench нет), из-за чего секции
  `compiler`/`size` падали на `step_expr`; добавлен во все три списка,
  вход `compiler` вырос 19→20 модулей, дампы байт-в-байт с эталоном.
- **Этап 2 — аксессоры пула Check+Ir. ✅ РАЗБЛОКИРОВАН (2026-07-18)** —
  [LIMITS_PLAN](LIMITS_PLAN.md) этап 1 поднял потолок функций до 2048.
  `SelfIrOpt` теперь 1017/**2048**, +19 → 1036, запас ~1012; готов к
  реализации отдельной задачей.
  Замер перед стартом: пул-обращения `self.p.nX` в `check/` — 10 полей
  (nfc 141, nss 107, nsl 106, nns 92, nk 56, nx 29, ny 18, nl 7, nc 7,
  nlc 4 ≈ 567) и `self.c.p.nX` в `ir/` — 9 полей (nfc 59, nss 40, nsl
  39, nns 25, nl 24, nk 24, nc 24, nx 5, ny 3 ≈ 243); поля `self.nty/
  nrt/nfm/nfs/nvm` (и `self.c.nty/nvm/nfs/nfm` в Ir) — **не** пул
  парсера, не сворачиваются. Итого набор — **19 методов** (Check 10 +
  Ir 9). **Потолок:** `SelfIrOpt` = **1017/1024** (единственная
  программа с фазами Check+Ir+Verify разом), +19 → 1036 > 1024 →
  `verify_selfhost_opt` красный. Прочие с запасом: SelfSig/Typed/
  IrCodes ≈ 728, SelfVerify 744. Урезать до 7 свободных слотов
  (≈ 1023/1024) — нулевой запас, риск для соседа/будущих правок; решение
  пользователя 2026-07-18 — **отложить** до [LIMITS_PLAN](LIMITS_PLAN.md)
  (конфигурируемый потолок). После снятия потолка: замена скриптовой
  свёрткой с машинной сверкой (как этап 1 REFACTOR_SELFHOST), готовность
  — полный гейт зелёный + **`make bench` без регрессии** (перф-ворота
  §6.2, аксессоры промотируются инлайн-хинтами). Образец — блок методов
  `selfhost/verify/Verify.eat:578` (`func nk(let self, n: u32) -> u32
  requires n < 131072 { return self.c.p.nk[n / 65536][n % 65536] }`).
- **Этап 3 — `Http.hexv` → `Hex.hex_val`. ✅ (2026-07-18).**
  `lib/http/Http.eat`: `import { hex_val } from "lib/fmt/Hex.eat"`,
  удалён метод `hexv` (не экспортировался), `self.hexv(b)` → `hex_val(b)`
  в `chunk_size`. Гейт зелёный.
- **Этап 4 — цифры Json/Http → `Ascii`. ✅ (2026-07-18).**
  `import { is_digit, digit_value } from "lib/fmt/Ascii.eat"` в
  `lib/json/Json.eat` и `lib/http/Http.eat`; заменены `b >= 48 and
  b <= 57` → `is_digit(b)` (и негации `b < 48 or b > 57` →
  `not is_digit(b)`), `b - 48` → `digit_value(b)` (7 мест: Http parse_len,
  Json целая/дробная части + мембершип-тесты next/хвост). Границы сверены:
  `digit_value` requires `b∈[48,57]` доказывается через `ensures` у
  `is_digit` (верификатор narrow'ит по границе). `make verify`,
  `verify_selfhost_verify_all` зелёные; `bench_crosslang` http/todo —
  инстр/оп байт-идентичны эталону (2530.3 vs 2530.5; 1096.7 vs 1096.8):
  хелперы полностью инлайнятся, регресса нет.

**Заметка по `lib.sig` (этап 3–4):** снапшот `tests/sig/lib.sig`
**сдвинулся** (вопреки раннему ожиданию «не дрогнет»): формат сигнатуры
пишет строки `import` и позиции символов, а обе поверхности изменились
(+3 импорта Http, +2 Json, сдвиг номеров строк, `funcs` 253→252 —
приватный `hexv` убран). Экспортируемый контракт **тождественен** (диф
без строк-импортов/номеров/`stats` пуст). Пересобрано `make regen_sig`
(документированный путь «осознанно?»); `verify_sig` зелёный.

Этап 1 независим; 2 независим от 1 (но удобнее после — стабильные
пути); 3–4 независимы друг от друга, ждут своих файлов.

## 9. Паритет

- Этапы 1–2 меняют только `selfhost/` (+ сборку/доки): зеркалить
  нечего — эталон Python и self-hosted компилируют одни исходники, а
  весь гейт готовности (включая `verify_bootstrap`,
  `verify_selfhost_verify_all`) перегоняется целиком.
- Этапы 3–4 меняют только `lib/` — в Python не зеркалится.
  **Факт (2026-07-18):** `tests/sig/lib.sig` **сдвинулся** механически —
  формат снапшота включает строки `import` и позиции символов (обе
  изменились: +5 импортов, сдвиг номеров, `funcs` −1 из-за убранного
  приватного `hexv`). Экспортируемый контракт тождественен; снапшот
  пересобран `make regen_sig`, `verify_sig` зелёный.

## 10. Открытые вопросы

Решены пользователем 2026-07-17:
- **Раскладка** — вариант «5 фаз, `*Main.eat` внутри своей фазы» (§6.1).
- **Глубина** — все три рычага (аксессоры + оба дедупа lib).
- **`src/eatc/`** — не зеркалим раскладку папками (§7).

## 11. Инварианты

- Power of 10 не трогаются: перенос файлов и аксессоры —
  синтаксическая гигиена, семантика тождественна; ни кучи, ни рекурсии,
  ни новых обходов. `requires n < 131072` на аксессорах — та же
  доказуемая граница, что у ручной индексации.
- Все дампы/IR: Python-эталон == self-hosted байт-в-байт на каждом
  этапе; частично зелёное не коммитится.
- Производительность selfhost и lib не деградирует: `make bench`
  (+`bench_crosslang` для lib) сверяется на этапах 2–4.
- Раскладка полей каждой структуры остаётся в одном блоке `struct`
  (аксессоры — методы, не поля).

## 12. Затронутые поверхности

`Makefile`, `tests/gate/verify_all_file.sh`, `tests/bench/bench.py`,
`tools/gen_verifymain.py`, все `selfhost/*.eat` (перенос),
`selfhost/Check*.eat`/`Ir*.eat` (аксессоры), `lib/Http.eat`,
`lib/Json.eat` (дедуп); доккарты — `docs/plans/SELFHOST.md`, `AGENTS.md`,
`README.md`, `docs/MODIFYING.md`, `docs/PROCESSES.md`,
`docs/POWER_OF_10.md`, `docs/GUIDE.md`, `tests/bench/README.md`.
SPEC/editor/vscode — не затрагиваются (язык не меняется).

## 13. Определение готовности

Полный гейт AGENTS.md §3 (`check verify verify_suite verify_selfhost
verify_bootstrap verify_trapcodes verify_selfhost_opt verify_sig
verify_selfhost_verify verify_selfhost_verify_all`) зелёный целиком +
`make bench` и `make bench_crosslang` без регрессии — на каждом этапе.
