# Треки работ: производительность, микроконтроллеры, язык

Живой список направлений после спринта оптимизации 2026-07-13
(10 коммитов, детальные замеры и уроки — итерации 4–9 в
[tests/bench/FINDINGS.md](../tests/bench/FINDINGS.md)). Каждый трек —
кандидат на отдельный этап-коммит; изменения синтаксиса/семантики/
встроенных — только с решением пользователя
([MODIFYING.md](MODIFYING.md), инвариант 4).

## Достигнутая база (не переоткрывать)

Вход всех замеров — компилятор компилирует сам себя
(≈376 КБ, ≈91 К токенов), секция `compiler` в `make bench`:

| Фаза | Утро 13.07 | После спринта | К Python-эталону |
| --- | --- | --- | --- |
| lex | 0.05 с | 0.02 с | ×13 |
| parse | 0.06 с | 0.02 с | ×16 |
| sig | 0.05 с | 0.01 с | ×21 |
| typed | 0.54 с | 0.08 с | ×4.8 |
| ir (весь конвейер) | 0.98 с | **0.13 с** | **×9.0** |

Главные уроки спринта: (1) профилировать нативный бинарник
(`sample`, символы `eat_*` читаемы), (2) два крупнейших выигрыша
были не в алгоритмах — блокировки stdio и вывод через интерполяцию,
(3) LLVM-атрибуты нейтральны: их выгоды проект уже собрал через
`unreachable`-после-trap и снятие доказанных проверок,
(4) `fwrite` коротких спанов медленнее `putc_unlocked` — в шиме
гибрид с порогом 32 байта.

## Трек 1. Строковая модель `str<256>` (слой 4, решение по SPEC)

**Сделано 2026-07-13**: направление «read-only по ссылке» реализовано
по плану [STR_MODEL_PLAN.md](STR_MODEL_PLAN.md) (решения пользователя
в §6 там же): defensive copy у вызываемого снята, перекрытие с
`var self`-получателем закрывает caller-копия, атрибуты
`noalias nocapture nofree nonnull [readonly]`; SPEC §5.4, пример
`examples/alias/`, уроки — десятая итерация FINDINGS. Направления
«копия по длине» (по замеру не нужна) и `str<N>` — отложены.

**Проблема (исходная).** Строка — фиксированные 260 байт (`{i32, [256 x i8]}`),
передаётся по значению: каждое связывание — memcpy 260 байт, даже
для трёхбайтового имени. Это доминирующая цена строкового кода
(StrBench ~88 M оп/с против 820 M у арифметики; получатель без
`var self` копирует агрегат при каждом вызове — `Rt.write(self)`
копирует 260 байт, чтобы вывести 5).

**Направления дизайна** (обсудить до кода, у каждого свои trade-off):

- копия по фактической длине (`memcpy ln + 4` вместо 260) — семантика
  не меняется, меняются оба кодогена; выигрыш ограничен: длина
  известна только в рантайме, LLVM теряет константный размер;
- read-only параметры по ссылке (передача `str` в функцию без
  `var` — без копии; язык уже передаёт указатель, копию делает
  вызываемый) — самый большой выигрыш, но надо доказать отсутствие
  алиасинг-ловушек (`s.append_str(s)`) и зафиксировать семантику
  в SPEC §5;
- `str<N>` с настоящей параметризацией ёмкости (сейчас рантайм
  фиксирован на 256) — отдельная большая тема, тянет за собой
  layout рантайма.

**Объём.** SPEC §5 + оба кодогена + Rt.eat + интерпретатор +
verifier; полный паритет и фикспойнт. Самый инвазивный трек.

## Трек 2. Микроконтроллеры: размер, trap-строки, cross-compile

Язык уже идеален для МК по построению (статическая память, точная
верхняя граница стека §8, нет кучи). Не хватает:

- **политика trap-строк — сделано 2026-07-13**: режим `--trap-codes`
  (оба компилятора: флаг eatc / `SelfIrCodes` из `IrCodesMain.eat`,
  паритет — `make verify_trapcodes`); `eat_trap_code(u32)` в шиме,
  таблица `; trap <код>: <сообщение>` в хвосте .ll. Числа: Mos6502
  `__const` 4.8 → 0.1 КБ (−98 %), флеш-метрика −30 %; SelfIr −64 КБ
  (тринадцатая итерация FINDINGS);
- **замер размеров секций — сделано 2026-07-13**: секция `size` в
  bench (бинарник, `__text`/`__const`, строковые/trap-глобалы,
  канон .ll; одиннадцатая итерация FINDINGS);
- **cross-compile — сделано 2026-07-13**: mos6502 собран под
  Cortex-M3 (thumbv7m) и проходит `make verify_arm` — вывод в QEMU
  mps2-an385 байт-в-байт равен интерпретатору. Шим аксиом поверх
  CMSDK UART — [mcu/runtime_mcu.c](../mcu/runtime_mcu.c), вход
  прошивается в образ (у UART нет EOF), exit/trap — полухостинг;
  сборка clang'ом из канонического `.ll` + `ld.lld`, LTO на коде
  программы (кандидат из трека 4 встроен: −32 % флеша, линковка
  мгновенная). Флеш 7.1 КБ (хост-бинарник — 69.7 КБ); детали и
  числа — [mcu/README.md](../mcu/README.md), семнадцатая итерация
  FINDINGS;
- стек по §8: mos6502 ≤ 262 594 Б (Cpu в кадре main) — в 4 МБ RAM
  платы влезает с запасом ×15; отчёт §8 — готовая нижняя граница
  выбора чипа;
- **этап 2 — «уровень реального проекта», план и решения
  пользователя: [MCU_PLAN.md](MCU_PLAN.md)** (2026-07-13): extern-
  функции (+ u16, hex-литералы, `~x`; u64 — следом), структура
  портов mcu/boards/, QEMU-сверка четырёх плат, периферия через
  ISR-в-шиме + суперцикл, флагманский пример. Живой UART-ввод — через
  extern, протокол конца потока для аксиомы не нужен.

## Трек 3. Верификатор: добивать «100 % доказанного»

- **каталог собран 2026-07-13**: инструмент
  `tests/verify_catalog.py`, карта остатков и направления лечения —
  [VERIFICATION_PLAN.md](VERIFICATION_PLAN.md) «Каталог остатков»
  (на bootstrap: 4624/6110; главные классы — интервалы значений из
  пулов → bounds 959, инварианты полей struct → assert/overflow);
- интервалы через поля struct (сейчас факты о полях умирают чаще,
  чем нужно; `var self`-вызов убивает всё состояние получателя);
- связка «ширина маски ↔ величина сдвига» (из
  [CRYPTO_SUITE_PLAN.md](CRYPTO_SUITE_PLAN.md) §1: общий `rotl`
  оставляет одну проверку — верификатор не связывает `32-n` с `n`);
- доказанные факты уже идут в оптимизатор (`nsw`/`nuw` в build-пути) —
  чем больше доказано, тем быстрее и меньше код.

## Трек 4. Хвосты производительности компилятора (низкий приоритет)

По остаточному профилю SelfIr (0.13 с) и FINDINGS:

- `lb_tp`/копии в банкованные пулы: скалярный масками-доказанный
  цикл; попытка аффинной по-банковой формы, которую LLVM распознаёт
  как memcpy, не делалась — потенциал ~десятки мс;
- `g_hash` считается по содержимому при каждом commit — можно
  инкрементально при записи в пул;
- экранированные спаны дампов (`write_span_esc`, значения токенов) —
  посимвольные; батчить между escape-символами;
- LTO — **замерено 2026-07-13** (шестнадцатая итерация FINDINGS):
  −10 % времени самокомпиляции, −29 % размера, цена — 2.75 с
  линковки; в build-путь не встраиваем, кандидат для МК-сборок;
- F7 (FINDINGS): RSS emit_ir у Python-эталона — низкий приоритет;
- F2 — **закрыт целиком 2026-07-13**: `eprint` в Rt.eat, все шесть
  fail-путей пишут в stderr, stdout при ошибке пуст, rc=1.

## Трек 5. Эргономика языка для крипто/МК (решения по SPEC)

Находки [CRYPTO_SUITE_PLAN.md](CRYPTO_SUITE_PLAN.md) §0 — каждая
требует решения пользователя:

- hex-литералы — **сделано 2026-07-13** (этап МК, [MCU_PLAN.md](MCU_PLAN.md)
  §1.10): `0x…` до 8 цифр, нормализация в лексере, SPEC §4;
- битовое NOT `~x` — **сделано 2026-07-13** (там же): оп-код 21,
  только беззнаковые, `xor -1` в IR, точный интервал верификатора;
- беззнаковое вычитание с заворотом / отрицание mod 2³²;
- тип шире u32 (u64) — SHA-512, длины в битах;
- сама криптосюита `examples/crypto/` — готовый план в файле,
  компилятор не трогает.

## Порядок по умолчанию

1 (строки — **сделан**, см. выше) → 2 (МК: trap-строки + размеры) →
3 (верификатор по каталогу остатков) → 5 (по мере надобности
крипто) → 4 (по остаточному профилю). После каждого этапа:
`make check verify verify_suite verify_selfhost verify_bootstrap`
плюс секция `compiler` в bench, до/после — в FINDINGS.
