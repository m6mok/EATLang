# EATLang

Учебно-исследовательский язык программирования. Текущая версия: **0.0.1**
(черновик спецификации + эталонные примеры).

## Описание

- **Компилируемый** — через LLVM (llvmlite → LLVM IR → clang)

- **Статически типизируемый** — без неявных преобразований

- **Тотальный** — каждая программа доказуемо завершается; память и глубина стека
  известны на этапе компиляции

- **10 правил NASA (Power of 10) зашиты в сам язык** — нарушить их синтаксически
  невозможно: нет `while` без границы, нет рекурсии, нет кучи, нет указателей,
  контракты `requires`/`ensures` обязательны, результат вызова нельзя отбросить молча

Документация:

- [Руководство по языку](docs/GUIDE.md) — учебник: синтаксис, контракты,
  верификация, каталог «выстрелов в ногу», которые не скомпилируются
- [SPEC.md](SPEC.md) — формальная спецификация: грамматика EBNF,
  семантика, пределы
- [SELFHOST.md](docs/SELFHOST.md) — self-hosting: компилятор EATLang
  на EATLang, дорожная карта и дифференциальная сверка с эталоном

**Исследовательский вопрос:** каков минимальный набор конструкций языка, при котором
нарушить Power of 10 невозможно, а писать на языке всё ещё удобно?
Долгосрочная цель — self-hosting компилятор.

## Как выглядит язык

```text
func max(a: i32, b: i32) -> i32
    requires true
    ensures result >= a and result >= b
{
    if a > b {
        return a
    }
    return b
}

test max {
    assert max(10, 20) == 20
}

func main() {
    print("The maximum is {max(10, 20)}")
}
```

Эталонные примеры — в [examples/](examples/): `hello_world`, `math`, `functions`,
`if_statement`, `iterator`, `struct` и витрина всех конструкций `all`.

## Запуск

```sh
make check           # компиляция всех примеров: парсинг, Power of 10,
                     # типы, исполнение test-блоков
make run_all         # запустить пример интерпретатором (run_math, ...)
make verify          # нативные бинарники + сверка с интерпретатором
make verify_suite    # регрессионный набор верификатора (25 кейсов)
make verify_selfhost # self-hosted лексер и парсер против эталона на всех .eat
```

Требуются [uv](https://docs.astral.sh/uv/) и clang (для линковки бинарников).

## Статус

- [x] Спецификация: синтаксис, семантика, пределы ([SPEC.md](SPEC.md))
- [x] Эталонные примеры в новом синтаксисе
- [x] Лексер и парсер (`src/eatc/`, `make check`) со структурными
  проверками: правило 4 (≤ 60 statements), правило 5 (обязательные
  контракты), правило 6 (нет глобалов), пределы §6
- [x] Тайпчекер: строгие типы без неявных преобразований, правило 7
  (результат нельзя отбросить, match исчерпывающий), правило 10
  (неиспользуемые переменные, недостижимый код)
- [x] Семантика Power of 10: правило 1 (DAG вызовов, глубина стека —
  артефакт компиляции), правило 2 (константные границы for, loop
  только в main)
- [x] Исполнение: интерпретатор — эталон семантики; контракты и assert
  как trap с координатами, `test`-блоки исполняются при `make check`,
  переполнение/деление на ноль/выход за границы — trap
- [x] LLVM-кодогенерация: llvmlite → объектный файл → clang + C-рантайм
  → нативный бинарник (`make verify`: вывод бинарника совпадает с
  интерпретатором на всех примерах; все trap'ы работают и в нативном
  коде; ни одного malloc)
- [x] Отчёт компиляции §8: `eatc build` печатает верхнюю границу стека
  худшей цепочки вызовов (ABI-размеры кадров из LLVM × DAG), размер
  статических данных и кадр каждой функции
- [x] Статическая верификация (этап 1): интервальный анализ целых по
  DAG вызовов — доказывает контракты, переполнения, деление на ноль,
  границы массивов; доказанные проверки удаляются из бинарника, отчёт
  `eatc build` показывает счёт «доказано N из M»
- [x] Верификация (этап 2): реляционный анализ — факты `a < b` между
  переменными с транзитивным замыканием, символический `result` на
  return (доказывает `ensures result >= a` у max), параметрические
  summary (интервал аргумента проходит сквозь вызов — доказано деление
  в `Stats.mean`)
- [x] Верификация (этап 3): ускорение линейных циклов (`p = p ± const`
  получает точный интервал `v0 + k·d` — Iterator компилируется без
  trap'ов, `assert sum == 700` после цикла доказан) и структурное
  равенство выражений (`ensures result == x * 2` доказан)
- [x] Верификация (этап 4): регрессионный набор из 18 кейсов
  (`make verify_suite`, [план](docs/VERIFICATION_PLAN.md)) и механизмы
  под self-host-паттерны — модульные контракты (ensures вызова — факт
  вызывающего), охраняемые обновления в циклах (пул + курсор),
  `!= 0` для делителей, off-by-one (`i + 1 <= n` из `i < n`),
  вычитание курсоров, границы у целей присваивания
- [x] Кирпичи self-host: char↔u8, enum с payload, байтовый I/O, модули,
  `var self`, литерал заполнения массива (кейсы 19–25 в `tests/verify/`)
- [x] Self-hosting, фаза 1 — лексер на EATLang ([selfhost/](selfhost/),
  [план](docs/SELFHOST.md)): стриминговый конечный автомат, полный
  синтаксис языка; `make verify_selfhost` сверяет его дамп токенов
  с эталоном `eatc lex` на каждом .eat репозитория, интерпретатор ==
  бинарник; попутно интерпретатор стал байт-точным (строки — байты,
  как в нативном рантайме)
- [x] Self-hosting, фаза 2 — парсер на EATLang: автомат с явным стеком
  кадров (рекурсии в языке нет), шунтинг-ярд для выражений, пул узлов
  AST, интерполяция строк без рекурсии; дамп AST байт-в-байт совпадает
  с эталоном `eatc parse` на каждом .eat репозитория, парсер разбирает
  сам себя; попутно в бутстрапе: копирование агрегатов через memcpy
  (пулы в 64К элементов роняли LLVM), стек бинарников 64 МБ
- [x] Self-hosting, фаза 3a — декларации и сигнатуры на EATLang:
  интернированный пул типов, таблицы имён, resolve/const_eval как
  итеративные машины, циклы типов, правила 4–5, main; дамп сигнатур
  совпадает с эталоном `eatc sig`, чекер проверяет сам себя
- [x] Self-hosting, фаза 3b — тайпчекер тел на EATLang: машины
  выражений и инструкций с явными стеками, области видимости
  (правила 6, 10), исчерпывающий match, DAG вызовов и глубина стека
  (правило 1); типизированный дамп совпадает с эталоном `eatc typed`,
  тайпчекер типизирует сам себя (≈29 000 строк дампа байт-в-байт)
- [x] Self-hosting, фаза 4 — эмиссия текстового LLVM IR на EATLang:
  принтер llvmlite воспроизведён байт-в-байт (модель имён, буферизация
  блоков, инлайн-строки типов, дедупликация глобалов); IR всего
  фронтенда совпадает с эталоном `eatc ir`, собранный clang'ом из
  self-IR тайпчекер равен эталону; попутно в бутстрапе: alloca
  подняты в entry-блок (кадр функции статичен в циклах)
- [x] Self-hosting, фаза 5 — bootstrap: предел токенов 131 072
  (SPEC §6), пулы фронтенда выросли (вложенные массивы в лексере,
  65 536 узлов AST, 1 024 функции, 4 096 рёбер), стек 128 МБ;
  `make verify_bootstrap` — компилятор эмитит IR самого себя
  байт-в-байт с эталоном, и собранный из этого IR stage2 эмитит
  тот же IR (фикспойнт: компилятор собирает сам себя)
- [x] Self-hosting, фаза 6 — рантайм на EATLang: логика строк,
  интерполяции, `read_line`/`parse_i32` — модуль `selfhost/Rt.eat`,
  первый модуль каждой программы, с контрактами и test-блоками;
  новый builtin `write_byte` — примитив вывода; в C остался шим из
  трёх аксиом ОС (`read_byte`, `write_byte`, `trap`, ~30 строк);
  фикспойнт девяти модулей — компилятор собирает собственный рантайм
