[project]
name = "eatlang"
version = "0.0.1"
description = "Компилируемый, статически типизируемый, тотальный язык — Power of 10 NASA зашиты в дизайн"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "llvmlite>=0.48.0",
]
