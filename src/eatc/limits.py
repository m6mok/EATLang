"""Пределы компилятора (SPEC.md §6). Фиксированные пулы вместо кучи."""

MAX_STMTS_PER_FUNC = 60  # правило 4 NASA
MAX_PARAMS = 6
MAX_BLOCK_DEPTH = 8
MAX_EXPR_DEPTH = 32
# 262144: самоприменение верификатора (Rt + lib + фронтенд + Verify +
# VerifyMain одним входом) — 138 039 токенов; 2×запас, 4 банка по 65536
MAX_TOKENS_PER_FILE = 262_144
MAX_AST_NODES = 131_072  # ёмкость пула узлов self-hosted парсера (2 банка)
# 2048 (LIMITS_PLAN этап 1, 2026-07-18): осознанно поднятый потолок —
# самохост-компилятор ~1006 функций, band-aid инлайн-хинтов §7.6 упирался
# в 1024. Пулы function-индексируемых сущностей в selfhost/Check/Ir/Verify
# расшиты 1024→2048, кодировка графа сдвинута 1024/2048→2048/4096.
# Флаг --limits (этап 2) лишь опускает enforcement ниже этого физического
# максимума; поднятие выше = пересборка (размер пула — литерал, кучи нет).
MAX_FUNCS_PER_PROGRAM = 2_048
MAX_STR_CAPACITY = 256  # == EAT_STR_CAP рантайма: тип не обещает больше буфера
MAX_ARRAY_ELEMS = 65_536
MAX_MODULES = 64  # именованных модулей в потоке (#module)
MAX_IMPORT_BINDS = 512  # импортированных имён на программу
MAX_MODULE_PATH = 128  # длина канонического пути модуля в байтах
# comptime (§5): предел шагов на ОДИН constexpr-вызов (не суммарный —
# иначе порядок объявлений влиял бы на успех). Шаг — одно вычисленное
# выражение (eval) или инструкция (exec_stmt) вычислителя; определение
# фиксировано в SPEC §6 и зеркалится в selfhost Eval.eat байт-в-байт.
# 1e6: таблица CRC 256×8 ≈ 20К шагов, запас ×50.
MAX_COMPTIME_STEPS = 1_000_000
# глубина comptime-вызовов: ёмкость пула кадров вычислителя selfhost
# (COMPTIME_PLAN §9.6) — та же граница у Python-эталона для паритета
MAX_COMPTIME_CALL_DEPTH = 64
