# Кросс-языковое сравнение: EATLang против C, Rust, Go, Python

Дата: 2026-07-17. Машина: Apple M2, 16 ГБ, macOS 26.3.1.
Тулчейны: eatc (Python 3.11.14 + llvmlite, LLVM O2 + clang -O2 линк),
Apple clang 21.0.0, rustc 1.97.1 (`-C opt-level=2`), go 1.24.3,
CPython 3.11.14. Методика и оси — план
[CROSSLANG_BENCH_PLAN](../../docs/plans/CROSSLANG_BENCH_PLAN.md),
раннер — [crosslang/](crosslang/README.md). Прогон: `make
bench_crosslang`, 12 мин; полные таблицы всех вариантов — в
`build/bench/crosslang/results.json`.

Все порты сверены **байт-в-байт** с EATLang-бинарником (микро — единый
REPEAT, макро — два лёгких ROM); стена — медиана ≥ 5 прогонов;
инструкции/циклы — `/usr/bin/time -l` (instructions retired / cycles
elapsed).

## Скорость исполнения (нс/оп; меньше — лучше)

| Нагрузка | EATLang | C -O2 | Rust | Go | Python | EAT/C |
| --- | --- | --- | --- | --- | --- | --- |
| arith — u32-арифметика | 1,23 | 1,17 | 1,16 | 1,45 | 32,7 | 1,05 |
| sort — вставки, data-dependent | 0,39 | 0,41 | 0,41 | 0,61 | 33,6 | 0,95 |
| branch — диспетчер по битам | **1,10** | 2,47 | 2,43 | 1,21 | 84,7 | **0,44** |
| bank — банкованная память | 4,06 | 4,16 | 4,40 | 5,27 | 154,5 | 0,97 |
| strcmp — сборка+сравнение строк | **25,8** | 67,8 | 46,2 | 43,1 | 129,5 | **0,38** |
| u64 — 64-битная арифметика | 4,45 | 4,42 | 4,65 | 4,93 | 168,5 | 1,01 |
| mos6502 — эмулятор, нс/шаг | 3,63 | 3,25 | 4,35 | 5,58 | 514,8 | 1,12 |

Геометрическое среднее EAT/C по семи нагрузкам — **0,79** (EATLang на
21 % быстрее); без двух выбросов (branch, strcmp) — **1,02**: паритет
с C в пределах шума. Rust — та же LLVM-лига (±5 %), Go стабильно
+15…70 %, Python — в 26–40 раз медленнее на микро и в 142 раза на
эмуляторе.

Оба выброса — не артефакт замера, а различие кодогенов, проверено
дизассемблированием:

- **branch ×2,2**: clang и rustc сворачивают if/elif-диспетчер в
  безветвенную цепочку `csel` — считают все четыре плеча каждую
  итерацию и завязывают их в длинную цепь зависимостей; EATLang (и Go)
  оставляют живые ветвления, которые предсказатель M2 обрабатывает
  быстрее (14,5 инстр/оп против 23; IPC 3,8 против 2,8).
- **strcmp ×2,6 к C**: интерполяция `"{x}"` компилируется в
  специализированный `Rt.append_u32` (без разбора format-строки и без
  аллокаций); у соперников — их естественные идиомы: C `snprintf`
  (парсинг формата каждый вызов), Rust `format!` (аллокация String),
  Go `strconv` + конкатенация. Меряется цена идиомы языка, не
  качество бэкенда — это осознанный выбор методики.

## Эффективность кода для процессора

Инструкций на операцию (плотность кода) и IPC — по базовым вариантам:

| Нагрузка | EAT инстр/оп | C | Rust | Go | EAT IPC | C IPC |
| --- | --- | --- | --- | --- | --- | --- |
| arith | 6,0 | 6,0 | 6,0 | 7,0 | 1,50 | 1,50 |
| sort | **4,8** | 6,3 | 6,3 | 10,9 | 3,82 | 4,60 |
| branch | **14,5** | 23,0 | 23,0 | 14,5 | 3,82 | 2,84 |
| bank | 21,1 | 19,1 | 19,1 | 26,2 | 1,56 | 1,36 |
| strcmp | **492** | 1208 | 932 | 771 | 5,94 | 5,50 |
| u64 | 12,0 | 11,1 | 11,1 | 12,1 | 0,78 | 0,70 |
| mos6502 | **54,8** | 58,3 | 56,2 | 94,4 | 4,44 | 5,31 |

EATLang-код по плотности не уступает C/Rust (на sort — заметно
плотнее: банкованные границы уже доказаны и LLVM получает чистые
guarded-циклы), Go тратит на 20–70 % больше инструкций на ту же
работу. Низкий IPC у arith/bank/u64 — у всех языков сразу:
loop-carried зависимость (следующая операция ждёт предыдущую),
процессору нечем заполнить такты; высокие IPC (4–6) — там, где есть
независимая работа. Python исполняет 726–7969 инстр/оп при IPC 5–7:
интерпретатор занят собой, а не задачей.

## Налог на безопасность (внутри каждого языка)

Дельта времени «вариант с проверками ↔ без» на одной и той же
нагрузке:

| Нагрузка | EAT: все-trap ↔ build | C: UBSan ↔ -O2 | Rust: safe ↔ unchecked | Go: bounds ↔ -B |
| --- | --- | --- | --- | --- |
| arith | −3 % (шум) | 0 % | — (нет массивов) | 0 % |
| sort | −3 % (шум) | **+70 %** | +0 % | **+34 %** |
| branch | **+41 %** | −5 % (шум) | — | −4 % |
| bank | +1 % | +7 % | +6 % | −4 % |
| strcmp | −4 % (шум) | +2 % | — | +1 % |
| u64 | +5 % | +15 % | — | −3 % |
| mos6502 | −3 % (шум) | +14 % | — | — |

Главный вывод оси: **у EATLang налог на безопасность почти нулевой** —
верификатор доказал и снял проверки там, где они стоили бы дорого
(sort/bank: маска и границы доказаны статически), а оставшиеся trap'ы
почти бесплатны (предсказуемые никогда-не-взятые ветки; на u64 +5 % —
цена недоказанных переполнений). Единственный крупный выигрыш элизии —
branch (+41 %: доказанные сдвиги и переполнения убирают инструкции из
цепи зависимостей). Для сравнения: Rust платит ~0–6 % (LLVM сам снимает
bounds-check по маске), Go — до +34 % на сортировке, UBSan для C —
до +70 %. Обратная сторона: у C/Rust-вариантов «без проверок» при
ошибке — UB/тишина, у EATLang все недоказанные проверки остаются
trap'ами всегда.

Оговорка: у EATLang «все-trap ↔ build» проходит через разные конвейеры
(clang -O2 по .ll против llvmlite O2 + линк объектника), поэтому
дельты ±3–5 % — шум конвейера, а не эффект проверок; сигнал только
branch и u64.

## Компиляция, размер, память

| Метрика | EATLang | C | Rust | Go | Python |
| --- | --- | --- | --- | --- | --- |
| Сборка микробенча | 0,19–0,21 с | 0,05–0,07 с | 0,09–0,59 с | 0,05–0,40 с | — |
| Сборка эмулятора | 0,59 с | 0,12 с | 0,58 с | 0,13 с | — |
| Бинарник (strip) | **50 КБ** | 32 КБ | 364–380 КБ | 2,2 МБ | — |
| RSS микро | **0,9 МБ** | 0,9 МБ (UBSan 10,7) | 0,9 МБ | 2,2–8,0 МБ | 5,8–6,2 МБ |
| RSS эмулятора | 1,0 МБ | 0,9 МБ | 1,0 МБ | 2,4 МБ | 7,8 МБ |

Сборка eatc — это Python-бутстрап (llvmlite): 0,2 с на микробенч —
медленнее clang, быстрее худших случаев rustc/go в этом прогоне.
Бинарники EATLang компактны (50 КБ — это Rt.eat-рантайм целиком);
память — как у C: всё в стеке и статике, кучи нет по построению.

### Сборка селфхост-компилятором (второй прогон, 2026-07-17)

Вариант «EATLang selfhost -O» — цепочка `SelfIrOpt` (компилятор,
написанный на EATLang; ось `-O`: свёртка + элизия) + `clang -O2` +
runtime.c; IR сверен с `eatc ir -O` байт-в-байт перед замером.

| Сборка | микробенч | эмулятор mos6502 |
| --- | --- | --- |
| eatc build (Python + llvmlite) | 0,18–0,19 с | 0,70 с |
| **SelfIrOpt + clang** | **0,11–0,13 с** | **0,23 с** |
| — из них чистая эмиссия SelfIrOpt | 0,01 с | 0,02–0,03 с |
| clang -O2 (порт C) | 0,05 с | 0,10 с |
| rustc -C2 | 0,09–0,69 с | 0,65 с |
| go build | 0,05–0,22 с | 0,05 с |

Сам компилятор на EATLang эмитит IR эмулятора за **20–30 мс** — в
~25 раз быстрее Python-эталона (который, справедливости ради, ещё
гоняет verify и test-блоки); почти всё время цепочки — это clang.

По скорости исполнения строка «selfhost -O» легла в тот же коридор
±5 %, но вскрыла нюанс: на branch (1,52 против 1,09 нс/оп у build) и
эмуляторе (3,96 против 3,79/3,58) она медленнее обоих других
EATLang-вариантов при меньшем числе trap'ов. Первичная гипотеза —
«`llvm.assume`-факты оси `-O` съедают выигрыш элизии» — замером
этапа 7.1 **опровергнута** (2026-07-17): в branch-варианте оси `-O`
assume нет вообще (0 вызовов — IR совпадает с build с точностью до
trap-строк), а снятие всех 15 assume эмулятора не сдвинуло
инстр/шаг (56,32 → 56,29; стена в коридоре шума ±2,5 % канона).
Реальные причины разрыва: на branch — конвейер сборки (llvmlite у
build против clang по .ll, OPTIMIZATIONS_PLAN §7.2), на эмуляторе —
сама элизия оси `-O` против канона при том же clang (инстр/шаг
56,3 против 54,6: снятый trap-блок уносит и range-факт ветки,
профиль — §7.3). `llvm.assume` как инертный снят из эмиссии
(OPTIMIZATIONS_PLAN §7.1).

Замер §7.2 (2026-07-17, `backends.py`: оба бэкенда на
байт-идентичном .ll build-пути) закрыл вопрос конвейера: clang-путь
нигде не быстрее штатного — branch +28…+31 % к build (кодоген),
mos6502 +4,9…+6,8 % (мид-энд), остальное паритет; llvmlite 0.48
несёт LLVM 22.1.0 против Apple clang 21.0.0. Маршрут `eatc build`
остаётся llvmlite; `-flto` — ось размера (33К против 50К), не
скорости. Числа — FINDINGS, 39-я итерация.

Между полными прогонами дрейф ±5 % по стене — нормален; таблицы выше
из первого прогона, селфхост-числа — из второго
(`results.json` — всегда последний прогон).

### HTTP-парсер (третий прогон, 2026-07-17)

Бенч `http` (HTTP_PLAN этапы 0–3): 2 000 запросов на REPEAT=1 — тот же
парсер, что `lib/Http.eat` (request-line + заголовки в фиксированные
буферы, срезы-Header, OWS-трим, регистронезависимый поиск, мини-роутер
c параметром пути, решение keep-alive); порты C/Rust/Go/Python 1:1,
9 вариантов сходятся байт-в-байт. Сборка — драйвером (`--lib`):
HttpBench → `lib/Http.eat` → `lib/Fmt.eat`.

| Вариант | нс/запрос | инстр/запрос | IPC |
| --- | --- | --- | --- |
| EATLang build | 305 | 4 775 | 4,77 |
| EATLang все-trap | 376 | 5 119 | 3,93 |
| EATLang selfhost -O | 304 | 4 734 | 4,68 |
| C -O2 | 226 | 2 834 | 3,80 |
| C -O2 UBSan | 336 | 4 730 | 4,25 |
| Rust safe | 232 | 3 338 | 4,19 |
| Go | 417 | 5 965 | 4,26 |
| Python 3.11 | 7 834 | 157 245 | 5,18 |

EAT/C = 1,35, EAT/Rust = 1,31; EAT быстрее Go в 1,37 раза и Python в
26 раз. Налог проверок: все-trap/build = 1,23 (элизия верификатора
возвращает бо́льшую часть), у C UBSan = 1,48. Селфхост-цепочка
(`SelfIrOpt`) — паритет со штатной сборкой (304 против 305 нс).
Сырые числа — `results.json` прогона.

Декомпозиция разрыва (замер по частям, 2026-07-17): гипотеза «виновата
интерполяция» **опровергнута** — на чистой сборке строк EATLang
**быстрее** C в 5,8 раза (8,7 против 50,2 нс/строка: `snprintf`
парсит формат в рантайме, интерполяция компилируется в
специализированные аппенды RtStr). Инициализация `Req` — паритет
(99 против 95 нс: LLVM элидирует by-value копии возврата; обнуление
`raw[8192]` — ~95 нс, треть-половина цены запроса у обоих). Весь
разрыв сидит в **побайтовом цикле парсера**: на константных строках
полный GET стоит 458 против 191 нс (×2,4; за вычетом общего memset —
×3,7), 8 291 против 3 074 инстр/запрос при полностью заинлайненном
горячем пути (нуль вызовов) и лишь +23 % от рантайм-проверок. Из
одинаковой семантики clang выжимает из C ~27 инстр/байт, конвейер
EATLang — ~74; высокий IPC 4,77 — признак «много дешёвой лишней
работы». Разведка §8.3 (FINDINGS, 2026-07-17) нашла её источник:
идиома «скан с нуля под стражем» в `find_sp` и скане двоеточия
`parse_header` (статические границы циклов Power of 10) — 75 %
разрыва в инструкциях и ≈97 % в стене; первоначальная атрибуция
«поля не промотируются без TBAA» замером опровергнута.

Проверка бэкендов по методике §7.2 (`backends.py`, тот же
байт-идентичный .ll, два прохода, дрейф ≤ 3 %): **паритет всех пяти
вариантов** — build (llvmlite) 301–306 нс/запрос, `.ll → clang -O2`
302–305, `-flto` 300 (выигрыш только в размере: 33К против 68К),
гибриды мид×кодоген 303–314. Конвейер **оправдан**, смена бэкенда
из рычагов вычеркнута. Гипотеза «причина — отсутствие TBAA в .ll»
опровергнута разведкой §8.3 (`tbaa.py`, FINDINGS 2026-07-17):
TBAA-прототип даёт −1,2 % инстр и 0 стены, clang без strict
aliasing не теряет на C-порте ни инструкции; разрыв несёт идиома
сканов с нуля в lib/Http.eat (C с этой идиомой равен EATLang: 199
против 202 нс/запрос). Рычаги: span-API парсера + единый проход
сканов от позиции (§8.1); ленивое обнуление `raw` — сделано §8.2.

### 128-битная арифметика U128 (четвёртый прогон, 2026-07-17)

Замер-гейт этапа 4 [U128_PLAN](../../docs/plans/U128_PLAN.md)
(intrinsics — «только по замеру»). Две нагрузки: `u128` — микс без
деления (шаг LCG128 `state*MUL + INC` mod 2^128 на константах PCG64 +
точное произведение `mul_64`), `u128div` — деление (400 полных
`divrem` + 4000 `divrem_32` на виток, делители 64-битные — профиль
клиентов Q32.32/Deci6/печати). EATLang считает программными
32-битными лимбами `lib/U128.eat` (вариант A), C/Rust — нативным
`unsigned __int128`/`u128`, Go — идиоматичной парой `uint64` через
`math/bits` (Mul64/Add64/Div64, как в реализациях PCG), Python —
`int`. 9 вариантов байт-в-байт; сборку EATLang ведёт драйвер импортов
(режим `"driver"`, общий с http).

Первый прогон (до оптимизации): микс EAT build 13,7 нс/оп — EAT/C =
×6,1 (вариант «все-trap» 11,0 — ×4,9), внутри вилки плана «5–15×»;
деление blend 50,6 нс — ×8,4, раскладка: `divrem_32` ≈ 3,9 нс —
паритет с нативным, полный сдвиговый `divrem` ≈ 490 нс (3,8 нс × 128
итераций) — ×30 к C, единственное узкое место.

**Оптимизация по замеру** (2026-07-17, тот же день): `divrem` стал
диспетчером по ширине делителя — 1 лимб → `divrem_32`, 2 лимба →
новый `divrem_64` (Кнут D по базе 2^32: нормализация `clz64`, две
цифры `div_step` с коррекцией ≤ 2, чистое деление ~30 нс — ×14 к
сдвиговому), шире → прежний сдвиговый `divrem_wide`; `mul`/`mul_wrap`
— прямая школьная свёртка по лимбам (10 произведений, MAC-лемма)
вместо композиции трёх `mul_64` + `add_wrap`. Повторный прогон:

| Вариант | u128 (микс), нс/оп | u128div, нс/оп |
| --- | --- | --- |
| EATLang build | 10,5 (было 13,7) | **6,67 (было 50,6)** |
| EATLang все-trap | **9,7** | 6,75 |
| EATLang selfhost -O | 10,2 | 6,58 |
| C -O2 | 2,35 | 6,06 |
| Rust safe | 2,37 | 5,33 |
| Go | 2,39 | 8,99 |
| Python 3.11 | 272 | 486 |

- **Деление: EAT/C = ×1,10 — паритет с нативным `__int128`** (Rust
  ×1,25; Go медленнее EATLang). Инстр/оп 58 против 508 до
  оптимизации (−89 %): у клиентов Q32.32/Deci6/печати деление больше
  не узкое место. Сдвиговый ×30-путь остался только у честных
  128-битных делителей (клиентов нет).
- **Микс: EAT/C = ×4,5** (все-trap ×4,1; было ×6,1/×4,9) —
  прямолимбовая свёртка сняла ~23 %. Остаток — цена лимбовой
  эмуляции (102 инстр/оп против 14,5 у C: у нативного 128-бит `mul`
  — две инструкции). Канон-IR стабильно быстрее build-элизии на ~8 %
  (до оптимизации ~25 %) — бекенды по дозамеру 7.2 в паритете,
  значит это кроссовер «аутлайн × элизия» из §7.3 (trap-icmp
  восстанавливает диапазоны через границы неинлайнящихся методов);
  профиль «глубокие цепочки мелких методов lib/» — второй свидетель
  кандидата «инлайн-хинты малых листовых» (OPTIMIZATIONS_PLAN §7.3).
- Вердикт этапа 4 не меняется: intrinsics не оправданы — деление
  добито алгоритмом без правок языка, остаток микса упирается в
  конвейер/инлайнинг, а не в отсутствие инструкции.

### HTTP-парсер: переиспользование кадра Req (пятый прогон, 2026-07-17)

Рычаг §8.2 реестра OPTIMIZATIONS: кадр запроса не пересоздаётся —
`Req.reset(var self)` сбрасывает скалярные поля, `raw[8192]`/`hdr` не
трогаются (обнуление стоило ~95 нс/запрос обоим языкам; инвариант
чистоты разбора — докстринг и test-блок `http_reset_reuse` в
`lib/Http.eat`). Порты зеркальны (reset вместо memset/пересоздания),
9 вариантов сходятся байт-в-байт, чек-сумма не изменилась. Повторный
прогон (до/после в одном стенде):

| Вариант | нс/запрос | было | инстр/запрос | IPC |
| --- | --- | --- | --- | --- |
| EATLang build | **205** | 294 | 4 045 | 5,91 |
| EATLang все-trap | 279 | 370 | 4 384 | 4,69 |
| EATLang selfhost -O | 206 | 295 | 3 998 | 5,81 |
| C -O2 | **123** | 221 | 2 091 | 5,19 |
| C -O2 UBSan | 235 | 337 | 3 994 | 5,06 |
| Rust safe | 140 | 245 | 2 595 | 5,49 |
| Go | 220 | 429 | 4 229 | 5,75 |
| Python 3.11 | 7 584 | 8 159 | 154 645 | 5,22 |

Честная оговорка: рычаг двигает абсолют всех языков, а у C memset
был 42 % цены запроса против трети у EATLang — отношение EAT/C
**выросло** 1,33 → 1,67 (EAT/Rust 1,20 → 1,46), это ожидаемо: сервер
быстрее, бенч честнее. Остаток разрыва — целиком побайтовый цикл
парсера (рычаги §8.1/§8.3). Подробности и полная таблица до/после —
FINDINGS, «HTTP: переиспользование кадра Req».

### HTTP-парсер: span-парс и прямые сканы (шестой прогон, 2026-07-17)

Рычаг §8.1 реестра OPTIMIZATIONS — замыкает разрыв, найденный
разведкой §8.3 (носитель — идиома «скан с нуля под стражем», 75 %
инструкций разрыва): `find_sp` и скан двоеточия `parse_header` идут
прямо от позиции (requires-цепочка доказывает границы, trap-ветвей
в теле нет), `feed_line`/новый батч `feed_str` ведут строку одной
спан-полосой `span_copy` вместо побайтового автомата (n/ls — раз на
строку; редкие LF-в-строке и кромка 413 — на прежнем `push_byte`).
Правка только lib/Http.eat, порты не менялись (C сканирует прямо с
рождения), 9 вариантов сходятся байт-в-байт, чек-сумма та же:

| Вариант | нс/запрос | было | инстр/запрос | IPC |
| --- | --- | --- | --- | --- |
| EATLang build | **124** | 203 | 2 505 | 6,37 |
| EATLang все-trap | 119 | 277 | 2 360 | 5,97 |
| EATLang selfhost -O | 118 | 206 | 2 306 | 5,92 |
| C -O2 | **116** | 116 | 2 091 | 5,42 |
| C -O2 UBSan | 234 | 233 | 3 999 | 5,08 |
| Rust safe | 141 | 145 | 2 594 | 5,50 |
| Go | 220 | 222 | 4 229 | 5,74 |
| Python 3.11 | 7 624 | 7 486 | 154 684 | 5,16 |

EAT/C = 1,75 → **1,06**, EAT обгоняет Rust safe (124 против 141) и
Go вдвое; налог все-trap/build = 0,96 — канон со всеми проверками
не медленнее элизии. Суммарно по реестру §8: 305 → 124 нс/запрос
(×2,5) при неизменных портах. Гипотеза «состояние в локалах на
байт» исходного промта §8.1 замером опровергнута (три формы автомата
на локалах медленнее базы — store-forwarding Apple silicon делает
круговорот полей почти бесплатным); выиграли прямые диапазоны и
меньшее число проходов. Подробности — FINDINGS, «HTTP: span-парс
lib/Http.eat».

### TODO-list: модульный накопитель в верификаторе (восьмой прогон, 2026-07-17)

Рычаг из седьмого прогона (разрыв `todo` EAT/C = 1,17 — весь в
`Store.serialize`) снят **в верификаторе**, не в примере (OPTIMIZATIONS
§9). Накопитель контрольной суммы `sum = (sum*31 + x) % 65536` пишется
во вложенном цикле свёртки; loop-carried анализ (линейные индукции) на
входе витка расширял `sum` до полного диапазона типа, оставляя 6
overflow-проверок (`umul ×3`, `uadd ×3`). Новый проход `_mod_carry`
(зеркало байт-в-байт в selfhost): накопитель, все определения которого —
`_ % K` (K — литерал), несёт `[0,K-1]` на входе витка. Проверки сняты,
пример и порты не тронуты.

| вариант | нс/оп было → стало | инстр/оп было → стало |
| --- | --- | --- |
| EATLang build todo | 195,8 → **166,5** | 1 301 → **1 031** |
| C -O2 todo | 167,0 | 1 025 |
| EATLang build http | 111,4 → 111,1 | 2 410 (без изм.) |

**EAT/C `todo` = 1,17 → 1,00** (паритет с C: инстр/оп 1 031 против
1 025); `http` держит **0,93**. Чек-суммы 46615 (todo) / 17664 (http)
и сверка 9 вариантов байт-в-байт неизменны; канон `eatc ir` не тронут
(сворачивает ось `-O`); полный гейт 10 целей зелёный. Класс снят целиком
(любой `acc = f(acc) % K`), а не один бенч — рычаг в интервальном
анализе. `main.acc` тем же классом получил паритет; `acc + next_id`
корректно остался проверяемым (счётчик неограничен).

## Честные оговорки

1. **EATLang, C и Rust делят бэкенд LLVM** — сравнение микробенчей
   во многом меряет фронтенды/семантику, а не три независимых
   компилятора. Go (свой бэкенд) и Python (интерпретатор) — настоящие
   внешние точки.
2. **Порты 1:1, а не идиоматичные программы**: C-программист написал
   бы strcmp через ручной itoa и обогнал бы `snprintf`; сравнивается
   «та же программа», а не «лучшая программа на языке».
3. **Сравнивается только выразимое на EATLang** подмножество задач:
   без кучи, рекурсии, неограниченных циклов. Задачи, где C/Rust/Go
   используют динамические структуры, в сравнение не попадают
   по построению.
4. Микробенчи чувствительны к капризам планировщика LLVM (см. branch);
   макробенч-эмулятор — самая представительная строка таблицы:
   паритет EATLang с C в пределах 12 % при полностью доказанной
   памяти (все обращения к mem[65536] — без проверок в рантайме,
   маска доказана верификатором).
5. Python-порту вычтен старт интерпретатора (~18 мс); REPEAT у него
   меньше (нормировка на операцию).

## След

- Порты и раннер: `tests/bench/crosslang/` (гоняются `make
  bench_crosslang`); сырые числа прогона — `results.json`.
- Кандидаты продолжения: порт mos6502 на idiomatic-Rust (таблица
  опкодов) как верхняя референс-точка; повтор прогона на x86-64;
  строка «сборка селфхост-цепочкой» ✅ добавлена вторым прогоном.
- Рычаги оптимизации EATLang, извлечённые из замера, — реестр
  [OPTIMIZATIONS_PLAN §7](../../docs/plans/OPTIMIZATIONS_PLAN.md).
