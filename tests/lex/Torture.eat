# Стресс-тест лексера: краевые случаи токенизации (файл только
# лексируется, компилировать его не требуется).

func ops() {
    a = b -> c .. d == e != f <= g >= h < i > j
    k = -1 + 2 - 3 * 4 / 5 % 6
    m.n = p[q].r(s, t)
    u = v.w..x.y
    dot = one.two
    arrow2 = a->b
}

struct Str1ngs {
    s1: str<16>
}

func strs() {
    let s2: str<32> = "escape: \n \t \\ \" \' \0 конец"
    let s3: str<8> = ""
    let s4: str<64> = "скобки {{ не интерполяция"
    let s5: str<32> = "число {x} и {y.z} рядом"
    let c1: char = 'a'
    let c2: char = '\n'
    let c3: char = '\''
    let c4: char = '\\'
    let c5: char = ' '
    print("кириллица в строке") # комментарий после: -> .. == != <= >=
}

# многострочные скобки: NEWLINE подавляется
func multi(
    aaa: u32,
    bbb: [u32; 4]
) -> u32 {
    let z: [u32; 3] = [
        1,
        2,
        3
    ]
    return aaa
}

func кир_идент(число: u32) { # не-ASCII идентификаторы, как у эталона
    let итог: u32 = число + 1
}

test смешанное {
    assert 1 <= 2 and 2 >= 1 or not false
    x=y
    a5=67+b8
}
