#! expect: error=цикл в comptime-константе
# Негатив: constexpr -> функция -> та же constexpr — цикл резолюции
# (правило 1 покрывает только func -> func).

constexpr A: u32 = viaA(1)

func viaA(x: u32) -> u32
    requires true
    ensures true
{
    return x + A
}

func main() {}
