#! expect: overflow=1/1 assert=1/1
# Точный интервал аккумулятора после цикла.

func main() {
    let sum: i32 = 0
    for _ in 0..100 {
        sum = sum + 7
    }
    assert sum == 700
    print("{sum}")
}
