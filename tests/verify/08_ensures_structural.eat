#! expect: ensures=1/1 overflow=2/2 requires=1/1
# Структурное равенство: ensures повторяет выражение return.

func double(x: i32) -> i32
    requires x > -1073741824 and x < 1073741824
    ensures result == x * 2
{
    return x * 2
}

func main() {
    print("{double(21)}")
}
