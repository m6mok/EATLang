# Ветвление if / else.

func print_parity(number: i32)
    requires true
{
    if number % 2 == 0 {
        print("{number} is even")
    } else {
        print("{number} is odd")
    }
}

func main() {
    print_parity(8)
}
