# Арифметика и иммутабельные переменные.
# Переполнение i32 — это trap, а не молчаливый wrap.

func main() {
    const a: i32 = 10
    const b: i32 = 5
    const result: i32 = (a + b) * 2
    print("Result is: {result}")
}
